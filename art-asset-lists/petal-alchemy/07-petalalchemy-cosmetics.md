<!-- Petal Alchemy · Sheet 7: Cosmetics — Node frames + Log parchments + wardrobe furniture -->
<!-- 💰 COSMETICS / ECONOMY sheet. Copy everything below into your image generator to make this ONE sheet. -->

STYLE — "Midnight Apothecary" (Petal Alchemy / Sky Wolf Studios candlelit crafting-sandbox). Collectible workshop cosmetics: enamel tile frames and parchment folio panels, brass-and-glass lab charm under warm candle-gold light in a deep midnight-green room. Chunky rounded silhouettes, ONE warm key-light upper-left + cool dew fill each, soft inner glow, restrained bloom, never neon; reads at thumbnail. COLORBLIND RULE: the four node frames MUST differ by corner ORNAMENT SHAPE (plain hairline / riveted / pressed petals / leaded glass), never by color alone, because they wrap tiles whose printed cream name labels are the game's readability guarantee — and art NEVER bakes text. Flat front-on emblem camera, centered, upright. Palette (the game's real CSS colors): midnight greens #0d100c / #0b0f0b / #0e140d / #0f150c / #101610, void #05070a, tile-face #182013→#10160d, hairline #2a331f, sage #7ab356 + deep leaf #3f6b34 / #4a6b30 / #5c8f3f, brass-gold #c8a84b + candle-bloom #ffe9a8 + amber-dark #141005, cream #e8dcc8, moss #8a9178, petal-pink #e58fa0 + rose glaze #d98fa6→#b2657c + blush rim #eeb0c2, wine #6f2f42 / #3a1622, water-blue #5b9bd5, dew #bfe0f2, tincture-violet #b57de0. Soft cel enamel + glaze sheen, subtle grain, painterly not photoreal, NO harsh outlines, NO text/letters/numbers/logos/watermarks. Compress under 150KB.

Create one sprite sheet. File: pa_cosmetics.png. Grid: 4 columns x 4 rows (16 cells, left-to-right, top-to-bottom). Cell: 512x512. Master: 2048x2048.

KNOCKOUT: Flat magenta #FF00FF fills the entire background of every cell — INCLUDING the open center of every frame (frames are hollow; the magenta shows through the middle and is knocked out at cut time). NO magenta / hot-pink inside the art (petal-pink #e58fa0 stays clearly dusty-rose, distinct from #FF00FF). Each item centered, upright, fully inside its cell with margin, NO ground shadow (these composite over DOM tiles, cards and panels). These are PURELY VISUAL skins — they never change a tile's size, tap target, recipe or printed name.

NODE FRAMES (cells 1-4) — reskins of the element-tile border, all on the SAME rounded-rectangle proportions as the sheet-1 tile plates (about 3:2, hollow center) so the engine can border-image-slice them. Named and gated exactly as the code (`FRAMES[]`, unlock = total discoveries):
1. frame_plain — "Sage" (starter, free): the default look — a fine sage-dark #2a331f hairline frame with the faintest sage #7ab356 glint at the corners; quiet, near-invisible, lets the icons lead. ⚠️ CUT-NAME NOTE: the code id for this frame is `plain`, NOT sage (`FRAMES[0]={id:'plain',nm:'Sage'}`, index.html ~428) — the wire notes apply frames as a `frame-<id>` class, so the cut file MUST be `frame_plain.png` even though the display name is Sage. The other three cells already match their code ids (brass / pressed / stained), as do the logs (sage / parch / gilt).
2. frame_brass — "Brass" (unlock: 20 discovered): a slim polished brass-gold #c8a84b frame with a small round rivet in each corner and a soft candle sheen along the top rail; the working alchemist's upgrade.
3. frame_pressed — "Pressed Flower" (unlock: 50 discovered): a cream-parchment-edged frame with tiny real pressed petals and leaf flecks laminated into each corner (rose, gold and sage tones); scrapbook-precious, the corner petals are its shape signature.
4. frame_stained — "Stained Glass" (unlock: 90 discovered): a dark leaded-came frame whose corners hold tiny stained-glass diamond panes glowing in the game's five keepsake hues (#e58fa0, #c8a84b, #7ab356, #5b9bd5, #b57de0); the cathedral-tier flex for near-complete collectors (90 of 106).

LOG PARCHMENTS (cells 5-7) — reskins of the discovery log / Grove panel, each a tall rounded portrait panel (about 3:4) with an EMPTY center mat where cards and printed text composite. Named and gated exactly as the code (`LOGS[]`, unlock = True Blooms grown):
5. log_sage — "Sage" (starter, free): a deep sage-green cloth-bound journal panel, #0f150c face with a moss-green fabric border and a thin blind-embossed vine line; humble and calm.
6. log_parch — "Parchment" (unlock: 2 True Blooms): a warm aged-parchment panel in muted cream-on-dark — deckled edges, a faint tea-stain ring, one small wax seal dot in wine red at the top edge; the naturalist's folio.
7. log_gilt — "Gilt" (unlock: 4 True Blooms): a midnight folio panel edged in gleaming gold leaf with delicate gilt filigree corners and a soft candle-bloom shimmer; the master alchemist's tome (4 of only 6 possible blooms).

WARDROBE FURNITURE (cells 8-13) — the pieces `renderWard()` and the wardrobe screen use:
8. ward_card_frame — the wardrobe card plate (`.wardcard`): a rounded dark enamel card #0f150c with a thin #2a331f hairline rim and a soft top sheen; EMPTY — the engine composites the cosmetic preview icon, its name and its unlock caption on top.
9. ward_card_locked — the locked variant: same card dimmed and desaturated with a small closed brass padlock centered low; the requirement caption the engine prints stays legible above it.
10. ward_equipped_ring — the "equipped" highlight (`.wardcard.on`, pink border in code): a petal-pink #e58fa0 glow frame overlay that hugs the card edge, with a tiny petal tick at the top center; hollow, composites over cell 8.
11. lock_padlock — a standalone chunky brass padlock with a warm wine-red keyhole glint and rounded shackle; reusable on any locked slot.
12. unlock_badge — the "just unlocked!" celebration: a small gold-and-cream starburst rosette with two ribbon tails in petal-pink. ⚠️ FUTURE / needs-wiring (same honest-gap treatment as `SAVE.log` and `mintKeepsakeFx` in the 00 wire notes): the engine has NO "moment its threshold is met" today — `refreshCosmeticUnlocks()` (index.html ~436) is an EMPTY stub and thresholds are only re-read when `renderWard()` draws the wardrobe. Natural hook: fill that stub (compare `unlocked()` before/after each discovery, then toast + pop this badge). Cut and hold until that code lands.
13. progress_token — a pictographic near-unlock hint: a thin brass arc partly filled with glowing candle-gold, a tiny bloom bud at the goal end — shows "getting closer" with NO numbers or letters. ⚠️ FUTURE / needs-code: NO progress ring exists anywhere in the engine or the wire-notes map today. Concrete wiring target (budgeted in the 00 wire notes): composite this over a locked `.wardcard` in `renderWard()` (~437-446) when the player is past roughly half the requirement (`discCount()`/`bloomCount()` vs `it.need`) — until that lands, this asset is unmapped, cut and hold.

ECONOMY GLINTS (cells 14-16) — small tokens for the earn/streak moments the engine toasts:
14. streak_star — the Daily Recipe streak token (the goal bar's ⭐): a plump rounded five-point gold star with a cream halo and one glint; warm, collectible, no numerals. ⚠️ WIRE NOTE: that ⭐ is a code-printed TEXT glyph inside `#pa-goal` innerHTML — `startMode()` (~347) and `updateDailyGoal()` (~397-398) both print `'streak '+SAVE.streak+' ⭐'` — so replacing it means editing those two strings to append an `<img>` (a two-line code change; budget it, per the 00 wire notes). Until then this cell is DECORATIVE garnish for toasts / screen dressing, not a mapped swap.
15. seal_daily — a wax seal stamp in deep sage with a tiny embossed sprout emblem, pressed and slightly cracked at the edge; marks "today's recipe grown" on the goal ribbon.
16. corner_filigree — a reusable brass filigree corner ornament (a curling vine-and-petal flourish, about a quarter-frame) to dress screen corners, the How panel, or future cosmetic tiers; must tile by rotation into all four corners.
