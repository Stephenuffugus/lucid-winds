<!-- Micro Meadow · Sheet 4: HUD & FX — hearts, Bloom Meter, timer, stamps, prompt plaque, flashes, confetti, button plaque -->
<!-- Copy everything below into your image generator to make this ONE sheet. -->

STYLE — "Firefly Funfair" (Micro Meadow / Sky Wolf Studios blink-fast microgame carnival). Festival-lantern HUD jewelry for a moonlit meadow funfair: chunky rounded pictographic pieces, each with one soft cream rim-light and a gentle inner glow, luminous but never neon-blown, instantly readable at HUD size during five-second rounds. Soft cel + a whisper of gouache grain. Colorblind law: state pairs never differ by hue alone — the low-time timer also FRAYS, the win/miss stamps differ by silhouette (leaf-check vs snapped-twig cross). Flat front-on camera, subjects centered and upright. Palette: meadow-night #0b0f0b #0d100c #141d12 #1a2415, hedge #2a331f, deep moss #3f6b34, stem #5c8f3f, sage #7ab356; festival gold #c8a84b #d9b85a #eed48a, lantern bloom #ffe9a8, bug-yellow #d9c25a; cream #e8dcc8, muted #8a9178; petal pink #e58fa0, ripe red #c83a4a, alarm #e56b6b, plum #7d3450; moon blue #5b9bd5, dew #bfe0f2, violet #b57de0; timber #8a5a2b #5c3a1a. NO photoreal, NO hard black keylines, NO text/letters/numbers/logos/watermarks (icons pictographic only). Compress under 150KB.

Create one sprite sheet. File: mm_ui.png. Grid: 4 columns x 4 rows (16 cells, left-to-right, top-to-bottom). Cell: 512x512. Master: 2048x2048.

KNOCKOUT: Flat magenta #FF00FF fills the entire background of every cell. NO magenta / hot-pink ANYWHERE inside the art (keep petal pink #e58fa0 and violet #b57de0 clearly distinct from #FF00FF). Each piece centered, upright, fully inside its cell with margin, glow contained so it never tints the magenta field, NO ground shadow (these composite onto the HUD and DOM panels). Plates and plaques keep their centers EMPTY where noted — the engine prints all words and numbers.

CELLS — most map to a real HUD/FX draw the wirer swaps 1:1 (fillText/fillRect → drawImage); cells marked **ADDITIVE** have NO existing engine draw call and need NEW render code written when wired (they are deliberate upgrades, still generate them):
1. heart_full — one life: a plump petal-heart in pink #e58fa0 with a cream rim-light and soft inner glow (the engine prints a ♥ row top-left, up to 4; this replaces one glyph, so it must read at ~24px).
2. heart_empty — the lost life: the same heart as a hollow muted-moss #8a9178 outline, dim and quiet; silhouette identical to cell 1 so the row reads as a gauge. WIRE NOTE: the engine's render() (~line 458) prints ONLY the remaining filled ♥ — lost hearts simply vanish, so no empty state is ever drawn today; skinning must switch the hearts row to 4 FIXED slots (full/empty per slot) or this cell stays orphaned.
3. bloommeter_trough — the Bloom Meter trough: a long rounded capsule bar plate in dark #1a2415 with a hedge #2a331f edge (engine bar 300×14 at top-center), EMPTY inside, subtle inner shading.
4. bloommeter_fill — the meter fill: a TILEABLE horizontal sage #7ab356 vine strip with tiny leaf nubs and a bright leading tip, cut to slide inside cell 3 as the meter fills 1/8 per clean win.
5. bloommeter_blossom — **ADDITIVE** (no engine draw today — the meter is a bare bar, ~462–463; needs a new draw call). The goal-end blossom: a small pink #e58fa0 bloom with a lantern-gold #ffe9a8 heart that pops at the meter's right end when the 8th win fills it and a keepsake grows.
6. timer_gold — the round timer strip: a TILEABLE horizontal band in festival gold #c8a84b with a faint lantern sheen (the engine draws a 6px bar above the playfield that drains right-to-left).
7. timer_alarm — the last-third timer strip: the same band in alarm #e56b6b with a FRAYED, sparking trailing edge — shape change plus color change so low time never reads by hue alone (engine swaps color below 30%).
8. stamp_win — the clean-win stamp: a fat bouncing checkmark built from a folded sage #7ab356 leaf with a cream glint (the engine prints a big ✓ above the field for ~0.55s).
9. stamp_miss — the miss stamp: a cross of two snapped alarm #e56b6b twigs with tiny bark cracks (engine ✗); clearly different silhouette from cell 8, sturdy not punishing.
10. prompt_plaque — **ADDITIVE** (the engine prints the bare word with NO plaque behind it, ~469–471; needs a new drawImage under the text). The ringmaster's call plaque: a wide rounded marquee banner in dark #1a2415 timber with festival-gold #eed48a pin-lights along the border, center EMPTY — the engine prints the flashing prompt word (TAP! WATER! …) on it, tinted by the equipped frame.
11. flash_soft — the full-screen flash: a soft radial cream-white #ffe9a8→transparent wash, heaviest at center (the engine tints the whole canvas rgba(234,246,255) on misses and blooms); must fade to nothing well inside the cell.
12. gust_streaks — **ADDITIVE** (the engine has no speed-up flourish at all — advance() ~427 just bumps G.speed silently; needs a new FX pass). The speed-up flourish: three or four soft cream #e8dcc8 horizontal motion streaks carrying a tiny sage leaf each, for the beat when the show gets faster (speed steps every 4 clears).
13. sparkle_win — **ADDITIVE** (no engine sparkle exists — wins draw only the ✓ stamp; needs new particle code). A small lantern-gold #ffe9a8 four-point sparkle with a soft halo, popped over the subject on every clean win.
14. puff_miss — **ADDITIVE** (no engine puff exists — misses draw only the ✗ stamp + flash; needs new particle code). The miss puff: a soft, round, harmless muted-moss #8a9178 dust poof, three lobes, gently comic.
15. petal_confetti — **ADDITIVE** (the engine celebrates a bloom with only the G.flash wash, ~475; needs a new particle burst). Bloom celebration confetti: a loose scatter of 6–8 individual petals in petal pink #e58fa0, festival gold #c8a84b and violet #b57de0 tumbling at varied angles (cut apart for the meter-fill celebration).
16. btn_plaque — the menu button plate: a wide rounded fair-stall plaque, timber #8a5a2b face with a dark #5c3a1a edge and a festival-gold #eed48a top highlight, center EMPTY — reskins the DOM .btn / .btn.primary buttons (engine owns all button labels and emoji).
