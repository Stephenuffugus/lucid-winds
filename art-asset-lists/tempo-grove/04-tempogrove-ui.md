<!-- Tempo Grove · Sheet 4: UI / HUD — buttons, chips, icons, track drawer -->
<!-- Copy everything below into your image generator to make this ONE sheet. -->

STYLE — "Moonlit Metronome" (Tempo Grove / Lucid Winds deco music-hall marquetry). Lacquered wood inlay + brass edging: flat marquetry pieces, thin brass outlines, soft lacquer sheen. Crisp FLAT fills, NO photoreal render, NO glossy 3D, NO harsh black keylines; reads at small size. Palette: void #0d100c/#05070a, walnut #3a2a18/#241a10, sage #7ab356 / deep #3f6b34, brass #c8a84b + hot #ffe9a8 + cream #e8dcc8, indigo #3d478f/#5b6bd5, moss #8a9178, dusk #2a331f, dew #bfe0f2, rose #e58fa0. NO text, letters, numbers, logos, watermarks — every icon is pictographic only. Compress under 150KB.

Create one sprite sheet. File: tempogrove_ui.png. Grid: 4 columns x 4 rows (16 cells, left-to-right, top-to-bottom). Cell: 512x512. Master: 2048x2048.

KNOCKOUT: Flat magenta #FF00FF fills the entire background of every cell. NO magenta / hot-pink / hot-purple inside the art. Subjects centered with generous margin, front-on, NO drop shadow. Button plates must survive 9-slice stretching (keep detail at edges/corners, centers plain).

1. plate_button — the standard button plate: a rounded-rectangle walnut #3a2a18 marquetry plate with a thin brass edge and subtle grain, plain center (9-slice safe).
2. plate_button_primary — the primary CTA plate: same shape in brass #c8a84b with a hot #ffe9a8 top bevel and walnut edge (9-slice safe).
3. plate_chip — a small square control chip plate: walnut with recessed darker center well and brass rim, for the dock move/rotate/drop chips (9-slice safe).
4. icon_left — a solid brass LEFT arrow glyph, deco-faceted, on nothing (transparent-ready icon).
5. icon_right — matching brass RIGHT arrow glyph.
6. icon_rotate — a brass circular-arrow rotate glyph with a small finial tip.
7. icon_drop — a brass double-chevron DOWN glyph (hard drop).
8. icon_note — a brass music-note glyph with a leaf sprouting from the stem (the soundtrack button).
9. icon_pause — two brass pause bars with deco caps.
10. icon_home — a small brass conservatory-arch (home/menu glyph).
11. icon_wardrobe — a brass hanger with a leaf hook (wardrobe glyph).
12. icon_grove — a small walnut shadow-box with a tiny bloom inside (Grove gallery glyph).
13. track_plate — a track-drawer row plate: long rounded walnut bar with a brass left-cap circle sized for a note icon, plain middle (9-slice safe).
14. toggle_knob — the settings toggle: a round brass knob with a cream glint, plus its walnut track behind (drawn as one unit, knob left).
15. meter_frame — the sweep-progress meter: a slim horizontal brass channel frame with deco end-caps, empty center (engine fills the gold gradient).
16. toast_plate — the toast/notification pill: a rounded walnut pill with brass edge and a soft inner glow, plain center.
