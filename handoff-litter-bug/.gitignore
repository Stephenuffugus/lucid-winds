# Stripe secret config — lives on the server only, NEVER committed.
stripe-config.php

# Node modules from jsdom install
node_modules/

# OS / editor cruft
.DS_Store
*.swp
.vscode/
.idea/

# Build artifacts (there shouldn't be any — no build step — but just in case)
dist/
build/
*.log
