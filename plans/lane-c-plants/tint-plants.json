[
  ["t1 the paint's words before the pour ends", "compare", [["main.js", "  pour = state;\n  sound('pour');", "  pour = state;\n  sound('pour');\n  truthEl.textContent = COPY.paintSays + ' ' + COPY.same;"]]],
  ["f1 the white scored one part off", "fill", [["main.js", "  const s = scoreFill(fillTask, whiteValue);", "  const s = scoreFill(fillTask, whiteValue + 1);"]]],
  ["s1 the demonstration ignores less motion", "scales", [["main.js", "    const p = instant ? 1 : Math.min(1, (now - state.startedAt) / DEMO_MS);", "    const p = Math.min(1, (now - state.startedAt) / DEMO_MS);"]]],
  ["w1 the resolve slowed to 600 ms", "pour", [["render.js", "export const RESOLVE_MS = 400, CLOTH_MS = 500, STREAM_MS = 300;", "export const RESOLVE_MS = 600, CLOTH_MS = 500, STREAM_MS = 300;"]]],
  ["o1 the worker deletes every cache", "offline", [["sw.js", "keys.filter(k => k.indexOf('tint-') === 0 && k !== SHELL_VERSION)", "keys.filter(k => k !== SHELL_VERSION)"]]]
]
