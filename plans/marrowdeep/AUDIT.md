# MARROWDEEP, THE AUDIT RECORD
Sep 08 2026. Before a line of the game was written, five lenses were pointed at the spec and at `RULES.md`: the
resolution engine and quest loop, the economy, the mathematics, the screens and the save, and the studio's own laws.
Each read the two documents against each other and against the code that existed. Two of the five ran their findings
past refuters. This file is what they found and what was done with it, so that a rule nobody can explain later can be
traced back to the argument that made it.

**How to read it.** Every finding below either changed `RULES.md`, changed the plan, or became a Director call in the
plan's section 10. Nothing here is advisory and nothing here is a suggestion nobody acted on.

**Three of them are measurements, not readings**, and they are the reason this file matters more than the usual
review: a 20,000 quest simulation of the boss, a 200,000 quest survey of which target numbers the game actually
deals, and a run of the prototype itself. Each found something no amount of reading would have.

⛔ **An honest note about the process.** The first pass ran out of model quota partway: the engine and economy lenses
finished, the other three and every refuter died mid flight. The two that finished were checked by hand against the
spec and applied; the rest were rerun. Where a finding was applied without a refuter, that is why. Separately, two
batches of these rulings were applied by a script that wrote its file only at the end, so when a later assertion
failed the whole batch was silently lost while printing success; they were re applied and each one verified in the
file afterwards.

---

## Lens: ENGINE AND QUEST LOOP

**What it reported:** Walked R1/R3/R4/R5/R7/R9/R12 as an implementer. The dice engine (R1) is sound: the corrected master table and floor table check out (d4 vs TN4 = 1/4, d12 floor 6 = 93/12 = 7.75), the floor cap always sits under the surge cap so "a floored value never surges" holds. The quest loop and boss have eight blockers the builder cannot code around: (1) Scholar means three different things in spec 5, R4.11 and R12; (2) the R12 vocabulary cannot express Ironbound, the 3 point Head affix, the 2 point half die floor, or Reaver's MIGHT restriction, and R12 says anything not expressible is not written; (3) no rule for a character who dies mid stage after being assigned (contagion from slot 1), including a declared Push; (4) BALANCE.STRIKE_TARGET and BALANCE.RESPITE have no value because proto/ is empty, and one candidate (`all`) contradicts the spec's own boss math while `spread` nullifies Vanguard and Unkillable; (5) R5.6 and R5.7 disagree on when Push/twice are chosen; (6) R5.6 contradicts itself on two living characters at Gate + Relay; (7) R3.3 skips a Chain's second check after a failure but says nothing about a Relay; (8) the Depth IV sealed stage loop is unwritable as stated and, read naively, is an infinite healing exploit. Ten majors follow (boss round vs stage for per stage counters, Ambush crossing into a boss, retarget ties, perDeadAlly scope, "last check" definition, Toll fee timing, the incoherent Blindfold Ward partial, stepStat on a d12, Vault/Open consequence stat). Cases the prompt asked about that RULES already decides consistently and need no ruling: Zealot plus Push (R4.12 reads Strain after Push), Hearthborn at Relay plus Relay (no bench, so no ally clear), WITS failure at stage 5 (R5.8), Cutpurse's auto twice (R1.8). Each finding proposes the smallest ruling.


### BLOCKERS (8)

**Scholar is three different Callings in three places**  
*Where:* spec 5 Callings table; RULES R4.11; RULES R12 `cond when: afterFailByOther`  
*The problem:* Spec: "After any failed check this stage, the next ally assigned gains +2" (the Scholar buffs someone else). R4.11: "the next check resolved by a DIFFERENT character than the one who failed gets +2" (anyone but the failer, a party aura that may include the Scholar). R12 puts the effect on the Scholar's own sheet and the resolver is only queried for the acting character ("nothing else in the engine knows a Calling from a Trait"), so `cond afterFailByOther` can only ever add +2 to the SCHOLAR's own check after someone else fails. Two failures in a row: under R4.11 A fails, B rolls +2 and fails, then C gets +2 from either charge; under R12 only the Scholar's next check gets +2 and B's own failure never arms B. The builder must pick one and the three readings have different assignment value.  
*The ruling taken:* Take the R12 reading and rewrite R4.11 as a CORRECTION: "Scholar: +2 on this character's next check after any OTHER deployed character's failed check this stage. A boolean charge, not a counter: set by any other character's failure, cleared when the Scholar rolls or at stage end; a failure by the Scholar does not arm it." This keeps pillar 2 (you put the Scholar in slot 2 behind the risky slot) and needs no aura plumbing.

**The R12 vocabulary cannot express Ironbound, two Head affixes, or Reaver**  
*Where:* RULES R12; R4.4 Ironbound; R4.16 Reaver; spec 11.2 Head rows  
*The problem:* R12 has no `floorPlus`: Ironbound ("Every floor from gear reads 1 higher") and the 3 point Head affix "Floors count as +1" have no key. `floor {k, stat, v}` takes a number, so the 2 point "Floor at half-die on one stat" (which must follow the worn die, and stepStat can change that die) has no key either. `surgeAspect {k, v}` has no stat field, so R4.16 "A MIGHT check that surged, against an Aspect, deals +2" can only be written as any stat, which is the Weapon affix, not Reaver. R12 says "A trait, unique or Calling that cannot be written in this table is not written" and `sim.js --data` refuses unknown keys, so an Origin and a Calling from the spec's tables (which R4 calls "the law") cannot be compiled.  
*The ruling taken:* Add `floorPlus {k, v, gearOnly:true|false}` [roll]: added to matching floors before the R1.3 cap (Ironbound gearOnly:true, the Head affix gearOnly:false so it lifts Vanguard's and Steady's floors too); several floorPlus add. Allow `floor.v = "half"` meaning the effective die / 2 at roll time. Add `stat?` to `surgeAspect` (Reaver = {k:'surgeAspect', v:2, stat:'might'}; the Weapon affix omits stat). Both Reaver and surgeAspect fire only on a PASSED check (spec: "A failed check deals nothing").

**A character who dies mid stage after assignment has no rule (and neither does their Push)**  
*Where:* RULES R5.6, R5.7, R3.1 contagion, R1.6  
*The problem:* Assignment fills both slots, then slot 1 resolves. A NERVE failure in slot 1 gives every other living party member 1 Strain (R3.1), which can kill the slot 2 holder, a Relay partner, or the bench (anyone at Toughness minus 1, common at Depth III). R5.6 only handles "With one living" at assignment time; nothing says what slot 2 does when its holder is dead at resolution, whether a Relay with one dead partner is short, or what a dead character's declared Push does (Push Strain is paid at roll per R4.12, so nothing was paid yet). Same gap for a Push validated as non lethal at toggle time that became lethal by roll time because contagion landed in between.  
*The ruling taken:* Ruling: a check whose holder is dead when its turn comes is FORFEIT exactly as R5.6's one living case (no roll, no reward, no Strain, no consequence); a Relay with a forfeited check pays nothing and its other check still rolls only if it comes first; a dead bench clears nothing (R3.5) and Hearthborn's ally clear needs a living bench. Push and twice toggles on the dead are void. Every Push is re-validated at roll: if it would now reach Toughness it is dropped and the RESULT card says "Push refused"; a dropped Push does not spend Saltblood's free one.

**STRIKE_TARGET and RESPITE have no value tonight, and one candidate contradicts the spec's boss math**  
*Where:* RULES R7.4 ("The prototype sim chooses the default; the handoff states it"), R5.7 ("BALANCE.RESPITE, default set by the sim"); plans/marrowdeep/proto/ and data/ are empty; spec 7.5, 8.6  
*The problem:* Both numbers are deferred to a sim and PROTO-REPORT.md that do not exist yet. Of the three target laws: `all` wipes the party whenever two Aspects survive round 1 (2 unbroken x 2 Strain = 4 = base Toughness, before Armor), while spec 7.5 says "A balanced roster breaks all three in 2–3 rounds and walks out at half health"; so `all` is not a candidate. `spread` lands "each point" as its own instance, so Vanguard's "1 less Strain from each Strike, minimum 0" and the Chest affix zero every point (a Vanguard is immune to the boss) and Unkillable stops one point and dies to the next. Only `attackers` survives the spec's own examples. RESPITE has the opposite tension: spec 8.6 computes death from accumulated failure Strain ("~4 failures ... ~1.33 each at Toughness 4") which a per stage clear erases, yet spec 9 says Depth III is where "Strain does not clear between stages", so RESPITE 0 at Depth I would make Depth III's headline change nothing. At Depth IV with `attackers`, the unfaceable fourth Aspect strikes everyone for 3 on top of the faced one: 6 per character per round; the sim must look at IV before the handoff.  
*The ruling taken:* Build default tonight: BALANCE.STRIKE_TARGET = 'attackers' (strike reduction and Unkillable apply per Aspect strike instance), BALANCE.RESPITE = 1; strike `all` from the candidate list; the sim may move RESPITE and must report Depth IV round 1 lethality. State both in the handoff even if the sim has not run.

**When are Push and roll twice chosen: before RESOLVE or at each check**  
*Where:* RULES R5.6 vs R5.7 vs R11.6  
*The problem:* R5.6: "Per check, before RESOLVE: Push toggles, roll twice toggles, the Vault and Open stat pick." R5.7: "each check: options -> roll -> RESULT card". R11.6 draws ONE Push chip on the character card, but a Chain gives one character two checks and Push is "once per check" (R1.6), and whether to Push a Chain's second check depends on seeing the first pass. The two rules describe two different UIs.  
*The ruling taken:* Ruling: the Vault/Open stat pick is at assignment (R5.5). Push and twice are chosen per check at its roll: RESOLVE starts the sequence, and each check shows a pre roll strip (character, stat die, TN, PUSH and TWICE chips, ROLL). A Chain's second check gets its own strip. Delete the toggles clause from R5.6; R11.6's chips move to that strip.

**Two living characters at Gate + Relay is contradictory; one living at a Relay is unstated**  
*Where:* RULES R5.6  
*The problem:* "With two living characters: one slot each, no bench; a Relay with two living takes both." Gate + Relay is three checks needing three bodies: "one slot each" leaves the Relay one short, "takes both" leaves the Gate empty, and "Every slot must be filled". With one living, "one slot, the other is FORFEIT" but a Relay needs "two DIFFERENT characters", so a lone survivor cannot hold a Relay at all; Relay + Relay at stage 5 (9%) with one living is undefined.  
*The ruling taken:* Ruling, one sentence that also covers the existing Relay + Relay exception: "A character may hold a second check only when the stage's checks outnumber the living deployed characters, and never both checks of one Relay." So two living at Gate + Relay: both in the Relay, one of them also on the Gate, no bench. One living: holds exactly one slot (a Chain is one slot), every Relay is forfeit, so Relay + Relay is a stage with nothing to do.

**A Relay's second check after a failed first check: rolled or skipped**  
*Where:* RULES R3.3 ("A Chain's second check only happens if the first passed"), R5.5 Relay, R5.9  
*The problem:* R3.3 covers only the Chain. A Relay "both must pass" with the reward paid once; if check 1 fails, rolling check 2 risks a second Strain instance (plus contagion, Ambush, Blindness) for a slot that can no longer pay. Bloodhound's "last check", Herald's "previous check", and Ambush's "same slot" all depend on whether that roll happens. The builder cannot write the Relay resolver.  
*The ruling taken:* Ruling: same as the Chain. A Relay's second check is skipped after a first failure; one failure, one instance; the second character's Push was not paid and their Quickstudy stat stays unused.

**The Depth IV sealed stage loop is unwritable as stated and reads as an infinite healing exploit**  
*Where:* RULES R9.1 Depth IV; R5.6 one living; R5.7 stage end; R5.8  
*The problem:* "a failed Vault costs its Strain and the stage is assigned again (new bench allowed), until it passes or the party is dead." Unstated: whether slot 1 re-resolves (paying its reward or Strain again), whether stage end runs between attempts, and what a lone survivor does when the other slot is forfeit but the Vault "must be passed to leave". If stage end runs per attempt: at Depth IV RESPITE is 0 and the bench is the only recovery, so a Warden benches for 3 (Feet 4, Armor refilled) while someone pays strainOnFail 1 per failed TN 7 Vault: net +2 Strain healed per tap, forever. Also the Vault is "their second slot" but R5.4 rolls slot order, so it could resolve first and then slot 2 would be re-run each attempt. Under Hollow Air a d4 with no flat has max 4 + Push 2 = 6 < 7, an unreachable Vault, which is a slow forced wipe.  
*The ruling taken:* Ruling: the sealed Vault is always resolved last. Only the Vault repeats; slot 1 resolves once. Between attempts nothing happens except the Vault's Strain instance and a re-choice of holder and stat (that is all "new bench allowed" means): no bench clear, no Hearthborn, no Armor refill, no rewards, no drops, no replacement. Stage end runs once when the Vault passes or the party dies. A lone survivor holds the Vault every attempt. Ambush from slot 1 is consumed by the first attempt; a GRACE failed Vault attempt puts Ambush on the next attempt. The UI greys a character whose best possible total is under 7 ("cannot reach"); if nobody can reach, the stage still offers the Vault, it is their funeral (R9.2's cruelty line).


### MAJORS (8)

**At the boss, is a round a stage for every once per stage effect**  
*Where:* RULES R4.10 Cutpurse, R4.14 Gambler, R4.7 Saltblood, R4.11 Scholar, R4.15 Herald, R1.8, R3.1 Thin Ice, R4.17 Bloodhound, R12 `[stage]` moment  
*The problem:* R4.17 already says Bloodhound at the boss is "the last action of a round" (per round), but Cutpurse's first GRACE check, Gambler's and the Charm's rerolls, the Charm twice, Saltblood's free Push, Thin Ice's first failure, the Scholar charge and Herald's "previous check resolved in this stage" are all "per stage" with the boss also called a stage (Ninth Hour "+3 at the boss stage"). One boss fight of 3 to 5 rounds versus per round is a big swing (a Gambler reroll per round), and Herald across a round boundary is undefined.  
*The ruling taken:* Ruling: at the boss every round is a stage for every `[stage]` counter and every firstOfStage/lastOfStage/sameStatAsPrev/afterFailByOther condition (reset at round start), matching Bloodhound. Flag the Gambler per round consequence for the sim; if it is too strong, the fix is one line (`rerollStage` resets per fight, not per round), not a different model.

**Ambush crossing from the last slot before a boss**  
*Where:* RULES R5.8 GRACE clause vs the "At the boss: none of these apply" clause; R9.1 Depth II and III stage 3  
*The problem:* "if the GRACE failure was in the stage's last slot, Ambush lands on the next stage's first slot" but the next stage after Depth I stage 5, Depth II and III stage 3 and stage 7 is a boss, where "a failed Aspect check simply deals no damage" and there is no failure Strain for the +1 to sit on. WITS has an explicit boss rule (round 1 TNs hidden); GRACE has none. Also unstated: Ambush is a set not a counter (two GRACE failures cannot make +2), it is consumed on pass or fail, and it shows on the card at assignment (R5.3 tags).  
*The ruling taken:* Ruling: Ambush never crosses into a boss (dropped with the stage). Ambush is a one shot tag on the slot: shown at assignment, applies +1 to every failure in that slot (Untethered excepted), consumed when the slot resolves whatever the result, never stacks.

**Retarget at the boss: ties, carried toggles, and who counts as an attacker**  
*Where:* RULES R7.2, R7.4 `attackers`, R5.6 options  
*The problem:* "rolls instead against the unbroken Aspect with the fewest hit points left": two Aspects at equal hit points has no tie break. The retargeted character declared Push and twice against the original stat and TN and cannot re-toggle mid resolution. Under `attackers` ("every character assigned to it this round") a retargeted character was assigned to the broken Aspect, so the rule as written has them struck by nobody. A tie on the retarget can also land a d4 stat, the same unseeable punishment the DECIDED line was avoiding.  
*The ruling taken:* Ruling: tie goes to the Aspect whose stat the character rolls the bigger die on, then card order. Toggles persist and are paid against the new stat and TN. For Strikes a character is an attacker of the Aspect they actually rolled against. If every Aspect is broken before their turn the character does not roll and no Push is paid.

**`perDeadAlly` and Grim scope is unbounded as written**  
*Where:* RULES R12 `cond when: perDeadAlly`, `grim`; spec 11.2 "+1 per dead ally"; R1.7  
*The problem:* Conditionals are outside the +3 cap (R1.7). "per dead ally" does not say dead this quest or dead ever; the Hall wall grows without limit, so a 1 point Token would read +20 at hour 50 and collapse TN 7. Grim says "after an ally dies this quest" but a mid quest replacement (R5.10) joined after the death.  
*The ruling taken:* Ruling: perDeadAlly counts deployed characters who died THIS quest (0 to 2); Grim and perDeadAlly read the quest's death list regardless of when the wearer joined.

**"Last check of a stage" cannot be known in advance when checks can be skipped**  
*Where:* RULES R4.17 Bloodhound ("the sim knows which is last"), R12 `lastOfStage`, R3.3  
*The problem:* A Chain's second check exists only if the first passes (R3.3), Relay likewise (finding above), forfeits remove checks. So at roll time "is this the last check" is not a property of the stage layout. If slot 2 is a Chain, its first check might be last (on a failure) or not; Bloodhound has to decide before the dice land.  
*The ruling taken:* Ruling: a check is last when no check can follow it whatever its result: a Chain's or Relay's first check is never last; a second check in the final slot is; a Gate/Vault/Toll/Open in the final slot is; at the boss, the last character in party order who still has an unbroken Aspect to roll against. firstOfStage is the first check that actually rolls.

**Toll fee timing and lethality**  
*Where:* RULES R3.1 ("paid on assignment, refunded by nothing"), R5.5 Toll, R1.6, R5.6 "Every slot must be filled"  
*The problem:* If the fee is literally paid when a character is dropped on the Toll card, dragging someone off it before RESOLVE burns 1 Strain per try. And the fee is the only self paid instance with no lethality rule: a Push that would reach Toughness "is refused by the UI", but a Toll fee at Toughness minus 1 either kills silently or, if refused, leaves a slot that "must be filled" unfillable when everyone is at Toughness minus 1.  
*The ruling taken:* Ruling: the fee is paid at RESOLVE when assignment locks, skips Armor (R3.4), can trigger Unkillable. A character whose fee would reach Toughness cannot be assigned to the Toll (chip: "Cannot pay"); if no living character can pay, the Toll is FORFEIT as in R5.6.

**The Blindfold Ward partial is incoherent as written**  
*Where:* RULES R9.3 Blindfold partial  
*The problem:* "Blindfold, TNs are shown for the wearer's own checks after assignment... no: before assignment on the wearer's own card only." TNs live on the challenge cards, and before assignment the wearer has no check, so there is nothing to show on the wearer's card. The sentence is also an unfinished edit ("... no:"), as is R6.2's "with benchOnce twice? no:", and a builder pasting RULES into a spec will trip on both.  
*The ruling taken:* Ruling: with the Blindfold partial, assigning the wearer to a slot reveals that slot's TN on its card before RESOLVE; the player may reassign (one TN per stage, earned by placing the wearer). Immunity: every TN shown for the party, as Lanternborn (R4.6). Clean the two "no:" fragments to their final rule only.

**stepStat on a d12 is written as a generation rule but the wearer is unknown at generation**  
*Where:* RULES R6.3; R12 `stepStat [equip]`; R6.7 drop screen; R6.6 MOVE TO  
*The problem:* "A 'step one stat up one rung' on a d12 stat is rerolled to another stat; if all four are d12 it becomes +1 Toughness lines." A drop is rolled before it is offered to three characters and later moves between roster characters, so "a d12 stat" has no referent at roll time; retargeting at equip would mutate the item's affix and name per wearer.  
*The ruling taken:* Ruling: generation picks the stat uniformly and never rerolls. At equip a step on a d12 is greyed and does nothing (R1.7's fourth flat point precedent) and the drop screen's point delta shows 0 for it. The effective die after stepStat feeds the floor cap, the surge threshold, Deepdrawn's highest stat and Quickstudy, all read at roll time.


### MINORS (8)

**Vault and Open failure consequences follow the chosen stat, which the spec's Open line hides**  
*Where:* RULES R5.5, R5.8; spec 7.2 ("Open is the safe parking spot for a hurt character")  
*The problem:* R5.8 keys consequences to the stat, R5.5 lets the player choose the Vault's and Open's stat, and nothing says whether a NERVE Open at TN 3 that fails triggers contagion. The spec calls Open safe; it is safe only if the player picks MIGHT (self contained). A d4 NERVE Open fails 50 percent and hurts everyone.  
*The ruling taken:* Ruling: consequences follow the stat actually rolled, chosen or named; the pre roll strip shows the consequence glyph beside the chosen stat so the choice is informed. No exception for Open.

**Thin Ice composition with strainOnFail 2 and Ambush; the Ward partial's trigger**  
*Where:* RULES R3.1 Thin Ice; R5.4 (stage 5 strainOnFail 2); R9.3 Thin Ice partial  
*The problem:* "the first failure each stage by anyone costs 2 instead of 1": at stage 5 (base 2) that is either 2 (no effect) or 3 (+1). With Ambush: 2 + 1 or max(2, 1 + 1). The partial ("the wearer's first failure each stage costs the normal amount, others' do not change") does not say whether the wearer's failure uses up the stage's "first failure".  
*The ruling taken:* Ruling: instance = max(strainOnFail, 2 if Thin Ice and this is the stage's first failure) + 1 if Ambush. The stage's first failure by anyone, the wearer included, consumes the trigger; Thin Ice immunity works the same way with the immune wearer's cost at base.

**Floors from two items and floorPlus stacking**  
*Where:* RULES R1.3, R4.4, R6.2 repeat rule, spec 11.2 Head rows  
*The problem:* "the highest holds, they do not add" is clear, but R6.2 allows floor_flat3 WITS and floor_half WITS on one item (different keys, same stat), a wasted point by construction. Ironbound (+1 to gear floors) plus the Head "floors count as +1" affix: add to +2 or highest wins?  
*The ruling taken:* Ruling: for the R6.2 repeat rule every floor key counts as one key per stat (one floor line per stat per item). floorPlus sources add (+2) before the R1.3 cap; Ironbound lifts gear floors only, the Head affix lifts every floor the wearer has including Vanguard's and Steady's.

**twiceStage plus rerollStage on one check; two twice sources on one check; where reroll1s sits**  
*Where:* RULES R1.8, R1.1 order, R4.10, R4.14, R12  
*The problem:* A Cutpurse's auto twice (two chains) then a Gambler or Charm reroll ("the whole chain is redrawn once"): one chain or both? A Cutpurse also wearing the Charm twice: could the first GRACE check roll three chains? R1.1's step list never places "reroll natural 1s", and whether a rerolled 1 that lands the top face surges is unstated.  
*The ruling taken:* Ruling: a reroll re-runs the whole roll with the same options (twice stays twice, a paid Push stays paid); the new result stands. A check rolls at most two chains: when an auto twice fires, the manual twice chip is unavailable and not consumed. reroll1s is step 0 of R1.1: the second draw is the natural and goes through surge test, floor, and surges normally.

**Pressgang edges and benchOnce as a bench**  
*Where:* RULES R3.1 Pressgang, R5.6 Relay + Relay, R7.4, R12 `benchOnce`, R11.6  
*The problem:* Pressgang's "third character takes 1 at stage end instead of benching" has no third character at Relay + Relay, with two living, or at the boss. benchOnce ("act and bench both") does not say whether it triggers Warden's 3, Feet +1, Hearthborn's ally clear and the Armor refill, whether it works under Pressgang or at the boss, and R11.6 lists no chip for it.  
*The ruling taken:* Ruling: Pressgang Strain lands only when a character would otherwise have benched (none at Relay + Relay, two living, or the boss). benchOnce is a bench for every purpose (Warden, Feet, Hearthborn, Armor refill), forbidden under Pressgang and at the boss, chosen by a BENCH TOO chip at assignment.

**Party order, and which reroll source a REROLL button spends**  
*Where:* RULES R7.2 ("Resolve in party order"), R1.8, R11.4 DEPLOY  
*The problem:* Herald, Scholar, Bloodhound and retargeting at the boss all depend on party order, which is never defined. When two reroll sources are unspent (a Gambler and an ally's Charm) one REROLL button spends an unnamed one.  
*The ruling taken:* Ruling: party order is the order the player marked DEPLOY, shown left to right and used everywhere "party order" appears. REROLL spends the first unspent source in party order; each source may still fire once, so two sources can reroll the same check twice.

**Unkillable versus a refused Push, and death test timing at Strikes**  
*Where:* RULES R1.6, R2.4, R3.2, R7.4  
*The problem:* "A Push that kills is refused by the UI": with Unkillable unused the Push would not kill, so is it allowed? R7.4 says "Then the death test" after all Strikes while R3.2 tests death per instance; with a character taking two Strike instances in one round the order decides whether the second lands on a corpse.  
*The ruling taken:* Ruling: a Push never brings Strain to Toughness, refused regardless of Unkillable. Death is tested per instance (R3.2); R7.4's line means interment happens after the Strike animation. Later instances in the same round skip the dead.

**Hollow Air Ward partial: does the one surge use surgeMinus and fire Reaver**  
*Where:* RULES R9.3 Hollow Air partial, R1.2, R4.16, R12 surgeAspect  
*The problem:* "the wearer surges once with no chain" does not say whether a lowered threshold (Fenwise, Deepdrawn, Hands) applies to that one surge or whether it counts as "a check that surged" for Reaver and the Weapon affix.  
*The ruling taken:* Ruling: the one surge uses the normal threshold (R1.2 with all surgeMinus and the cap), adds exactly one die, and counts as a surge for every `surgeAspect` and Reaver.


---

## Lens: ECONOMY AND PROGRESSION

**What it reported:** Economy and progression audit of MARROWDEEP_DESIGN_SPEC.md v1.0 and RULES.md. Three blockers: nothing says how many characters a new account gets or at what price (Recruit is 25 Renown, the account starts at 0, the seam gate rolls ONE character and deploys it solo); a first quest wipe leaves 0 characters and ~12 Renown against a 25 Renown Recruit with Marrow unable to buy a body, a soft lock at the spec's own ~8 percent wipe rate; Depth IV rolls Commons at 10 percent with no point budget. Five majors: Recruit then one quest then Retire converts Renown to Marrow at ~9 Renown per Marrow (R2.6's DECIDED fix only added a Renown positive delay, tripling Marrow income and deleting the veteran arc); Depth multiplied death Marrow plus mid quest Recruit makes a rookie suicide farm at Depth IV and V; Depth V "Relic rarity only" contradicts the spec's own 11.3 and 11.4 V rows and RULES keeps both (every drop a unique, 20 uniques gone in ~4 quests, +1 tier a no op); roster full and dead in roster undecided (the wrong pick fills the roster with the dead and refuses Recruit forever); playing a Depth I quest re rolls every Depth's offer, a cheap re roll R9.4 says must not exist. Seven minors. Verified consistent and not reported: Depth I Renown ~36 vs ~35, wipe ~10 vs ~12, Commission salvage EV 3.9 to 8 vs cost 40, Depth multipliers, unlock counts, Reforge on uniques, a Ward with no shelf slot.


### BLOCKERS (3)

**How many characters does BEGIN roll, and at what price**  
*Where:* spec 3.2, 7.1 ("Roster: 3 characters deployed"), 14 ACCOUNT (renown 0, rosterSlots 3); RULES R2.1, R8.1 (Recruit 25), R11.3, R11.4 ("DEPLOY marks three"); HANDOFF P1.10 seam gate ("BEGIN; ROLL; the first Calling; KEEP; DEPLOY; GO")  
*The problem:* The account starts with renown 0. The only way to make a character is Recruit at 25 Renown. Neither the spec nor RULES says the first characters are free or how many there are. The handoff's seam gate rolls exactly ONE character then DEPLOY and GO, which means the first quest is played solo: at the boss three Aspects strike 2 each per round onto one Toughness 4 body, dead in round 1 or 2 under every STRIKE_TARGET law, and every number in spec 8.6 (death 12 to 15 percent per character, wipe 8 percent) assumes a party of three. The builder cannot write BEGIN, the creation price, or the DEPLOY gate without this decision.  
*The ruling taken:* R2.1 addendum: a new account is created with `account.freeRolls = 3`; the creation screen's price is 0 while freeRolls > 0 (decrement on KEEP; Redeal still costs 10); BEGIN runs creation three times before the Hall; the seam gate rolls three. R11.4: DEPLOY marks up to three; GO with fewer is allowed only when the roster cannot fill three (see the stray ruling). `rosterSlots` stays 3.

**A first quest wipe soft locks the account: 0 characters, ~12 Renown, Recruit costs 25, Marrow buys no body**  
*Where:* spec 8.2 ("~12 on an early wipe"), 8.6 ("Full wipe ~8%"), 8.3 (Marrow purchases: none is a character); RULES R8.1, R8.2, R6.8, R11.4  
*The problem:* After a wipe the roster is empty (three dead, each 1 Marrow), Renown is the stages passed plus wipe salvage, about 10 to 12 by the spec's own number, and Recruit costs 25. Renown comes only from quests; quests need a deployed character; the Hall offers nothing at 0 deployable. Marrow (3 on hand) buys floors, slots, Origins, none of them a body. The same lock at 2 dead and 1 alive with 12 Renown if GO needs three (undecided, R11.4 says marks three). Even a veteran roster can hit it: three characters at 4 Scars (Toughness 0, "Needs Toughness") on an account that spent its Renown on Commissions can Retire for Marrow but cannot buy anyone. Probability on the very first quest is the spec's 8 percent; over the first three quests roughly 22 percent of new players see it.  
*The ruling taken:* R8.1 addendum, THE STRAY: whenever the roster has no deployable character (alive, effective Toughness > 0) and account.renown < 25, the Recruit button reads TAKE IN A STRAY and costs 0, one at a time (the next costs 25 again). R11.4: GO is allowed with one or two characters only when the roster cannot fill three; the offer card says short handed. With the proven death ruling below the stray's own death pays 0 Marrow, so the stray cannot be farmed; a stray that survives a quest is proven like anyone. Gear salvage on Retire (R2.6) is not a substitute: a wiped roster has already salvaged everything (R6.8).

**Depth IV drops Commons at 10 percent with no point budget; Ashwalker's Common at IV and V is undefined**  
*Where:* spec 11.3 (Depth IV Common "—", Depth V Common "—") vs 11.4 (Depth IV Common 10%, Depth V 0%); RULES R6.1 ("the spec's 11.3 and 11.4 tables"), R4.2 (Ashwalker: "a Common relic"), R6.2 (fill until points equal the budget)  
*The problem:* 11.4 says one drop in ten at Depth IV is Common; 11.3 gives a Common at Depth IV no budget. R6.2's fill loop reads `budget[IV][common]` and gets nothing; the sim's `--depths` gate generates 200 Depth IV quests and will hit it in the first one. R4.2 rolls a Common every quest for an Ashwalker at Depth IV and V, where the table has no Common at all. The 11.4 V row (Common 0) and both 11.3 dashes agree with each other; the IV 10 percent is the stray cell.  
*The ruling taken:* R6.1 CORRECTION: the 11.4 Depth IV row becomes 0 / 55 / 34 / 11 (the 10 folded into Uncommon, matching its 11.3 dash and the V row). R4.2: Ashwalker's relic is Common at Depth I to III and Uncommon at IV and V. The sim asserts no Common is ever generated at IV or V.


### MAJORS (5)

**Recruit, one quest, Retire converts Renown to Marrow at ~9 Renown per Marrow; R2.6's DECIDED fix only delayed it by one Renown positive quest**  
*Where:* spec 6.3 (Retire 2 + 1 per Trait, Death 1), 8.1 ("A player can grind Renown forever and stay flat. Marrow is gated behind emotional cost"), 8.2 (Recruit 25, income ~35), 8.3 (Marrow ~1.5 per quest); RULES R2.6 (DECIDED: "closes the Recruit for 25 Renown then Retire for 2 Marrow conversion, which would have made Marrow purchasable"), R8.5 (retirement flat), R2.5 (one Trait per quest survived, so traits.length == questsSurvived)  
*The problem:* Retire after N quests pays 2 + N. Marrow per character quest: N=1 gives 3.0, N=2 gives 2.0, N=3 gives 1.67, N=4 (the spec's veteran, 6 Marrow) gives 1.5. The spec's income target is 1.5 per quest for a party of THREE, 0.5 per character quest. Retiring every character after its first quest is six times the design rate and twice the veteran's, and it is also safer (Toughness 3 or 4, never 1). The only brake is Recruit at 25: at Depth I (~35 Renown a quest) the player affords 1.4 fresh bodies a quest, retires 1.4 for 0.85 x 3 + 0.15 x 1 = 2.7 each, about 3.8 Marrow plus honest deaths, roughly 4 a quest against 1.5. At Depth III (79 Renown) all three cycle every quest: about 8 against the expected 2.5. The conversion R2.6 says it closed ran at 12.5 Renown per Marrow with no risk; the surviving one runs at 25 / 2.7 = 9.3 Renown per Marrow AND the quest in between pays 35, so the fix made it cheaper. Under this play no character ever carries more than one Scar, the retire or run decision (spec 6.3), Excise, Warden's late value and the 4 to 7 quest career never happen, and roster slots (a Marrow sink) are worthless because three bodies always suffice. This is a payout, Stephen's, but the builder needs a default tonight.  
*The ruling taken:* BALANCE.RETIRE_VESTING = 3. Retire pays 2 + traits.length only when questsSurvived >= RETIRE_VESTING; under it Retire pays traits.length (1 or 2), still writes a Legacy and a wall line. Rates become 1.0, 1.0, 1.67, 1.5, 1.29 per character quest for N = 1, 2, 3, 4, 7: never increasing at the small end, and the spec's veteran number is untouched. The vest lands exactly where Toughness hits 1 (three Scars), which is where the spec wants the decision to bite.

**Death Marrow is Depth multiplied for a character who has never survived a quest, and mid quest Recruit refills the sacrifice: a rookie suicide farm at Depth IV and V**  
*Where:* RULES R8.5 (death pays round(1 x DEPTH_MARROW_MULT) = 1, 1, 2, 2, 3), R5.10 (Depth I to IV: at any stage end pay 25 Renown to Recruit straight into the party), R1.6 (a Push that kills is refused, one Push per check), R3.1 (Toll fee 1); spec 8.1's pillar  
*The problem:* A fresh Recruit has no gear and no history. At stage 1 assign him to a Toll (fee 1) with a Push (1) and a fail (1); stage 2 the same: dead on demand by stage 2 or 3, in about two minutes. Depth V: three Recruits (75 Renown) deployed and wiped at stage 2 pay 3 x 3 = 9 Marrow; the stages passed give back 10 to 25 Renown (x5.1), so net about 55 Renown per 9 Marrow, 6 Renown per Marrow. An honest Depth V quest pays about 200 Renown and 3 to 4 Marrow in nine minutes, so each real quest funds three wipes worth 27 Marrow. Depth IV: R5.10 lets the player Recruit at EVERY stage end once someone has died; seven stage ends x 2 Marrow = 14 Marrow a quest for 175 Renown against an income of about 115, so 4 sacrifices a quest, roughly 8 Marrow plus honest income against the expected 3.3. Any free character (the stray above, or any future free roll) turns this into an infinite loop, so it must be closed at the payout, not the price.  
*The ruling taken:* R8.5 CORRECTION: a death pays Marrow only when the character has questsSurvived >= 1 (proven); an unproven death pays 0 Marrow but still makes the Legacy and the wall line (spec pillar 5: death stays productive through the Calling card). A proven character's death pays the Depth multiplied rate as written. This costs the honest player at most the 0.4 Marrow a quest of first quest deaths early on and nothing later; combined with RETIRE_VESTING every Marrow in the game routes through at least one survived quest, which is what spec 8.1 says the gate is.

**Depth V "Relic rarity only" contradicts the spec's own 11.3 and 11.4 V rows, and RULES R6.1 keeps both**  
*Where:* spec 9 (Depth V: "Relics drop at Relic rarity only") vs 11.3 (V: Uncommon 4, Rare 7, Relic 10) and 11.4 (V: 0 / 45 / 40 / 15); RULES R6.1 ("the spec's 11.3 and 11.4 tables. Depth V drops are Relic rarity only. '+1 rarity tier' ... Relic stays Relic"), R6.4 (one unique per Relic rarity item, 20 uniques), R6.10 (Commission at the highest Depth's weights); HANDOFF P3.5 asserts "Depth V drops are all Relic rarity"  
*The problem:* Two spec tables give Depth V three live rarities with 15 percent Relic; one prose line says Relic only. R6.1 states both, so the builder cannot tell whether Commission at V (R6.10, "weights") rolls the 45/40/15 row or three uniques. Under the Relic only reading: every Depth V drop is a 10 point item with a unique; R5.9's rolls at the eight stage shape give about 7.5 relic rolls a quest, roughly 4.5 drops, so 4.5 uniques a quest from a list of 20 filtered by slot (2.5 per slot), duplicates by the second quest and the list exhausted by the fourth; Vault and boss "+1 tier" is a no op at V; salvage is 12 per drop, about 54 Renown a quest on top of the x5.1 income; Commission is best of three uniques for 40 Renown, five a quest at V income. Whether a unique may repeat is undecided either way.  
*The ruling taken:* R6.1 CORRECTION: Depth V uses the 11.3 and 11.4 V rows (0 / 45 / 40 / 15; budgets 4 / 7 / 10) and +1 tier applies normally; the section 9 line is the error (two tables beat one prose line). R6.10: Commission rolls at the highest unlocked Depth's weights AND budgets. R6.4: a unique may repeat across items (it is a name and one effect, not a one of). HANDOFF P3.5's assertion becomes "no Depth V drop is Common". If Stephen wants Relic only at V it is one BALANCE row and the unique repeat rule already covers it.

**Roster full: Recruit against rosterSlots, and whether the dead occupy a slot, are undecided; the wrong pick refuses Recruit forever**  
*Where:* spec 14 (character status alive | dead | retired; rosterSlots 3), 8.3 (Roster slot: "Expand owned characters 3 to 8"); RULES R8.1 (Recruit), R8.2 (Roster slot), R5.10 (mid quest Recruit "straight into the party"), R2.4 (interred), R6.8 (a dead character's gear is offered at quest end, so its record must persist until then); HANDOFF save `roster: [characters]` beside `wall: [lines]` and P2.3 ("greys when the account cannot pay")  
*The problem:* Nothing says what Recruit does when living characters equal rosterSlots, and nothing says whether a dead or retired character still counts as owned. If `roster` keeps the dead (the spec's status field suggests records persist) and rosterSlots counts `roster.length`, three deaths fill a 3 slot roster and Recruit is refused with a full account of Renown: a soft lock the P2 test ("every Hall purchase pays and refuses") would pass. Mid quest Recruit at Depth I to IV has the same question with reserves: eight owned, five alive on the bench, one deployed dies; does the 25 Renown Recruit exceed the cap.  
*The ruling taken:* R8.1 addendum: the roster count is living characters only; Recruit (Hall and mid quest) is refused with the label Roster full when alive >= rosterSlots, and the mid quest sheet offers the reserve instead. A dead character leaves `roster` at quest end after R6.8 has offered its gear (its record lives on in `wall` and `legacies`); a retired or dismissed one leaves at once after its gear hits the drop screen. TEST asserts Recruit refuses at a full roster and accepts after a death.

**Any quest re rolls every Depth's offer, so a four minute Depth I run is the cheap re roll R9.4 says must not exist**  
*Where:* RULES R9.4 ("An offer stays until a quest at ANY Depth is played, then every offer re-rolls. No paid re-roll (DECIDED: a free or cheap re-roll deletes the Sigil decision)"), R9.2 (no exclusion table, "supposed to be that cruel"); spec 10 ("building a stash of situational gear is directly rewarded"), 11.1 (the Ward Shelf loop)  
*The problem:* A Depth V offer of Hollow Air with Shivering is dodged by playing the Depth I offer: four to six minutes, plus about 35 Renown, at a tier 10 account with d8 average dice near zero risk. The only cost is one Scar per survivor, and under the retire cycle those are rookies anyway. A paid re roll was refused because it deletes the Sigil decision; this one pays the player to make it. It also devalues Wards, whose whole job (spec 11.1) is surviving the Sigil you would otherwise dodge.  
*The ruling taken:* R9.4 CORRECTION: an offer re rolls only when a quest at ITS Depth ends (won or wiped); the other Depths' offers stay. The Depth I dodge then changes nothing, and a cruel Depth V offer sits until the party faces it, which is R9.2's own stance.


### MINORS (7)

**Reforge on a line with no other affix of the same points loops or no ops**  
*Where:* RULES R6.9 ("redrawn to another valid affix of the same points"), R6.2 (the 1 point Toughness filler on Hands, Feet, Weapon); spec 11.2 (Head has one 3 point affix, floors count as +1; Feet none at 1)  
*The problem:* Head's only 3 point affix is "floors count as +1"; a Reforge of that line has no "another" to draw. The 1 point Toughness filler on Hands, Feet or Weapon has no 1 point sibling on those slots. A draw loop either never terminates or returns the same line for 15 Renown.  
*The ruling taken:* R6.9 addendum: a stat retarget of the same key counts as another affix; when no different key or stat at that point value is valid for the slot, the line's REFORGE is greyed with the label Fixed. TEST asserts the grey on a Head floorsPlus line and on a filler line.

**The slot of a drop is never rolled**  
*Where:* RULES R6.2 (fill), R6.1 (rarity), R5.9 (relic rolls); only R4.2 (Ashwalker, "a random slot") mentions a slot roll  
*The problem:* Every drop needs a slot before affixes can be drawn; RULES rolls rarity and budget and never the slot. Uniform over eight makes 12.5 percent of Depth I drops Wards for one of six Sigils in a Depth with 0 or 1 Sigil, mostly 1 Renown salvage; the builder should know that is the intended rate rather than invent a weighting.  
*The ruling taken:* R6.2 addendum: the slot is rolled uniformly over the eight (BALANCE.SLOT_WEIGHTS, all 1) before rarity; Commission fixes it. One row to tune if Wards drown the drop screen.

**Ward Shelf edges: TO THE SHELF with no free slot, salvaging from the shelf, moving a worn Ward to the shelf**  
*Where:* RULES R6.7 ("a Ward may go TO THE SHELF if a shelf slot is free"), R8.1 (slot 30 then +15, six max), R11.5 (MOVE TO on a gear tile); HANDOFF P2.4 (swap in, the swapped out Ward returns to the shelf)  
*The problem:* Not a dead end (a Ward with no shelf slot still equips or salvages), but three small paths are unwritten: the account starts at wardShelfSlots 0 so the first Ward drop shows no shelf button, which should be a deliberate absence not a bug; a shelf Ward has no TAKE RENOWN; a worn Ward cannot reach a free shelf slot except through a swap.  
*The ruling taken:* R6.7 addendum: TO THE SHELF is hidden (not greyed) with no free slot; a shelf tile carries TAKE RENOWN at SALVAGE value; a Ward's MOVE TO sheet lists THE SHELF when a slot is free.

**questsCompleted at Depth II and III with two bosses, and the first boss's relic**  
*Where:* RULES R8.4 ("counts boss wins only"), R7.6 (first boss: half hit points, reward 8, "its Strikes normal"), R7.3 (win: "the boss's relic"), R5.9 ("the boss 1 at +1 tier"); spec 9 unlock counts (3, 10, 25, 50)  
*The problem:* Unlock counts are consistent between spec and RULES. But at Depth II a quest holds two bosses and "boss wins" could count the first, halving the unlock pace at II and III (25 wins for IV becomes ~13 quests). R7.6 gives the first boss Renown 8 and never says whether it drops a relic.  
*The ruling taken:* R8.4: questsCompleted increments once, on the quest's final boss (R7.3). R7.6: the first boss drops one relic at +1 tier like any boss (it is the +1 tier roll that makes a mid quest boss worth its Strikes).

**Legacy deal can overflow three cards with a consecrated Legacy and three Legacy slots**  
*Where:* RULES R8.7 ("the consecrated Legacy first if any; then up to account.legacySlots Legacies ...; then stock Callings ..., until three"), R8.2 (Legacy slot 2 to 3, cap 3)  
*The problem:* With legacySlots 3 and a consecrated Legacy the order reads 1 + 3 = 4 Legacy cards before the "until three" clause, which is written on the stock fill. A player who paid 6 Marrow to consecrate and 2 for the third slot should know what the 2 bought.  
*The ruling taken:* R8.7 wording: the consecrated Legacy occupies one of the legacySlots (Legacy cards on the deal <= legacySlots <= 3); stock fills the rest.

**A paid creation floor can show a die under the floor through Straycall**  
*Where:* RULES R2.1 ("Straycall shifts rungs AFTER the floor, so a Straycall NERVE can sit under the floor"), R8.2 ("that stat never rolls under d6 at creation", 3 Marrow, then d8 for 6)  
*The problem:* The Marrow purchase's text promises NERVE never rolls under d6; a Straycall deal then shows a d4 NERVE. A player who paid 3 or 9 Marrow for the floor notices on the creation screen.  
*The ruling taken:* R2.1: apply the account floor after the Origin shift (the floor is a guarantee the player paid for); or keep the order and word the purchase "rolls" not "is". The first is one line and never surprises.

**Marrow has a finite sink of about 98, so retire or run stops mattering after it is bought**  
*Where:* spec 8.3 (the five Marrow purchases), 6.3 ("the endgame tension ... emerges naturally around quest 15"); RULES R8.2 (floors 3 + 6 per stat = 36; roster slots 4 + 6 + 8 + 10 + 12 = 40; Origins 4 x 5 = 20; Legacy slot 2; Consecrate 6, one at a time)  
*The problem:* Every Marrow purchase but Consecrate is finite: 36 + 40 + 20 + 2 = 98 Marrow, then only 6 per consecration swap. At the honest 1.5 to 2 a quest the tree is bought by quest 50 to 65, about when Depth V unlocks; the retire or run decision the spec calls the endgame hook is then worth nothing and running every veteran to death is always right. Design, not a rule, and Stephen's; the builder does not need it tonight.  
*The ruling taken:* Record as a Director call: one repeatable Marrow sink after the tree (a second consecration slot at 6 per swap, or Excise payable in Marrow at Toughness 0). Nothing to build tonight.


---

## Lens: ECONOMY AND PROGRESSION (second pass)

**What it reported:** Economy and progression pass over spec sections 8 to 11 and RULES R2, R6, R8, R9. AUDIT.md already carries an economy lens, so I report only what it does not: ten findings, two blockers, both structural rather than edge cases. First blocker: the Sigil Ward slot has only 5 points of legal affixes (immunity 3 plus partial 2, both naming the same Sigil per R9.3) against budgets of 6, 8 and 10, and Feet (7 max) and Chest (6 max) have the same hole, so R6.2's fallback pours the gap into raw Toughness. R6.2's fallback also contradicts its own no-repeat law inside one sentence ("without repeating a key" then "'+1 Toughness' lines, 1 point each"), so the fill loop is unwritable, and read the generous way a Depth V character wearing Ward, Feet and Chest carries +13 Toughness over base 4, which deletes spec 6.2's Scar treadmill (career 4 to 7 quests), spec 6.3's retire-or-run endgame, and most of the Marrow supply. Second blocker: the drop screen is defined only over "the three deployed characters", yet R6.7's own last line, R6.10 (Commission, 40 Renown) and R2.6 (retired gear) all use it between quests when nobody is deployed, so the game's second most expensive purchase has no keep step. Three of the four majors share one root: prices are constants while Renown income multiplies 5.1x by structure and multiplier together (Depth I ~35 plus ~8 salvage, Depth V ~245 plus ~28), so by Depth V one run buys six Commissions and four excisions against spec 8.2's "not both", Renown cannot zero out as spec 8.1 requires, and the 25 Renown brake the existing farm rulings lean on erodes under them. Also major: R8.4 counts boss wins at any Depth, so 50 four-minute Depth I runs unlock Marrowdeep (fix: count only wins at the deepest unlocked Depth, which preserves 3/10/25/50 and the 50-quest total exactly); the 2 Marrow Legacy slot at legacySlots 3 permanently deletes stock Callings from the deal, narrowing an account's Calling pool forever to whichever Callings died; and the spec's pacing prose is about ten times off its own session lengths (Depth III at ~1 hour, not "hour 10"; the whole ladder spent in ~6 hours, not "hour 200"), which is the curve the content banks are being sized against. Minors: the Vault pays 3.27 EV against a Toll's 4.71 while being the game's only TN 7 and Depth IV's mandatory seal; Mend dies as a purchase the moment a Marrow roster slot is bought; the authored challenge banks are half to a quarter of spec 16's target and repeat inside the first hour; and spec 8.2's "a good run buys a Commission or a Scar excision" is false for Excise (1.71 runs) and self-contradicted six lines later. Income sanity check as asked, at R5.9's computed ~35 a Depth I run: Redeal 0.29 quests, Reforge 0.43, Mend 0.57, Recruit 0.71, first Ward slot 0.86, Commission 1.14, Excise 1.71, six-slot Ward Shelf 11.6. Asked-about items I checked and did NOT report because AUDIT.md's economy lens already rules them consistently: the Recruit-then-Retire Renown-to-Marrow loop (RETIRE_VESTING 3), the rookie suicide farm (proven-death rule), Recruit at a full roster, a Toughness 0 roster on a broke account and the first-quest wipe soft lock (THE STRAY plus three free rolls), Depth V "Relic rarity only" versus the +1 tier rule and the Commission's Depth weights, the Sigil offer reroll, Reforge with no sibling affix, and a Ward dropping with no shelf slot. Reforge on a unique is decided correctly by R6.9 ("never the unique") and needs nothing. I edited neither file.


### BLOCKERS (2)

**The Sigil Ward slot can legally spend only 5 of its 10 points, and R6.2's Toughness filler both contradicts its own no-repeat law and hands out +3 to +5 Toughness per item, deleting the Scar treadmill**  
*Where:* RULES R6.2, R9.3, R6.1; spec 11.2 (affix table), 11.3 (budgets), 6.2 ("Every quest survived also inflicts a Scar: permanent -1 max Toughness", "Expected career: 4-7 quests"), 6.3  
*The problem:* R6.2 says "Draw affixes valid for the slot, without repeating a key on one item" and then "the remainder becomes '+1 Toughness' lines, 1 point each, which every slot may carry." Lines, plural, of one key. The two clauses contradict each other and the builder cannot write the fill loop. Worse is where the remainder comes from. Spec 11.2 lists exactly two Sigil-Ward affixes: immunity 3 and partial relief 2. R9.3 says "A Ward names one Sigil at generation", so both name the same Sigil and the slot's entire legal spend is 5 points. Budgets (spec 11.3) run to 6 at Depth I Relic, 8 at III, 10 at V. So: Depth I Relic Ward = immunity 3 + partial 2 + 1 Toughness. Depth III Relic = 5 + 3 Toughness. Depth V Relic = 5 + 5 Toughness (and immunity plus partial to the same Sigil is a nonsense pair on the card). Feet has the same hole: benchPlus 2 + relayPlus 2 + benchOnce 3 = 7 max, none stat targeted, so a Depth V Relic Feet is +3 Toughness. Chest: armor 2 + toughness 1 + strikeLess 3 = 6 max, and the toughness key is already spent, so a Depth V Relic Chest is 6 + 4 more Toughness lines = +5 Toughness. One Depth V character wearing all three carries +13 Toughness over base 4. Spec 6.2's lifespan (Toughness 4, minus 1 a quest, career 4 to 7 quests) becomes 17-plus quests, spec 6.3's retire-or-run endgame never fires, and Marrow income (death and retirement only, spec 8.1) collapses toward zero. This is generated by a fallback clause, not by a design choice, and the existing AUDIT.md treats the filler only as a Reforge edge, never as a Toughness faucet.  
*The ruling taken:* Three lines. (1) R6.2 CORRECTION: the filler is ONE `toughness` line whose value is the remainder, capped at BALANCE.FILLER_MAX = 2; if the remainder still cannot be spent the affix draw restarts (the 50-draw cap already exists) and, failing that, the item is generated at the next lower rarity's budget for that Depth. (2) Give the three short slots enough legal affixes to spend their top budget: Sigil Ward gets "immunity to a second named Sigil, 3" with R9.3 amended to "a Ward names one Sigil per 3 points of budget", and immunity and partial may never name the same Sigil; Feet gets `benchAlly` at 2 (already in the R12 vocabulary, currently used by no affix); Chest's `toughness` line may repeat up to 3 times, which is legible because Chest is the Toughness slot. (3) sim.js --test asserts no generated item carries more than FILLER_MAX Toughness from filler and that every slot can spend the Depth V Relic budget.

**The drop screen is defined only over "the three deployed characters", but Commission (40 Renown) and a retired or dead character's gear both use it between quests when nobody is deployed**  
*Where:* RULES R6.7 ("its card, then the three deployed characters as small cards ... Between quests the same screen serves the Hall's Commission"), R6.10 ("keep one (the drop screen)"), R2.6 ("Gear on a retired or dismissed character goes to the drop screen (R6.7) for the roster"), R11.2, R11.5  
*The problem:* R6.7 defines exactly one target row, the three deployed characters, and then in its last sentence reuses the same screen for a purchase made in the Hall, where nothing is deployed. R2.6 sends retired gear to the same screen but says "for the roster", a different set of up to eight. So Commission, the game's second most expensive Renown purchase and step 8 of the spec's build order, has no defined keep step: the builder does not know which cards the player taps to keep the item, what happens to the replaced item (R6.7's auto-salvage assumes a wearer), or whether TO THE SHELF is available in the Hall. The same gap blocks R2.6's retirement flow, which is the front half of the Marrow economy.  
*The ruling taken:* R6.7 CORRECTION, one paragraph: the drop screen's target row is the DEPLOYED three during a quest and the LIVING ROSTER between quests (three cards visible, horizontal scroll, ordered by point delta in that slot, best first). TAKE RENOWN and, for a Ward, TO THE SHELF are available in both contexts. Pillar 6 ("never more than three cards to choose from") is about dealt choices, not about a roster the player already owns, so the scroll does not breach it.


### MAJORS (4)

**Every Hall price is flat while Renown income multiplies 5.1x across the Depths, so Renown stops being a decision after Depth II and the 25 Renown brake the farm rulings lean on erodes to nothing**  
*Where:* RULES R8.1 (Reforge 15, Commission 40, Recruit 25, Redeal 10, Mend 20, Excise 60, Ward slot 30 then +15), R6.7 (SALVAGE flat: 1/3/6/12), R5.9 ("Renown x BALANCE.DEPTH_RENOWN_MULT[depth] (1, 1.5, 2.25, 3.4, 5.1)"), R9.1 (Depth II+ is 8 stages and 2 bosses); spec 8.1 ("Flow currency. Expected to zero out constantly"), 8.2 ("A good run buys a Commission or a Scar excision, not both")  
*The problem:* Income scales twice over, by structure and by multiplier, while every price is a constant. Depth I by R5.9 with ~70 percent slots: 4.5+4.5+5.1+5.1+5.5+10.8 = ~35 Renown, plus ~3.5 drops at 2.28 salvage = ~43. Depth II (8 stages, two bosses) is ~48 base before the multiplier; x1.5 = 73, plus ~15 salvage = ~88. Depth V is ~48 x 5.1 = 245 plus ~28 salvage = ~273 Renown for a nine minute quest. Against flat prices that is: Commission 40 = 0.15 of a quest, Excise 60 = 0.22, Recruit 25 = 0.09, Mend 20 = 0.07, the whole six-slot Ward Shelf 405 = 1.5 quests. A Depth V run buys six Commissions and four Scar excisions, against spec 8.2's "not both". Renown cannot zero out, which is its stated job. It also quietly reopens the two farms the existing audit priced against 25 Renown: the Recruit brake weakens 5.1x while Marrow payouts scale only 3x (DEPTH_MARROW_MULT), so RETIRE_VESTING and the proven-death rule are load-bearing against a price that is falling under them.  
*The ruling taken:* R8.1 addendum: every Renown price is multiplied by DEPTH_RENOWN_MULT[deepest Depth ever completed] and rounded to 5. Commission 40/60/90/135/205, Excise 60/90/135/205/305, Recruit 25/40/55/85/130, Ward slot 30/45/... x the same index. One existing BALANCE row, one lookup, and it deliberately makes farming a shallow Depth unattractive (1x income against a 5.1x price index), which is the direction the game already wants. Alternative if Stephen prefers prices to stay printed: set DEPTH_RENOWN_MULT to all 1s and let structure carry the raise, which is already +40 percent at Depth II by stage count alone.

**Depth unlocks count boss wins at ANY Depth, so fifty four-minute Depth I quests unlock Marrowdeep**  
*Where:* RULES R8.4 ("Quests completed counts boss wins only. Depth unlocks: II at 3, III at 10, IV at 25, V at 50"), R9.4 (an offer per unlocked Depth, so Depth I is always playable); spec 9  
*The problem:* R8.4 puts no Depth qualifier on the counter, and the existing audit's minor 4 only settled the two-boss double count, not this. Depth I is the shortest (spec 13.1: 4 to 6 minutes against 8 to 9 at III), the safest (Strike 2, no fourth Aspect, no sealed Vault, and TNs never inflate so a tier-10 account with d8 average dice is near riskless), and it always stays on the board. The cheapest path to Marrowdeep is therefore 50 Depth I runs, roughly 4.2 hours of the easiest content, arriving at Depth V with a roster that has never seen Strike 3, a fourth Aspect or a sealed stage, and where R5.10 forbids mid-quest replacement. It also drags the deeper Depths' relic budgets and Marrow multipliers to a player who never earned them.  
*The ruling taken:* R8.4 CORRECTION, one clause: a boss win increments questsCompleted only when the quest was played at the deepest UNLOCKED Depth. The spec's thresholds are then exactly preserved and so is the total: 3 wins at I, then 7 more at II (10), then 15 more at III (25), then 25 more at IV (50). Shallow runs still pay Renown, relics and Marrow; they simply do not buy depth.

**Buying the 2 Marrow Legacy slot permanently deletes stock Callings from the creation deal, narrowing every future character to the Callings of the player's dead**  
*Where:* RULES R8.7 ("the consecrated Legacy first if any; then up to account.legacySlots Legacies drawn at random with distinct callings; then stock Callings not already represented, until three"), R8.2 ("Legacy slot 2 (+1 dealt, 2 to 3, cap 3)"), spec 14 (legacySlots: 2), spec 8.5 ("they are choosing between a stock Calling and the ability that belonged to the Zealot who died at the Drowned Gate")  
*The problem:* At legacySlots 3 the fill order consumes all three cards before the stock clause is ever reached, so once the player owns Legacies of three or more distinct Callings no stock Calling is ever dealt again. R8.7 also says "Picking a Legacy gives that Calling's effect, nothing more", so the deal becomes three cards drawn from whichever Callings happen to have died. An account whose dead are Vanguard, Zealot and Warden can never roll a Cutpurse or a Herald again, at any Renown tier, forever. The purchase is the cheapest thing in the game at 2 Marrow, it is irreversible (nothing un-buys a Marrow purchase), and it converts a permanent upgrade into a permanent narrowing of the character pool. Spec 8.5's whole framing is the word "between", and the purchase removes the other side. The existing audit's minor 5 ruled the overflow arithmetic (cards <= legacySlots <= 3) but not this consequence.  
*The ruling taken:* R8.7 addendum, one sentence: at least one of the three Calling cards is always a stock Calling not represented on the other two. The 2 Marrow purchase then raises Legacy cards from 1 to 2 (spec 14's legacySlots becomes 1 at start, cap 2), which is what the spec's own sentence describes, and the deal keeps a door to every Calling for the life of the account.

**The spec's pacing prose is off by roughly ten times against its own session lengths: Depth III lands at about one hour, not "roughly hour 10", and the whole Depth ladder is spent in about six hours**  
*Where:* spec 9 ("Undertow is the pivot point ... a genuine strategic re-learning at roughly hour 10"), 13.1 (Depth I 4 to 6 min, Depth III 8 to 9 min), 6.3 ("emerges naturally around quest 15"), 6.2 ("Expected career: 4-7 quests"), 11.5 ("keeps TN 3-7 honest at hour 200"); RULES R8.4 (unlocks 3/10/25/50), R10.1 (bank sizes)  
*The problem:* Multiply the spec's own numbers. Depth II at 3 wins with an 8 percent wipe is about 3.3 attempts at 5 minutes, ~17 minutes. Depth III at 10 wins is about 11 attempts at ~6 minutes, ~66 minutes, against a claim of hour 10. Depth IV at 25 wins is ~2.7 hours, Depth V at 50 wins is ~6.4 hours, after which no new structure exists at all, against 11.5's "hour 200". Section 6.3 puts retire-or-run at quest 15 (~1.7 hours) while 6.2 puts the career at 4 to 7 quests, so the decision actually first bites at quest 4, around 25 minutes. This is not cosmetic: the builder sizes the content banks, the Trait pool and the unique list to a curve that is ten times too slow, and Stephen is being told the game has a ten-hour on-ramp when it has a one-hour one.  
*The ruling taken:* Do not move the unlock counts (they retune every other number). Correct the prose and size the content to the real curve: Depth II ~20 min, III ~1 hr, IV ~3 hrs, V ~6 hrs, with the retire-or-run decision arriving at quest 4 or 5. Then record one Director call: at ~6 hours to max Depth, either the ladder needs a sixth Depth or a reason to replay Depth V (the Sigil offer, under the existing per-Depth reroll ruling, is the natural candidate), because after Depth V nothing structural is left.


### MINORS (4)

**The Vault, the spec's "Large relic reward" and Depth IV's mandatory sealed gate, is the worst-paying shape at Depth I, below a Toll**  
*Where:* RULES R5.9 ("Gate 3, Open 2, Toll 4, Chain 6, Relay 6, Vault 5 ... Relic rolls: Toll 1, Chain 1, Relay 1, Vault 1 at +1 rarity tier"), R5.5 (Vault TN 7, Toll TN 3), R9.1 (Depth IV stages 3 and 6 sealed on a Vault); spec 7.2 ("Vault | Player chooses the stat, TN 7. Large relic reward.")  
*The problem:* Expected value per slot at Depth I, using R1.4's corrected table and a d8 assignee (salvage 2.28 a roll flat, 4.92 at +1 tier). Toll: 0.75 x (4 + 2.28) = 4.71. Chain (TN 3 then 4, one character): 0.469 x (6 + 2.28) = 3.88. Vault (TN 7 on the player's best stat, ~33 percent at a tier-1 roster): 0.33 x (5 + 4.92) = 3.27. Relay: 0.313 x 8.28 = 2.59. So the hardest single check in the game pays less than a TN 3 Toll, and its Renown line (5) sits below Chain and Relay (6), which are easier. The card will read "Large relic reward" over the second-lowest payout on the board. At Depth IV the same shape becomes the seal the party must beat to leave the stage while still paying 5.  
*The ruling taken:* R5.9 CORRECTION: Vault pays 8 Renown and TWO relic rolls, the first at +1 rarity tier. EV becomes 0.33 x (8 + 4.92 + 2.28) = 5.02, the top of the shape ladder, which is what a TN 7 gate should be and what spec 7.2 already promises. Two numbers in BALANCE.RENOWN and one in the relic-roll row.

**Mend (20 Renown) becomes a dead purchase the moment a Marrow roster slot is bought: a Marrow upgrade permanently removes a Renown sink**  
*Where:* RULES R8.3 ("A roster character NOT deployed for a whole quest clears to 0 when that quest ends (rest). Mend clears everyone now."), R8.1 (Mend 20), R8.2 (Roster slot 4 then +2 each, 3 to 8), R5.7/R9.1 (RESPITE 1 at Depth I and II, 0 at III+); spec 8.2  
*The problem:* Three characters deploy out of up to eight owned, and every character not deployed rests free. From six owned onward the player always has three rested bodies and never needs Mend; before then, Depth I and II clear a point of Strain per stage through Respite anyway, so Strain rarely survives a quest. Mend's entire window is Depth III or deeper with rosterSlots 3 to 5, which is a few quests wide. R8.3's DECIDED note claims "this is the only reading under which Mend, the bench, and roster slots each have a job", but under it the roster slot purchase is exactly what kills Mend's job.  
*The ruling taken:* One clause in R8.3: a character who was not deployed clears half its Strain, rounded down (BALANCE.REST_FRACTION), rather than all of it. Mend then keeps a job at eight roster slots and rotation stays the cheap answer for light Strain. If Stephen prefers the free full rest, say plainly in the handoff that Mend is an early-game sink that dies at the second roster slot, so nobody tunes around a purchase that no longer gets bought.

**The authored challenge banks are half to a quarter of the spec's stated non-repetition target, and at the real pacing a line repeats inside the first hour**  
*Where:* RULES R10.1 ("Gate 20 lines per stat, Chain 8 per stat, Relay 16, Vault 16, Toll 12, Open 12", "A line is drawn per slot without repeating inside a quest"); plans/marrowdeep/data/challenges-*.json (verified counts: gate 20, chain 8 per stat; relay 16, vault 16, toll 12, open 12 total); spec 16 ("Challenge text pool ... Target ~40 entries per stat per shape for non-repetition across a 20-quest span")  
*The problem:* The spec asks for ~40 per stat per shape. RULES authors 20 per stat for Gate (half), 8 per stat for Chain (a fifth), and 16, 16, 12, 12 TOTAL, not per stat, for Relay, Vault, Toll and Open. A Depth I quest draws about 5 Gates, and repetition is only blocked inside one quest. Drawing ~1.5 GRACE Gates a quest from 20 lines gives a repeat with probability 1 - (0.95 x 0.90 x 0.85 x 0.80 x 0.75 x 0.70) = 69 percent by quest 5 and near certainty by quest 10. Relay has 16 lines against ~3 Relays a quest at Depth II, so it repeats by quest 3; Vault has 16 against two sealed Vaults a quest at Depth IV. Under the corrected pacing above that is the first hour of play, in a game whose only texture between runs is the text.  
*The ruling taken:* Author to the spec's number where it is cheap and say so where it is not: Relay, Vault, Toll and Open become per-stat banks (Relay and Vault 8 per stat, Toll and Open 6 per stat) rather than shared pools, and Gate rises from 20 to 40 per stat before launch. Add one line to R10.1: a line is drawn without repeating across the last 3 quests (a small recently-used ring in the save), which buys more perceived freshness than doubling any single bank.

**The spec's own "a good run buys a Commission or a Scar excision" is half false: at ~35 Renown a run, Excise is 1.7 runs, and the spec contradicts itself six lines later**  
*Where:* spec 8.2 ("Income: ~35 per completed Depth I quest", "A good run buys a Commission or a Scar excision, not both", "Excise is the most interesting purchase: nearly two runs' income"); RULES R8.1 (the same prices), R5.9  
*The problem:* At R5.9's computed ~35 Renown a Depth I run, the Hall costs in runs are: Redeal 10 = 0.29, Reforge 15 = 0.43, Mend 20 = 0.57, Recruit 25 = 0.71, first Ward Shelf slot 30 = 0.86, Commission 40 = 1.14, Excise 60 = 1.71. So a good run buys a Commission and change, but a Scar excision is nearly two runs, which is what the spec itself says two paragraphs later. The "or" sentence is the one a builder will quote when tuning, and it is wrong. Folding in salvage (~8 a run at Depth I, R6.7) makes real income ~43, so Commission is comfortably one run and Excise is 1.4.  
*The ruling taken:* Wording only, in the handoff's economy note: "a good run buys a Commission; a Scar excision is a run and a half." Keep both prices. If Stephen wants the printed sentence to be true, drop Excise to 40 and it becomes a genuine either/or at one run each, at the cost of making the veteran arc cheaper to extend.


---

## Lens: MATH

**What it reported:** MATH audit of MARROWDEEP_DESIGN_SPEC.md v1.0 against plans/marrowdeep/RULES.md. Every number in spec 2.1 to 2.5, 3.3, 8.2, 8.6, 11.2 and 11.5 was recomputed exactly, cross checked by Monte Carlo at 1,000,000 rolls per cell, and the death and economy claims were tested with a 20,000 quest simulation of R5.4/R5.5/R7.4 at the HANDOFF defaults (BASE_TOUGHNESS 4, RESPITE 1, STRIKE 2, STRIKE_TARGET attackers, Drowned Gate 3/3/4).

VERDICT ON THE TWO CORRECTIONS. R1.4 is RIGHT on both cells and on the whole corrected table: d4 vs TN 4 is (4-4+1)/4 = 1/4 = 25.0 percent (spec 31.3 is wrong) and d6 vs TN 6 is 1/6 = 16.667 percent (spec 22.2 is wrong). Monte Carlo at 1e6 returns 25.019 and 16.652. Every other spec cell is exact. The 50 percent diagonal survives. R1.5 is RIGHT: d12 floor 6 is (36+57)/12 = 93/12 = 7.75, gain +1.25; the spec's 7.58 and +1.08 are both wrong, and the other four floor rows are right.

About twenty other numbers are wrong. The three that stop a build are: (1) the boss clock at Toughness 4 gives an 81 percent per character death rate and a 78 percent wipe against targets of 12 to 15 and 8, and the spec's own 8.6 numbers turn out to describe the pre boss stages only (pre boss alone measures 13.45 / 31.50 / 1.47 percent and 0.404 Legacies per run, which is 8.6 almost to the decimal); (2) 8.6's three death numbers are arithmetically impossible together, and 15 makes them the acceptance gate; (3) the 2 point half die floor plus the 3 permitted flat points, 8 points on one stat, makes a d8 auto pass every TN in the game, which kills pillar 3.

Scripts and full output: /tmp/claude-1000/-workspaces-lucid-winds/e721c54b-b6bc-41a3-8304-ab0e74cec899/scratchpad/md-math.mjs (2.1 to 2.5, 3.3, 8.6, career and Marrow, leftover counts), md-boss.mjs (quest and boss simulation, parameter sweep), md-affix.mjs (measured quest shape, 11.2 pricing, 11.5 ceiling); outputs in OUT-math.txt, OUT-boss.txt, OUT-affix.txt.


### BLOCKERS (3)

**The boss clock kills the whole party at Toughness 4, and spec 8.6's numbers turn out to be the pre boss stages only**  
*Where:* spec 7.5 ("Each round, every unbroken Aspect deals 2 Strain to the party", "breaks all three in 2-3 rounds and walks out at half health"), 3.4 (Base Toughness 4, Base Armor 0), 8.6; RULES R7.4 (STRIKE 2, STRIKE_TARGET attackers), R7.2, R13.6, R2.3; proto/engine.js:31-34  
*The problem:* Under attackers with 3 characters on 3 Aspects each character is struck by exactly one Aspect for 2 every round, and when Aspects break R7.2 retargets while R13.6 makes the character an attacker of whatever it actually rolled against, so it stays 2 per character per round for as long as any Aspect lives. Toughness 4, Armor 0: Strain 2 after round 1, 4 after round 2, dead. The party has exactly two rounds against 3+3+4 = 10 Aspect hit points. Expected damage max(1, roll-TN) for a tier 1 party is about 1.5 per check without Push and 2.6 with, so 3 characters x 2 rounds = 6 checks buys 9 to 15 damage against 10. It is a coin flip by construction. 20,000 quest sim at the handoff defaults: per character death 81.4 percent (target 12-15), at least one death 86.6 (target ~35), full wipe 77.6 (target ~8), boss beaten 22.4, Legacies 2.44 per run (target ~0.4). Remove the boss and the same sim returns 13.45 / 31.50 / 1.47 percent and 0.404 Legacies, which is spec 8.6 almost exactly. Spec 8.6 was computed on the twelve pre boss checks and the boss was never in the arithmetic. Spec 7.5's own narrative also fails: 2 per round means half health is after round 1 and dead is after round 2, so nobody ever "walks out at half health" after 2 to 3 rounds, and "grinds the Choir for five rounds" is four rounds after the last character died.  
*The ruling taken:* Stephen's call on which lever moves, but the builder needs one today, because HANDOFF's own alarm band (per character death 8 to 20, wipe 3 to 15) goes red on the first sim run with no ruling attached. Measured options, all Depth I: Toughness 9 gives 17.2 / 23.6 / 12.8; Toughness 10 gives 10.5 / 14.6 / 7.7; STRIKE 1 with Toughness 8 gives 8.7 / 12.9 / 6.1; Aspect hit points 2/2/3 with Toughness 6 gives 32.4 / 43.3 / 24.9. The cheapest change that also repairs spec 7.5's prose is STRIKE 1 at Depth I with Toughness 6 to 8, because at STRIKE 1 a three round fight really does end at half health. Whatever is chosen, add an Armor refill or a party wide clear between boss rounds, or the bench slot that spec 7.1 calls the puzzle simply does not exist at the boss.

**Spec 8.6's three death numbers are arithmetically impossible together, and spec 15 makes them the acceptance gate**  
*Where:* spec 8.6 table (per character death 12-15%, at least one death ~35%, full wipe ~8%), 8.4 ("Legacies ... ~0.4 per run"), 15 Balance Harness ("target 12-15%", "target ~35%", "target ~8%")  
*The problem:* Take the last two as given. Runs with exactly three deaths are 0.08. Runs with at least one death but not three are 0.35 - 0.08 = 0.27, and each contributes at least one death. So E[deaths per run] >= 0.27 x 1 + 0.08 x 3 = 0.51, which forces a per character rate of at least 0.51/3 = 17.0 percent. The table says 12 to 15 percent, and 8.4 says 0.4 Legacies per run. 0.51 > 0.45 > 0.40. Solve it the other way and it is just as tight: with at least one death at 0.35 and E[deaths] at 0.40, E = P1 + 2*P2 + 3*P3 with P1+P2+P3 = 0.35 gives P3 <= (0.40-0.35)/2 = 0.025, so the maximum possible full wipe rate is 2.5 percent, not 8. Only two of the three can be true. Independence is not the problem and neither is correlation; this is a lower bound that holds for every joint distribution. My pre boss sim lands at 1.47 percent wipe, inside the 2.5 percent ceiling, which is the corroboration.  
*The ruling taken:* Pick the two that are the law and derive the third. If 0.4 Legacies per run is the anchor (it is, because 8.3's Marrow income is built on it), then per character 13.3 percent and at least one 35 percent stand and the wipe target must drop to 2.5 percent or below. Write the derived number into spec 8.6, spec 15 and HANDOFF's alarm band in the same edit, otherwise the harness is asked to tune to a point that does not exist and the builder will chase it.

**The half die floor plus the permitted +3 flat auto passes every TN in the game, for 8 points on one stat**  
*Where:* spec 2.4 ("a floor at or above the TN is a guaranteed pass", "Hard cap: floor may not exceed half the die"), 2.5 ("Hard cap: +3 total flat bonus per stat"), 11.2 ("Floor at half-die on one stat | 2 | Head", "+1 flat to one stat (cap +3 total) | 2 | Token, Hands, Weapon"), 11.5, pillar 3; RULES R1.3, R1.7, R1.9  
*The problem:* The two caps are stated in different sections and never composed. Minimum total on a roll is floor + flat. d8 floor 4 + 3 = 7. d10 floor 5 + 3 = 8. d12 floor 6 + 3 = 9. The TN band is 3 to 7 forever, so from d8 upward the character cannot fail any check in the game, ever, under any Sigil except Shivering. Price: 2 points for the Head half die floor plus three +1 flat lines at 2 each on Token, Hands and Weapon (R6.2 forbids repeating a key on one item, so they must be three items, all of which have budget at Depth I). Eight points. Four stats is 32 of the 80 points a Depth V loadout carries. Even d6 goes automatic at TN 3 through 6, which is 96.9 percent of all pre boss checks by the measured TN distribution. Spec 11.5's claim that "a player physically cannot spend 64 points on raw power" is also false on its own terms: +1 flat at the +3 per stat cap across four stats is 24 points, half die floors on four stats is 8, three die steps is 9, total 41 points of capped raw power, and R1.9 says in as many words that "Armor and Toughness have no cap", so the remaining points go there. Nothing forces a single point into a conditional.  
*The ruling taken:* Add one composed cap to R1.9 and to spec 2.5: floor + total flat on a stat may not exceed TN_MAX - 1, i.e. 6, so the worst roll always leaves a live check at TN 7. Or make floors and flats mutually exclusive per stat (Head reliability versus Token power, which is the axis 11.1 already claims). Either way the resolver needs the check at equip time and the Character screen needs to grey the line that does nothing, the way R1.7 already greys the fourth flat point.


### MAJORS (7)

**Spec 11.5's 64 point ceiling is the Depth III number; the Depth V ceiling is 80 plus 8 uniques**  
*Where:* spec 11.5 ("8 slots × ~8 points = 64 points if perfectly geared at Depth V"), 11.3 (Depth V Relic budget 10), 11.4 (Depth V weights 0/45/40/15), 9 (Depth V "Relics drop at Relic rarity only"); RULES R6.1, R6.4  
*The problem:* 8 points per slot is the Depth III Relic budget. Depth V Relic is 10, so a perfectly geared Depth V character carries 8 x 10 = 80 points, 25 percent more than the number the section reasons from, and R6.4 puts a named unique on every Relic rarity item on top of the budget, so it is 80 points plus 8 uniques. Worse, the two source tables disagree about whether 80 is a ceiling or the average: spec 9 and R6.1 both say Depth V drops are Relic rarity only, which makes every drop a 10 pointer, while spec 11.4 gives Depth V weights of 0/45/40/15 (mean 6.1 points per drop). R6.1 keeps both sentences. The same table pair contradicts itself one row up: 11.4 rolls Common at 10 percent at Depth IV while 11.3 gives Depth IV Common no budget at all, so a Depth IV Common is unrollable.  
*The ruling taken:* Fix the arithmetic to 80 and rewrite 11.5's conclusion against 80, since the section's whole argument is that the budget exceeds what raw power can absorb. Then rule the Depth V drop table once: either 11.4's weights stand and spec 9's "Relic rarity only" is struck, or the reverse and 11.4's Depth V row becomes 0/0/0/100. Give Depth IV Common a budget of 2 or set its weight to 0 and redistribute.

**Once per stage effects deliver 4x to 8x the +1 flat family per point, and R13.3 makes it worse**  
*Where:* spec 11.2 ("Reroll one die per stage | 3 | Charm", "Roll twice take higher, once per stage | 3 | Charm", "+1 flat to one stat | 2"); RULES R1.8, R13.3  
*The problem:* Measured against the R5.4/R5.5 quest shape (12.80 pre boss checks per quest, 4.27 per character, plus about 2.5 boss checks): +1 flat to one stat fires on roughly 1.4 of a character's checks and is worth 15.67 points of success per fired check, so 0.219 extra passes per quest for 2 points, 0.110 per point. Reroll one die per stage at 3 points converts a failed check into a pass at rate p on every stage, and R13.3 makes a boss round a stage, so it fires about 7.5 times a quest: 2.68 extra passes, 0.893 per point, 8.1x the flat rate. Roll twice take higher is 1.86 passes, 0.619 per point, 5.6x. Even granting perfect assignment steering that doubles the flat affix's firing rate, the gap is still 4x. Both reroll lines are also stat agnostic and the reroll can be spent on an ally's check, while every flat, floor, step, reroll 1s and surge line is locked to one stat and therefore idle on three quarters of the character's checks. The unit in 11.2 prices a stat locked line and a stat agnostic line identically.  
*The ruling taken:* Either price the once per stage family at 6 points and the stat locked family at 2, or state the unit as per fired check and give every affix a printed firing rate so the generator can weight by expected value rather than by nominal points. R13.3's own escape hatch ("if a Gambler reroll per round proves too strong, the fix is one line") should be taken now: reset rerollStage per fight, not per round, which cuts the 7.5 uses to 6.

**TN 6 never occurs anywhere in the game, so the 2 point Token affix "+2 vs TN 6+" is close to dead**  
*Where:* spec 11.2 ("Conditional: +2 vs TN 6+ | 2 | Token"), 7.2 (Gate TN 4-5, Chain 3 then 4, Relay 4 and 4, Vault 7, Toll 3, Open 3), 7.5 (Aspect TNs 5, 5, 4), 2.3 (the TN 6 column); RULES R5.5, R7.1  
*The problem:* Measured over 200,000 generated Depth I quests, the TN distribution of every pre boss check is TN 3 26.56 percent, TN 4 51.58, TN 5 18.73, TN 6 0.00, TN 7 3.13. No shape produces a TN 6. The boss produces TN 4 and 5. So the only TN 6+ check in the game is the Vault at TN 7, 0.400 per quest, and the wearer takes it about one time in three: 0.033 extra passes per quest for 2 points, 0.017 per point, 0.15x the +1 flat rate, i.e. about 6.7x underpowered. The whole TN 6 column of the 2.3 master table is decorative for the same reason, and 2.3's line "a designer can read difficulty directly off it" is reading a row nothing uses.  
*The ruling taken:* Either widen Gate to TN 4/5/6 in R5.5's GATE_TN_WEIGHTS (which restores the d10/TN6 rung of the 50 percent diagonal that 2.3 calls the spine, and costs one weight change), or reprice the affix to 1 point and rewrite it as "+2 vs TN 7". Do not ship both the affix and the empty column.

**"Floors count as +1" at 3 points is worth zero in the two states it will most often be in**  
*Where:* spec 11.2 ("Floors count as +1 | 3 | Head", the most expensive Head affix), 11.1 ("Head | Floors | ... 'floors count as +1'"), 4 Ironbound; RULES R1.3 ("Cap: F <= die / 2 ... applied after every '+1 to floors' effect"), R4.4, R13.11  
*The problem:* R1.3 applies the half die cap after the +1, so a stat already sitting at its half die floor gains exactly nothing: d8 floor 4 plus 1 is 5, capped straight back to 4. A stat with no floor at all also gains nothing, since there is nothing to lift. The only state where the affix does anything is a stat at floor 3 on a d8 or larger, and averaged over the tier 1 die mix that is +0.117 EV for 3 points, 0.039 EV per point against the section's own 0.500 target: 13x underpriced. Ironbound (R4.4, "every floor on your gear counts as 1 higher (respects the half-die cap)") has the identical hole, and an Ironbound wearing the 2 point half die Head affix gets nothing from its Origin.  
*The ruling taken:* Either let floorPlus raise the cap by the same 1 (floor <= die/2 + floorPlus), which makes the affix and Ironbound do what their words promise and is one clause in R1.3, or delete the affix and give Head a third line that is not a no op. If the cap is kept as written, the item generator must exclude floorPlus from any item that has no other floor, or 3 of a Depth I Rare's 4 points buy literally nothing.

**Spec 6.2's "expected career 4-7 quests" is 2.83 under its own Scar table, which breaks spec 8.3's Marrow income by 2.4x**  
*Where:* spec 6.2 ("permanent -1 max Toughness" per quest, the table ending "4 | 4 | 0 → cannot deploy", "Expected career: 4-7 quests"), 3.4 (Base Toughness 4), 8.3 ("~1.5 per quest"), 6.3; RULES R2.3, R2.5, R2.6  
*The problem:* One Scar per quest survived against base Toughness 4 is a hard wall at 4 quests, not a soft one. At the spec's own 13.4 percent per quest hazard the mean career is sum of survival probabilities to the cap: 0.867 + 0.751 + 0.652 + 0.565 = 2.83 quests. At 12 percent it is 2.94, at 17 percent 2.57. Nowhere near 4 to 7. And the hazard is not flat: at 3 Scars the character is Toughness 1, so a single failed check at strainOnFail 1 kills, and the real hazard in that last quest is close to the probability of taking any Strain at all, which is close to 1. Now run 8.3's chain. Three deployed and mean career C means 3/C characters leave the roster per quest; deaths are 0.4 of them at 1 Marrow, the rest retire at 2 + Traits with Traits = quests survived. At C = 5.5 that is 0.4 x 1 + 0.145 x 7.5 = 1.49, which is exactly 8.3's ~1.5, so the number was derived from the career the spec claims. At the career the Scar table actually produces, C = 2.83, it is 1.060 x [0.377 x 1 + 0.623 x 4.83] = 3.59 Marrow per quest, 2.4x the stated income. Spec 6.3's retire or run tension also does not exist where the spec puts it: a 4 Trait veteran has 4 Scars and Toughness 0, so R2.3 refuses to deploy her and the "gamble one more haul" choice is already made.  
*The ruling taken:* Pick one. Either a Scar every second quest survived, which puts the wall at 8 and the mean career near 5.5 and makes 8.3's 1.5 correct as written; or keep the Scar per quest and restate 6.2 as "expected career 3 quests" and 8.3 as ~3.6 Marrow per quest, then reprice every Marrow purchase against the new income. Also move the 6.3 retire or run example from 4 Traits to 3, which is the last quest a scarred veteran can legally deploy.

**The die step is the worst offensive point in the book, and spec 2.4's "floors give less raw EV than a die step" is false at d10 and d12**  
*Where:* spec 2.4 ("Floors give less raw EV than a die step", "Hard cap: floor may not exceed half the die"), 11.2 ("Step one stat up one rung | 3 | Hands", "Floor at half-die on one stat | 2 | Head", "+1 flat to one stat | 2"), 11.1 ("Head and Hands are the floors-versus-ceilings axis")  
*The problem:* At the half die cap the floor gain is d4 +0.25, d6 +0.50, d8 +0.75, d10 +1.00, d12 +1.25 EV. A die step is exactly +1.00 plain and +0.867 to +0.980 with Surge. So the claim is false at d10 (equal) and d12 (the floor is 25 percent better), and it is the floor that is the cheaper affix, 2 points against 3. Pricing the whole offensive column against the section's own 0.500 EV per point: +1 flat 1.000 EV for 2 points = 0.500 per point (exact, it is the definition of the unit); reroll natural 1s 0.544 for 1 = 0.544; surge on N-1 1.182 for 2 = 0.591; floor 3 0.355 for 1 = 0.355; half die floor 0.550 for 2 = 0.275; die step 0.924 for 3 = 0.308. The flagship Hands affix, the one 11.1 builds a whole design axis on, delivers 62 percent of the unit and less per point than the Head affix it is supposed to be the counterweight to. At TN 5 it is the same story: +1 flat buys 9.5 points of success on average and costs 2, a die step buys 10.4 and costs 3.  
*The ruling taken:* Price the die step at 2, or price +1 flat at 3, and restate 2.4 as "floors give less raw EV than a die step below d10". The half die floor also cannot honestly carry one price: it delivers 0.25 EV on a d4 and 1.25 on a d12, a 5x spread, so either price it per rung or let R6.3 refuse to roll it onto a d4 or d6 stat the way it already refuses stepStat on a d12.

**Surge on N-1 buys exactly zero success at TN 5 on every die from d6 up, at 2 points**  
*Where:* spec 11.2 ("Surge on N−1 as well as N | 2 | Hands, Weapon", under the header "1 Point ≈ +0.5 EV ≈ +6% success at TN 5"); RULES R1.2, R4.3 Fenwise, Trait Deepdrawn  
*The problem:* In EV the affix is priced perfectly: exploding at two faces gives EV = d(d+1)/(2(d-2)), so +1.667 on a d4 down to +0.709 on a d12, averaging 1.182 for 2 points against a 1.000 target. But the section's other stated unit is success at TN 5, and there the affix is worth nothing at all whenever TN <= die - 1, because those faces already passed: a d8 at TN 5 passes on 5, 6, 7 and 8 with or without the lowered threshold. The gain is 0.00 points of success on d6, d8, d10 and d12 at TN 5, and +18.75 on a d4. All of the affix's value is in surplus damage at the boss and in the TN 7 Vault, 0.4 checks a quest. Fenwise and Deepdrawn inherit the same shape.  
*The ruling taken:* No repricing needed, but the section's claim that every affix is priced in one unit is not true and the two units disagree by an infinite ratio here. Say plainly in 11.2 that the surge lines are damage and Vault affixes, not success affixes, and keep them off Token so they never read as a general bonus. Same footnote covers the four defensive lines (Toughness, Armor, strike reduction, bench value), which the EV unit cannot price at all.


### MINORS (7)

**The exploding die plateau makes a +1 flat modifier worth exactly zero in two cells, and nothing in RULES stops the game selling one there**  
*Where:* RULES R1.4 (the corrected table, d4 at TN 4 and 5 both 25.0, d6 at TN 6 and 7 both 16.7); spec 5 Herald ("+1 when using the same stat"), 6.1 Quickstudy, 11.2 ("Conditional: +1 while at 2+ Strain", "+1 per dead ally", "+1 to both characters in a Relay")  
*The problem:* R1.4 derives the plateau correctly and calls it a feature, but does not draw the consequence. Because P(d4, TN 5) = P(d4, TN 4) = 1/4 and P(d6, TN 7) = P(d6, TN 6) = 1/6, a +1 flat modifier is worth 0.00 points of success in those two cells. Computed gains for +1 flat, in points of success: d4 gives 25.00 / 25.00 / 0.00 / 6.25 / 6.25 across TN 3 to 7; d6 gives 16.67 / 16.67 / 16.67 / 16.67 / 0.00; d8 and up are flat at 1/die everywhere. So a Herald's +1 on a d6 NERVE at the TN 7 Vault, and every +1 source on a d4 stat facing TN 5, buy literally nothing. Push at +2 is unaffected, which is the correct instinct in 2.6, and this is a live case because 35 percent of tier 1 stat rolls are d4 and 30 percent are d6.  
*The ruling taken:* No rule change needed, but the pre roll strip that R13.10 already draws should show the modifier's real effect, and a +1 that changes nothing should read as +1 (no effect) rather than as help. One line in the resolver, and it saves the exact confusion the plateau will otherwise cause the first time a player Heralds into a Vault.

**Spec 3.3's average rung column is wrong in three of five rows**  
*Where:* spec 3.3 table ("Avg Rung" ~d7.1, ~d7.6, ~d8.0)  
*The problem:* With rung values 4/6/8/10/12 and the stated weights, which all sum to 100: tier 1 = 0.35x4 + 0.30x6 + 0.20x8 + 0.10x10 + 0.05x12 = 6.400 (spec ~d6.4, right); tier 3 = 7.020 (spec ~d7.1, off by 0.08); tier 5 = 7.540 (spec ~d7.6, off by 0.06); tier 7 = 7.940 (spec ~d8.0, off by 0.06); tier 10 = 8.300 (spec ~d8.3, right). R2.2's interpolated tiers are fine and monotone: 6.400, 6.710, 7.020, 7.280, 7.540, 7.740, 7.940, 8.060, 8.180, 8.300.  
*The ruling taken:* Change the three cells to ~d7.0, ~d7.5, ~d7.9. Small, but the column is the only thing a designer reads to feel the Renown curve, and 6.400 to 8.300 across ten tiers is a 1.9 point spread, which is worth stating honestly.

**Spec 2.1's "+1 per rung with or without Surge" is 13 percent short at the bottom of the ladder**  
*Where:* spec 2.1 ("Each rung is worth almost exactly +1 expected value, with or without Surge. This linearity is load-bearing")  
*The problem:* Exact exploding means are d(d+1)/(2(d-1)): 20/6 = 3.3333, 42/10 = 4.2000, 72/14 = 5.142857, 110/18 = 6.111111, 156/22 = 7.090909, all matching the spec's rounded column. The steps between them are +0.867, +0.943, +0.968, +0.980, not +1. Plain steps are exactly +1.000. So the ladder is linear without Surge and slightly compressed with it, and the compression is worst exactly where 2.2 says Surge is doing the most work ("small dice get compensating excitement"). The d4 to d6 rung is 13.3 percent short of the +1 the point economy is built on.  
*The ruling taken:* Reword to "+1 exactly without Surge, +0.87 to +0.98 with it" and note that the die step affix is therefore priced against 0.92, not 1.00, which is the number 11.2 needs anyway.

**Spec 2.4's floor table mixes units with 2.3, and the step 1 harness cannot tell which to reproduce**  
*Where:* spec 2.4 ("Base EV | 4.50" for d8), 2.1 ("Mean w/ Surge | 5.14" for d8), 2.3 ("All values include Surge"), 15 step 1 ("a 100,000-iteration harness that reproduces the §2.3 master table exactly. Do not proceed until it matches"); RULES R1.5 (restates base 6.50 for d12)  
*The problem:* 2.3 declares that every value in the resolution engine includes Surge, but 2.4's Base EV column is the plain mean (4.50 on a d8, not 5.14) and so is its Floored EV column. R1.5 keeps the plain convention when it corrects the d12 row. It happens not to matter for the Gain column, because the surge term does not depend on the floor: d8 floor 3 with Surge is 5.1429 to 5.5179, gain +0.3750, identical to the plain gain, and d12 floor 6 is 7.0909 to 8.3409, gain +1.2500. But the builder is told to reproduce the tables exactly before proceeding and has two conventions in adjacent sections.  
*The ruling taken:* One sentence in 2.4: "the EV columns here are plain means; Surge adds a constant per die and does not change the Gain column". Then the harness knows to assert the gains, not the absolutes.

**Spec 8.6's "~1.33 failures each" is 1.20, and "~4 failures" is 3.6**  
*Where:* spec 8.6 ("A quest is 12 checks. Well-assigned characters average ~70% success, producing ~4 failures spread across 3 characters (~1.33 each)")  
*The problem:* 12 x 0.30 = 3.6 failures, and 3.6 / 3 = 1.20 each. The 1.33 is 4/3, that is, the already rounded 4 divided a second time. The measured quest is also 12.80 checks, not 12 (R5.4/R5.5 over 200,000 quests: 10.00 slots, 2.803 Relay checks, 0.400 Vaults, 0.999 Tolls), which puts it at 3.84 failures and 1.28 each. The rounding compounds because this line is what 8.6's death rate is reasoned from.  
*The ruling taken:* State 12.8 checks and 3.8 failures, 1.28 per character. And note the real Strain arithmetic while you are there: at strainOnFail 1 on stages 1 to 4 and 2 on stage 5, 1.28 failures per character is about 1.6 Strain against Toughness 4, before Ambush, contagion, Toll fees and Push. That is the number that shows why the pre boss game is safe and the boss is not.

**R6.2's remainder rule writes repeated Toughness lines on one item while the same sentence forbids repeating a key**  
*Where:* RULES R6.2 ("without repeating a key on one item (a stat targeted key may repeat with a different stat)" then "the remainder becomes '+1 Toughness' lines, 1 point each, which every slot may carry"), R6.3 ("if all four are d12 it becomes +1 Toughness lines"), R13.11 (pins the repeat rule for floors only); spec 11.2 ("Toughness +1 | 1 | Chest"), 3.4 ("Toughness 4 is the single highest-leverage number in the game")  
*The problem:* "Lines" is plural in both places, and R6.3's all d12 case can leave a remainder of 3, so the fill produces three +1 Toughness lines on one item, which repeats a key the same rule just forbade. R13.11 resolves the identical ambiguity for floors and is silent on Toughness. It is not a cosmetic gap: it decides whether a Chest can carry +1 Toughness or +6, and Toughness is the constant spec 3.4 calls the highest leverage number in the game and spec 16 calls the highest risk. It also makes the cheapest and structurally strongest line in the book (1 point, permanent, cancels a Scar that costs 60 Renown to Excise) the automatic filler on every slot whenever nothing else fits, which is Hands, Feet and Weapon every time they land on an odd remainder.  
*The ruling taken:* Extend R13.11 to say one Toughness line per item, and make the remainder rule fall back to a second 1 point key rather than stacking. If multiple lines are intended, say so and cap it, then re run the death sim, because +3 Toughness on a Chest is a bigger swing than every offensive affix in 11.2 put together.

**Small count errors in 8.2, 11.6 and 12**  
*Where:* spec 8.2 ("A good run buys a Commission or a Scar excision — not both", income ~35, Commission 40, Excise 60), 11.6 ("12 prefixes × 6 bases per slot × 12 suffixes = 864 names per slot"), 12 ("Total: ~76 assets. 46 of them are icons"); RULES R10.6 ("per affix key 4 prefixes and 4 suffixes; per slot 6 bases"), R5.9  
*The problem:* 8.2: at ~35 Renown a run a good run buys neither, since a Commission is 40. The sibling claim about Excise is right, 60/35 = 1.71 runs, "nearly two runs' income". For the record my sim puts a won Depth I quest at 39.2 Renown from R5.9's per shape table before any salvage, so ~35 is 11 percent low, and 29.9 across all runs at the current death rate. 11.6: 12 x 6 x 12 = 864 is correct arithmetic but not reachable from R10.6, which authors 4 prefixes and 4 suffixes per affix key rather than 12 shared, so a given affix pair yields 4 x 6 x 4 = 96 names and a one affix item, which is every 2 point Depth I Common and therefore 60 percent of Depth I drops, has no "of" clause at all and only 4 x 6 = 24 names per slot. 12: the ten category rows sum to 73, not ~76, and the six icon rows sum to 38, not 46 (42 with card frames, 50 with relic art); 46 is not any subtotal of the table.  
*The ruling taken:* 8.2: "a good run buys most of a Commission; two buy a Scar excision", or raise the shape payouts in R5.9 until 35 is real. 11.6: either raise R10.6 to 12 and 12 per affix key, or restate the count as 96 per affix pair and add a second base list so Commons do not repeat names inside the first hour. 12: correct the total to 73 and the icon count to 38.


---

## Lens: SCREENS, FLOW AND SAVE

**What it reported:** Audited MARROWDEEP_DESIGN_SPEC.md §13 against RULES.md R11 (and R2, R5 to R9, R13) and HANDOFF-MARROWDEEP.md §5 and §6, through the screens, flow and save lens. Note both plan files were edited under me during the read (RULES.md R5.9's Vault reward and R6.7's target row are newer than my first pass); every line I quote was re-read at 12:47.\n\nSeventeen findings, seven of them blockers. The load bearing ones: (1) RULES contradicts itself on where PUSH and TWICE live — R5.6 and R5.7 make a per check pre roll strip mandatory and R11.6 plus the handoff's INPUT law put the chips at assignment, where Cutpurse's automatic twice and a Chain's second Push cannot be computed, and that strip is in no screen list. (2) Nothing already rolled and painted is in the saved state: resolveNext returns the RESULT rather than storing it, rng.n has already advanced, so the P2 save gate's three requirements (reload on a RESULT, a drop, a Trait pick) cannot be met as specified, and the Trait deal is save scummable. (3) The step enum is missing six stopping screens and bossRound resolves a whole round at once, so the boss cannot stop on every card. (4) The offer seed contradicts R9.4 in two directions: a wipe deals the identical quest back, a Depth I win re-rolls all five offers. (5) BOOT's CONTINUE has no routing, so a first run interrupted between the three free characters resumes into a Hall with one character, 0 Renown, a 25 Renown Recruit and no stray. (6) \"Tapping an assigned character clears it\" forbids the doubling R5.6 makes mandatory at Relay plus Relay and at every Depth IV boss round, and a Relay's two seats are not bound to their two independently rolled checks. (7) Drops that run with quest null (Commission, dead gear, retirement) have nowhere in the save to live, so a reload burns 40 Renown, and R6.10's keep one of three cannot be expressed on R6.7's one relic at a time screen.\n\nThe arithmetic that will turn a gate red on day one: three character cards at 320 CSS px are (320 − 24 − 16) / 3 = 93.3 px wide and two 48 px chips need 100 px, so P1 step 11's layout gate fails at 320x568 on the game's main screen while passing at 375. The Hall at Depth V asks for ≈790 px of offer cards in a 390 px band with a footer the inherited one column .screen rule cannot pin, and five DEPLOY cards is a five way decision against pillar 6. The four Aspect Depth IV boss at 320x568 comes to ≈550 px of a 568 px viewport and no gate ever measures it.\n\nOmitted from the plan's screens but required by RULES: the quest start drop screen for Ashwalker (a starting Origin), Lanternborn's lantern line, the replacement offer in the stage end sheet, seats on Aspect cards, a damage line on the RESULT card at the boss, BACK or CLOSE on Creation, Deploy, Character and all five sheets, any history or popstate handling, and five save fields (freeRolls, the per Depth offer counter, the pending drop queue, the shown Legacy flag, R10.1's recently used line ring) plus a wall typed as strings with an undefined MAX merge.


### BLOCKERS (7)

**Push and TWICE have two contradictory homes, and the pre roll strip is not in any screen list**  
*Where:* RULES.md R5.6 (≈line 226) and R5.7 (≈line 227) vs RULES.md R11.6 (≈line 502); HANDOFF-MARROWDEEP.md §4 INPUT (line 240) and §6 "Quest"/"Result" (≈lines 428, 440); §5 P1 steps 3 and 4  
*The problem:* R5.7 makes a per check screen mandatory: "Each check: a **pre roll strip** (the character, the stat die, the TN, the consequence glyph for that stat, a PUSH chip and a TWICE chip when either is available, ROLL), so a player decides to Push a Chain's second check having seen the first". R5.6 repeats it: "Push and roll twice are chosen per check at its own roll (R5.7)." But R11.6 puts them at assignment ("Push and roll twice chips on the character card once assigned; RESOLVE"), §4 INPUT says "Chips (PUSH, TWICE) are 48 px toggles on the assigned character's card", and §6 lists Title, Creation, Hall, Deploy, Roster, Character, Quest, Result, Stage end, Drop, Boss, Aftermath, Wall, HOW — no strip. P1 goes RESOLVE straight to the RESULT card. The strip is load bearing for four other rules that then have nowhere to render: R13.8's "Cannot pay" chip on a Toll, R9.1's "cannot reach" greying on a sealed Vault, R13.10's consequence glyph beside a chosen Vault/Open stat, and the Blindfold Ward's "shows its TN once the wearer is on it, before RESOLVE". It also makes two things uncomputable at assignment: on a Chain (one slot, same character, two checks) one chip cannot express "Push the second, not the first", and Cutpurse's automatic twice (R4.10, first GRACE check of the stage) cannot be known at assignment because slot order and forfeits decide which check is first, so R13.12's "when an automatic twice fires, the manual twice chip is unavailable and is not consumed" cannot be evaluated when the chip is drawn.  
*The ruling taken:* Build the strip as its own screen and its own step (`preroll`) between `assign` and `result`, exactly as R5.7 writes it, and delete the chips from R11.6/§4 INPUT (or demote them to a read only indicator on the character card). One line in §6 between "Quest" and "Result": "Pre roll. A veil over the Quest screen: portrait, stat die, TN, consequence glyph, PUSH and TWICE chips when available, ROLL (56 px)."

**Nothing that has already been rolled and shown is in the saved state, so a reload mid RESULT, mid drop or mid Trait pick cannot restore it**  
*Where:* HANDOFF-MARROWDEEP.md §4 SIM (line 207) and SAVE (line 242); §5 P2 step 9 (line 378: "reload on a RESULT card restores the same card with the same numbers; reload on a drop screen restores the drop; reload on a Trait pick restores the three cards")  
*The problem:* `resolveNext(state)` "resolves ONE check and returns the RESULT" — the RESULT is a return value, and the only things §4 says the quest state carries are `step` and `cursor`. The account save carries `rng: { seed, n }` and "the engine's draw() advances n, so a saved game resumes on the same stream". So the naive write (save after resolveNext, step='result', n already advanced past the roll) restores a card with no numbers and the next resolveNext resolves the NEXT check: the card is skipped. The alternative naive write (save n from before the roll) re-derives the same numbers, which works for the RESULT but makes the Trait deal save scummable: R2.5 deals "3 Traits it does not own" at `aftermathTrait`, and if the deal is redrawn from a stream position stored after it was painted the player reloads until the deal is good; if the deal is redrawn from before, it is identical and safe, but neither is written down. Same hole for the drop queue: R5.9 pays "Toll 1, Chain 1, Relay 1, Vault 2 ... the boss 1 at +1 tier" relic rolls, and a relic is a generated object with rolled affixes and a rolled name; if it is not persisted before it is painted, the reload shows a different relic than the one the player was looking at. Same for the three Calling cards at creation (R8.7) and the three locked Origin cards (R8.2).  
*The ruling taken:* One rule, one field: `quest.pending` (and `save.pending` outside a quest, see the Commission finding) holds every rolled artefact the moment it is produced and BEFORE it is painted — the RESULT object from `resolveNext`, the drop queue as generated relics, the dealt Trait/Calling/Origin ids — and the save is written at every step transition, not at stage end. Add one line to §4 SAVE naming the write points, and one assertion to test/save.mjs: reload on a Trait pick twenty times and assert the same three ids come back.

**The step enum has no state for six screens that stop, and bossRound resolves a whole round in one call**  
*Where:* HANDOFF-MARROWDEEP.md §4 SIM line 211 (`step` = `assign | result | drop | stageEnd | replace | boss | aftermathScar | aftermathTrait | done`) and line 207 (`bossRound(state, plan)`); §5 P1 step 6; RULES.md R11.7, R11.8, R11.9, R9.1  
*The problem:* Six screens that the plan says stop and wait for CONTINUE have no step to restore into: the pre roll strip (R5.7, see above), the STRIKE card (P1 step 6: "then the STRIKE animation ... next round", and R13.7 puts interment after it), the death moment (R11.9: "the character card goes to bone, one line, CONTINUE"), the rewards sheet (R11.8: "the boss falls, rewards, drops"), the wipe card (R11.8: "the wipe card with the salvage line and the Legacies made" — the only place a player learns what a wipe paid, and `done` would drop them in the Hall having never read it), and the sealed Vault retry at Depth IV/V (R9.1: the Vault "is assigned again" while stage end does not run). `boss` is one value covering round assignment, three RESULT cards and the strike. Worse, the API contradicts the screens: `bossRound(state, plan)` resolves a whole round, while `resolveNext(state)` exists "so the page can stop on every card" — so at the boss the page cannot stop on each card without a second path through the sim, and P1 step 4's law ("Nothing advances on a timer") cannot be kept.  
*The ruling taken:* Extend the enum to `assign | preroll | result | strike | death | drop | stageEnd | replace | boss | rewards | wipe | aftermathScar | aftermathTrait | done`, give `cursor` a defined meaning in each (check index, aspect index, survivor index, drop index), and make `bossRound(state, plan)` set up the round only — every check at the boss goes through `resolveNext` like every other check, and the strike is its own transition.

**The offer seed contradicts R9.4: a wipe deals the identical quest back, and one win re-rolls all five Depths**  
*Where:* HANDOFF-MARROWDEEP.md §4 RNG line 185 ("A quest is generated from `mixSeed(account.rng.seed, questCount)` at the offer") vs RULES.md R9.4 (≈line 465) and R8.4  
*The problem:* R9.4: "An offer re-rolls only when a quest at ITS OWN Depth ends, won or wiped; the other Depths' offers stand (audit CORRECTION: re-rolling everything on any quest made a four minute Depth I run the cheap re-roll this rule exists to forbid)". R8.4: quests completed "increments once per quest, on the FINAL boss falling". Put together with the single salt: (a) a wipe does not move `questCount`, so the re-roll R9.4 demands after a wipe produces the byte identical quest — same Sigils, same boss, same stage shapes; (b) a Depth I win moves `questCount`, so all five offers change at once, which is the exact dodge R9.4 exists to forbid; (c) with one salt the five offers would be identical to each other unless depth enters the mix, which the line does not say. The five offers are also persistent state and the save schema (§4 SAVE) has no field for them.  
*The ruling taken:* `account.attempts[depth]` (a counter incremented when a quest AT THAT DEPTH ends, won or wiped) in the account and the save; the offer for depth d is `mixSeed(account.rng.seed, d * 1000003 + account.attempts[d])`. One line in §4 RNG, one field in §4 SAVE, one assertion in --test: end a Depth I quest twice and assert the Depth II offer's boss and Sigils are unchanged and the Depth I offer's are not.

**A first run interrupted between the three free characters resumes into a dead Hall**  
*Where:* RULES.md R2.1 (≈line 103, `account.freeRolls` starts at 3) and R8.0 (the STRAY); HANDOFF-MARROWDEEP.md §4 BOOT ("reads the save, shows the title (CONTINUE if a save exists, BEGIN otherwise)") and §5 P1 step 1  
*The problem:* BOOT routes on one bit: a save exists or it does not. It never says where CONTINUE goes. A first run is BEGIN then three creations; a player who keeps one character and closes the tab has a save with `freeRolls: 2`, one character, 0 Renown. CONTINUE lands them in the Hall (nothing else is defined), where Recruit costs 25 Renown they do not have, and R8.0's stray does not fire because it requires "the roster holds no deployable character" and they hold one. Their two free rolls are unreachable, and their first quest is one character alone at a boss (R8.0 does allow a short handed deploy). The same hole eats a Recruit bought from the Hall: R11.3's creation screen has ROLL, KEEP and REDEAL and no cancel, so if the 25 Renown is charged on the button the player who backs out has paid for nothing, and if it is charged at KEEP nothing says so.  
*The ruling taken:* Write BOOT's routing as three tests in order: `quest` not null goes to the quest at its step; else `account.freeRolls > 0` goes to Creation; else the Hall. And in R8.1/R11.3: the Hall's Recruit reads `freeRolls` first and costs 0 while it is above zero, the 25 Renown is charged at KEEP not at the button, and Creation entered from the Hall carries a BACK that refunds nothing because nothing was spent.

**The assignment gesture forbids the doubling R5.6 makes mandatory, and a Relay's two seats are not bound to their two checks**  
*Where:* HANDOFF-MARROWDEEP.md §4 INPUT (line 239: "tapping an assigned character clears it"); RULES.md R5.6 (≈line 218), R5.5 (Relay: "two stats rolled independently (may match), TN 4 and 4"), R3.3, R7.2; §6 "Quest" ("a Relay card has two portrait seats")  
*The problem:* R5.6 requires a character to hold two checks in three named cases: three living at Relay plus Relay (four checks, three bodies), two living at Gate plus Relay, and every boss round at Depth IV/V (R7.2 "doubling up allowed; nobody benches", three characters against four Aspects). In all of them the player must tap an already assigned character a second time — which §4 INPUT defines as "clears it". The gesture cannot express the rule it exists to serve. Three more cases are unwritten: tapping a character onto an occupied seat (replace the occupant or refuse), which seat of a Relay a tap fills, and how to swap them. The seat order is not cosmetic: R5.5 gives a Relay two independently rolled stats, R3.3 says "A Relay that fails its first check pays nothing, rolls nothing more, and the second character's Push was never paid", so which body rolls first is a real decision, and §6's Relay card is drawn as two anonymous "portrait seats" with one TN.  
*The ruling taken:* Three sentences in §4 INPUT: (1) tapping an assigned character SELECTS it for a further seat when doubling is legal (checks > living deployed) and clears it only on a second tap while selected or on the seat's own X; (2) tapping a character onto an occupied seat replaces the occupant, who returns unassigned; (3) a Relay card's seats are numbered 1 and 2, each labelled with its own stat glyph and TN, filled in tap order, and tapping seat 1's portrait swaps the two.

**A drop that happens outside a quest has nowhere to live, so a reload mid Commission burns 40 Renown**  
*Where:* RULES.md R6.10 (≈line 328), R6.7 (≈line 309), R2.6 (retired/dismissed gear "goes to the drop screen (R6.7) for the roster"), R6.8; HANDOFF-MARROWDEEP.md §4 SIM (`applyDrop(state, relicId, choice)`) and SAVE (`quest: null | questState`)  
*The problem:* `applyDrop` takes the quest state and the drop queue is quest state, but three drop flows run with `quest: null`: a Commission (R6.10: three relics rolled, "keep one (the drop screen); the other two vanish"), a retired or dismissed character's gear (R2.6), and the Hall side of R6.7 ("Between quests the same screen serves the Hall's Commission"). A reload after paying 40 Renown and before keeping one loses the three relics, and there is nothing in the save to restore. R6.10 is also not expressible on the screen R6.7 describes: that screen shows ONE relic at a time with TAKE RENOWN on each, so walking three relics one at a time either pays salvage Renown for the two the player declines (contradicting "the other two vanish") or offers a TAKE RENOWN that must be hidden for reasons the screen never explains.  
*The ruling taken:* Move the queue up one level: `save.pending = { kind: 'drop', relics: [...], targets: 'deployed'|'roster' }`, written the instant the Renown is spent or the relics are rolled, consumed by `applyDrop`; `quest` may point at it but does not own it. And give the Commission its own three card pick first (which is pillar 6's three cards, and matches R8.2's locked Origin deal): three relic cards, tap one, the other two vanish with no TAKE RENOWN, and only the kept relic enters the drop screen.


### MAJORS (7)

**PUSH and TWICE cannot be 48 px on a character card at 320 wide**  
*Where:* HANDOFF-MARROWDEEP.md §6 "Quest" ("Three character cards in a row under them ... the chips row for PUSH and TWICE when assigned"), §0.3 ("Every button a thumb uses is at least 48 px tall and wide as RENDERED"), §5 P1 step 11 (layout gate at 375x667, 320x568 and 412x915: "every button 48 px and on top")  
*The problem:* Three cards in a row at 320 CSS px with the fleet's 12 px page margin and 8 px gaps gives each card (320 − 24 − 16) / 3 = 93.3 px. Two chips at the 48 px minimum plus a 4 px gap need 100 px. They overflow by 6.7 px, so the layout gate goes red at 320x568 on exactly the screen P1 exists to build. At 375 the same arithmetic gives (375 − 24 − 16) / 3 = 111.7 px and the pair fits with 5.8 px a side, which is why this passes at the width the law is written at and fails at the width the gate also runs. Zero margins would give 106.7 px and leave the card no padding at all.  
*The ruling taken:* Adopt the pre roll strip (first finding) and the problem disappears — the chips sit on a full width strip. If the chips stay on the card, they stack vertically (two rows of 48 px, card height +52) and §6 says so, because the gate will find this on the first run.

**The Hall at Depth V is a five card choice with a footer the inherited .screen rule cannot pin**  
*Where:* RULES.md R9.4 and R11.2; HANDOFF-MARROWDEEP.md §6 "Hall"; §2 the `.screen` row (line 103); §9 pitfall "The eighth button under the fold"; spec pillar 6 (line 18) and §13  
*The problem:* Two problems in one screen. (a) Pillar 6 is "Never more than three cards to choose from", and the handoff's correction 8 exempts "lists you own" — but five Depth offer cards, each with its own rolled Sigils and boss, is not a list the player owns, it is the between quests DECISION, dealt and re-rolled by R9.4. Nothing in RULES or the plan addresses the tension. (b) The arithmetic: each offer card is Depth name, blurb, up to three Sigil marks with names, one or two boss names with stat glyphs, and a 56 px DEPLOY, so ≈150 px; five of them plus gaps is ≈790 px. At 320x568 the counters take ≈36 and the pinned footer (four 48 px buttons plus a Ward shelf row of up to six cards, R8.1) takes ≈140, leaving a 390 px band for 790 px of offers. The pitfall already rules "the offers scroll, the buttons are pinned" — but the inherited `.screen` is one `overflow-y:auto` flex column, which scrolls the footer with everything else. A pinned footer needs a two region layout that the architecture law does not describe, and the same question hangs over RESOLVE on the Quest screen ("at the bottom above the chip's corner").  
*The ruling taken:* Say it once in §4 VIEW: a `.screen` may carry one `.pin` footer outside the scroll region (`display:flex; flex-direction:column` with `.body{flex:1 1 auto; overflow-y:auto}` and `.pin{flex:none}`), and the layout gate asserts the footer's rect is on screen at 320x568 with the body scrolled to its end. For the pillar: show the offers as one card with < > paging, deepest first, so the deal is one card and the choice is a swipe, not a list.

**Party order is load bearing and the Deploy screen neither shows it nor lets the player set it**  
*Where:* RULES.md R13.5 (≈line 586: "Party order is the order the player marked DEPLOY"), R7.2, R4.15 Herald, R4.1 Hearthborn, R13.4; HANDOFF-MARROWDEEP.md §6 "Deploy" (line 432: "The roster as small cards with a tick; three ticks then GO (56 px). A card that cannot go says why.")  
*The problem:* Party order decides boss resolution order (R7.2 "Resolve in party order"), who Herald's "previous check" was (R4.15), Hearthborn's tie break (R4.1), which character is last at the boss for Bloodhound and every `lastOfStage` (R13.4), and every tie break in R13. The Deploy screen shows a tick. A tick has no number, nothing tells the player the order matters, and un-ticking to reorder is undefined. A player who builds a Herald cannot use her.  
*The ruling taken:* The tick renders the order number 1, 2, 3 in tap order; un-ticking renumbers the rest; one muted line on the Deploy screen from `lines.json` says the order is the acting order. Also add the short handed case R8.0 already rules on: GO enables at one or two ticks when the roster cannot fill three, and the offer card says short handed.

**The boss screen has no seat for an assigned character and the RESULT card has no damage line**  
*Where:* RULES.md R7.2, R11.7; HANDOFF-MARROWDEEP.md §6 "Boss" (Aspect card = "name, stat glyph, TN, hit point pips, the line") and §6 "Result"; §5 P3 (no layout gate)  
*The problem:* At the boss every character is assigned to an Aspect and doubling up is allowed, so an Aspect card needs up to three portrait seats — the Quest screen's challenge cards have seats, the Aspect cards are specified without any, so the boss screen cannot show who is on what. The RESULT card is specified as "the modifier chips, TOTAL 9 against TN 5, PASS or FAIL, the Strain line, the consequence line" with no damage line, but at the boss the damage IS the outcome: R7.2 pays `max(1, surplus) + weapon bonuses` and R5.8 says the stat consequences do not apply there, so the card as drawn shows a boss check as a bare pass with no effect. And the tightest screen in the game is never measured: four Aspect cards at Depth IV in two rows of two, plus the boss line, the party Strain bar, the round counter, three character cards and RESOLVE comes to ≈550 px of a 568 px viewport before any frame chrome, and P1's layout gate covers Depth I only while P3 (which builds the fourth Aspect) runs no layout gate at all.  
*The ruling taken:* Aspect cards carry up to three small seats, drawn like the Relay's. The RESULT card gains a damage line, shown only against an Aspect ("4 to Grasping Chains, 2 pips left"). Add to P3 step 5 or 7: `test/layout.mjs` runs the Depth IV boss with four Aspects at 320x568 from a fixture, and a shot of it is opened.

**Three states the rules require have no place in the flow the screens describe**  
*Where:* RULES.md R4.2 Ashwalker (≈line 163), R4.6 Lanternborn (≈line 167), R5.10 and R5.7 ("replacement offered if someone died"); HANDOFF-MARROWDEEP.md §6 (Hall, Deploy, Quest, Stage end) and §5 P1  
*The problem:* (a) Ashwalker is one of the four Origins unlocked at account start (spec §4) and R4.2 says "At quest start a Common relic of a random slot is rolled and offered on the drop screen" — so a drop screen must appear between GO and stage 1. §6's flow goes Hall, Deploy, GO, Quest stage 1 assign, and P1 builds drops only at stage end, so on a new account with a common Origin the free relic has nowhere to appear. (b) R4.6 gives a Lanternborn party a "lantern line under the stage title" showing the next stage's two stat glyphs; §6's Quest top row is "Depth and stage, the Sigil marks" with no lantern line. (c) R5.7 and R5.10 put the replacement offer inside stage end and the step enum has `replace`, but §6's Stage end sheet is "the bench line, the Respite line, the Renown line, then NEXT; the drop screens interleave before NEXT" with no replacement.  
*The ruling taken:* One line each in §6: the drop screen may run at quest start (Ashwalker) as well as at stage end; the Quest top row carries the lantern line when any deployed character is Lanternborn; the Stage end sheet ends with the replacement offer (RESERVE, RECRUIT at 25, CONTINUE SHORT HANDED) before NEXT when anyone died.

**Ordinary resume looks like a second tab, and the save gate's Renown law is false by design**  
*Where:* HANDOFF-MARROWDEEP.md §4 SAVE (lines 244 to 246) and §5 P2 step 9 (line 380: "Renown never goes backwards across the two tabs"); RULES.md R8.1  
*The problem:* Ownership is `sessionStorage.md_tab === quest.tab`. sessionStorage survives a reload but not a closed tab, so the normal way a phone player comes back to an in progress quest — close the PWA, reopen it tomorrow — produces a fresh tab id, a mismatch, and the card "This quest is open in another tab" with a TAKE OVER button, for a quest nobody else has open. Every ordinary resume costs a takeover. Separately, the gate's law is not true: R8.1 spends Renown in the Hall (Reforge 15, Commission 40, Recruit 25, Redeal 10, Mend 20, Excise 60, shelf 30), so Renown goes backwards constantly and correctly; a gate asserting it never does either fails on the first purchase or, if written to pass, pins the bug it was meant to catch.  
*The ruling taken:* Add `quest.beat` (a timestamp written on every save) and show the takeover card only when `now - beat < 60000` and the tab id differs; otherwise adopt silently and write the new tab id. Restate the gate as "`account.renownLifetime` never goes backwards across the two tabs", which is the invariant that actually holds (R2.2 reads it for the tier).

**Four screens and five sheets have no way out, and nothing handles the system back gesture**  
*Where:* HANDOFF-MARROWDEEP.md §6 (Creation, Deploy, Character, and the sheets named in §5 P2 steps 2 and 3) and RULES.md R11.3, R11.5; §2 head row (framed, "the back button posts `{ sws: 'close' }`")  
*The problem:* §6 gives BACK to exactly two screens: Roster ("Tap for Character. BACK.") and The Wall ("Nothing tappable. BACK."). Creation, Deploy and Character have none, and neither does any of the five sheets the plan names: SPEND RENOWN, SPEND MARROW, the gear tile sheet, MOVE TO's roster sheet, and the takeover card. A player who opens the Marrow sheet to look at prices is stuck in it. Nothing anywhere handles history: the game is one page of screens with no pushState, so on Android the system back gesture closes the PWA from every screen, and inside the portal frame the frame's own back posts `close` mid boss round with no word about whether that is legal (R5.11 forbids Withdraw, but closing the app is not withdrawing — it is a resume, and only the save decides that).  
*The ruling taken:* One line in §6: every screen but Title and Hall, and every sheet, carries a 48 px BACK or CLOSE in its top row (never in the bottom left 120 by 120); each pushes a history entry and popstate runs that screen's BACK; the Quest and Boss screens re-push so back is a no op inside a quest, and closing the app is a resume, never a withdraw. Creation's BACK is a cancel that spends nothing (the Recruit charge lands at KEEP).


### MINORS (3)

**Three card deals drawn as a row are unreadable at 320**  
*Where:* HANDOFF-MARROWDEEP.md §9 pitfalls ("`flex:none` on the shelf row and the Trait card row"), §6 "Creation" and "Aftermath"; RULES.md R2.5, R8.7  
*The problem:* The pitfall list treats the Trait deal as a horizontal row. Three cards in a row at 320 with a 12 px margin and 8 px gaps are 93 px wide, 77 px inside padding. At the 0.7 rem minimum (11.2 px, ≈5.6 px an average character) that is about 13 characters a line, so "Once per quest, survive at 0 Strain. Dies normally thereafter." (62 characters) wraps to five lines in a 77 px column. The Calling cards are worse: R8.7 puts a name, a blurb and a Legacy line ("Zealot. The line of Vessa Orn, who drowned at the Gate.") on one card, and this is the screen the spec calls the thrill of the game.  
*The ruling taken:* Every three card DEAL (Callings, Traits, the locked Origins, the Commission's three relics) is three full width stacked cards, scrolled if needed; only the Ward shelf and the character row are horizontal. Say it once in §6 and let the layout gate assert card width ≥ 260 at 320 on those screens.

**The save schema is missing five fields the rules now require, and the wall's merge is undefined**  
*Where:* HANDOFF-MARROWDEEP.md §4 SAVE (line 242); RULES.md R2.1 (`freeRolls`), R9.4 (the offers), R10.1 (the recently used ring), R11.9 (the Legacy card "once"), R8.8 (the wall's six fields)  
*The problem:* `{ v, seq, account (spec 14 plus rng and renownLifetime), roster, relics, quest, wall: [lines], sound, seen: { how } }` does not hold: `account.freeRolls` (R2.1), the per Depth offer state or its counter (R9.4), the pending drop queue outside a quest (the Commission finding), the shown flag or queue for R11.9's "the Hall shows the new wall line and the Legacy card once" — `seen` holds only `how` — and R10.1's newly added "small recently used ring in the save" of challenge lines from the last three quests. The wall is typed as `[lines]` (strings) while R8.8 wants name, Origin, Calling, quests survived, Depth and cause per entry, and the merge rule "every wall entry MAX merge" is not an operation on an array of strings: two tabs each burying a character produce two arrays and one of the deaths is dropped. `relics: {id: relic}` also has no lifetime rule, so every relic ever rolled and salvaged accumulates.  
*The ruling taken:* Extend the schema line: `account.freeRolls`, `account.attempts[depth]`, `save.pending`, `seen: { how, legacies: [id] }`, `recent: [lineId]` (a ring of the last three quests' lines); `wall: [{id, name, origin, calling, quests, depth, cause, t}]` merged by union on `id`, newest first by `t`; `relics` holds only worn, shelved and pending relics, and a salvaged id is deleted.

**HOW's place in the first run flow is unstated, and it can land in front of the game's best moment**  
*Where:* RULES.md R11.10 ("Six lines, no more, shown before the first quest and from the title"); HANDOFF-MARROWDEEP.md §5 P3 step 4 ("shown once before the first quest and from the title") and §6 "HOW" ("Six lines. GOT IT.")  
*The problem:* "Before the first quest" is true of four different moments: before BEGIN's three creations, between the creations and the Hall, at the Hall before DEPLOY, and after GO before stage 1. A builder reading it at the top of the flow puts six rules in front of a player who has not yet rolled a die, against the spec's first pillar ("Rolling a character is the thrill. Creation is dramatic and instant"). GOT IT's destination is also unstated when HOW is opened from the title versus from the flow.  
*The ruling taken:* Pin it: HOW shows once automatically at the Hall the first time the Hall opens, after the three creations and before any DEPLOY; GOT IT returns to whatever screen opened it; `seen.how` is written when GOT IT is tapped, never before.


---

## Lens: FLEET LAWS

**What it reported:** I read the spec (744 lines), RULES.md, HANDOFF-MARROWDEEP.md sections 0, 2, 4, 5, 9 and 10, the Fathom handoff sections 0, 2, 4, 5, 9, HANDOFF-FABLE-SEP06-EVENING sections 8 and 9, HANDOFF-SEP09 section 6, and the fifteen memory feedback files covering dashes, touch, click, localStorage, gates, probes, stamps, pins, mjs, service workers, thumbs, text size and particles. I then verified every path and line number in the section 2 inheritance table by opening the files, and ran the prototype engine and harness.

19 findings, 5 blockers. The three that matter most are measurements, not readings. First, the engine section 2 says to paste "verbatim" is red (`node plans/marrowdeep/proto/sim.mjs --test` prints `MD TEST FAILED: 6 of 288`) and predates at least eleven of RULES' own binding corrections: eight of the BALANCE keys section 4 names are absent from the object, `freeRolls`, the stray, the price index, REST_FRACTION, retire vesting, the proven-death rule and the deepest-Depth gate are all absent from the source, and four shipped numbers (Vault Renown, Vault relic rolls, the Depth IV drop row, the Feet and Ward affix lists) still hold the values the audit overturned. `--grid`, which the table says becomes `sim.js --balance`, does not exist. Second, Depth IV and V are unwinnable: four Aspects totalling 21 hit points against three bodies at Strike 3 and Toughness 4 needs about seven rounds and kills everyone in two, and driving the plan's own policy for 200 quests per Depth gave 0 wins and 600 of 600 characters dead at both, with no gate in the plan asking whether a quest can be won. Third, BALANCE is called tuned and is not: 500 Depth I quests measured 69.0 percent per-character death, 80.4 percent at-least-one-death and 59.2 percent full wipe against the spec's 12 to 15, 35 and 8, missing all three of the plan's own P3 gate bands by 3.5x, 2.3x and 7.4x, and `proto/PROTO-REPORT.md`, cited six times as the evidence, does not exist.

The other two blockers are internal contradictions in RULES: R9.1 restates the Depth V rarity sentence R6.1(b) explicitly overturned, and the P3 `--depths` gate is written to enforce the overturned reading; and R6.2's new Sigil Ward "second named Sigil" affixes have no key, no word list, and are forbidden by R6.2's own no-repeat rule, so a Sigil Ward can never reach Relic rarity at any Depth.

On the copy law: I found zero violations. Every string in all eleven `data/*.json` files is free of hyphen-minus, en dash, em dash and exclamation point (grep returns nothing, and there is no non-ASCII at all); the R0 renames landed (Surehanded, Pressgang, Sigil Ward); the brand appears once, correctly, as "Sky Wolf Studio". The two copy findings are structural rather than textual: the lint's copy scan reads authored strings only and never sees a composed one (relic names, wall lines and the `{name}` card templates are assembled at runtime), and one HOW line states "three Aspects", which becomes false at Depth IV.

Both plan files changed on disk while I was reading them; my quotes and line numbers are from RULES.md md5 d031a28c and HANDOFF-MARROWDEEP.md md5 639c0283.


### BLOCKERS (5)

**The engine the plan says to paste verbatim is red and predates at least eleven of RULES' own binding corrections**  
*Where:* /workspaces/lucid-winds/plans/marrowdeep/HANDOFF-MARROWDEEP.md lines 8 to 10, 83 and 95 (section 2 inheritance table, row 1) against /workspaces/lucid-winds/plans/marrowdeep/proto/engine.js  
*The problem:* Section 2 row 1: "The whole engine goes between the SIM markers as BALANCE through SIM, verbatim except the UMD wrapper", and line 8: "the prototype engine and balance harness, already built and verified (three verifiers, findings applied)". Both are false today.

(a) `node plans/marrowdeep/proto/sim.mjs --test` prints `MD TEST FAILED: 6 of 288`, not MD TEST OK. The six: R5.6 relay plus relay assignment (got 2 want 3), R5.6 nobody benches (got "a" want null), R5.8 Blindness lasts one stage only, R5.7 Respite reaches a character who acted, R6.2 never repeat an affix key (repeat toughness on chest), R7.6 the first boss pays eight (got 12 want 8). Note the fifth: that assertion pins the rule R6.2(b) explicitly overturned ("toughness and armor may each appear twice on one item"), and section 2 says "Every assertion in --test is kept and grows", so the builder is told to carry an assertion that contradicts the rule book.

(b) The BALANCE object I read out of the engine is missing eight of the keys section 4 says it contains "verbatim, with the tuned defaults": RETIRE_VESTING, FILLER_MAX, PRICE_INDEX, REST_FRACTION, SLOT_WEIGHTS, SAVE_KEY, SAVE_V, GAME_ID are all ABSENT.

(c) Grepped in engine.js: `freeRolls` 0 hits (R2.1, the three free characters), `STRAY` 0 (R8.0, which RULES itself calls "a blocker: without it the game soft locks"), `REST_FRACTION` 0 (R8.3), `PRICE` 0 (R8.0b), `vesting` 0 (R2.6), `proven` 0 (R8.5), `deepest` 0 (R8.4).

(d) Numbers that disagree with corrected rules: `RENOWN.vault = 5` against R5.9's corrected Vault 8 (RULES.md:245, with its own arithmetic); `RELIC_ROLLS.vault = 1` against R5.9's "Vault 2, the first at +1 rarity tier"; `DROP_WEIGHTS[3] = [10,45,34,11]` against R6.1(a)'s corrected `0/55/34/11` (RULES.md:261, the exact 10-percent-Common-with-a-0-point-budget bug the correction was written for); Feet has no `benchAlly` and sigilWard has no second-sigil affixes, both required by R6.2 (RULES.md:289 to 292).

(e) Section 2 says "sim.mjs's --table, --test and --grid become sim.js's --table, --test and --balance". `grep -c grid plans/marrowdeep/proto/sim.mjs` returns 0; the runner at sim.mjs:1147 offers `--table | --test` and nothing else.

P0 step 0 has an escape hatch ("if either is red ... write the engine yourself from RULES.md"), but P0 is budgeted at 1.5 hours for a paste, and section 1 tells the builder the engine is verified, so the escape hatch is the path that will actually be taken and the day's sizing is wrong by most of a day.  
*The ruling taken:* Before the builder starts, either (i) bring proto/engine.js up to RULES (the six red assertions, the eight missing BALANCE keys, the four corrected numbers, R8.0 the stray, R2.1 freeRolls) and make `--test` print MD TEST OK, or (ii) rewrite section 2 row 1, line 8 and line 83 to say plainly that the engine is a red head start that implements RULES up to and NOT including the Sep 08 audit, list the eleven corrections it is missing by rule id, and rebudget P0 accordingly. Delete `--grid` from the row. Add to P0 a first gate that fails: an assertion per audit correction, cited by rule id, red on the pasted engine and green only when it is implemented.

**Depth IV and V bosses are unwinnable as specified: 0 wins in 200 quests, 600 of 600 characters dead, and no gate in the plan looks for it**  
*Where:* /workspaces/lucid-winds/plans/marrowdeep/RULES.md R7.1 (line 351), R7.2 (354), R7.4 (361), R9.1; gate at /workspaces/lucid-winds/plans/marrowdeep/HANDOFF-MARROWDEEP.md line 472  
*The problem:* R7.1 gives every boss a fourth Aspect "used at Depth IV and V only" with hit points "+2 at IV and V". R7.2 keeps the party at three. R7.4 sets STRIKE 3 at IV and V and STRIKE_TARGET `attackers`, where "an Aspect nobody faced strikes every living character".

Arithmetic, taken from the plan's own data. `GEN.newQuest('trace4', 4)` builds: grace/TN5/hp5, wits/TN4/hp5, nerve/TN5/hp6, might/TN5/hp5, total 21 hit points. Damage is `max(1, surplus)`, so a d8 against TN 5 (50 percent, mean surplus about 1.5) yields about 1 damage per character-round; three characters clear about 3 a round, so 21 hit points is about 7 rounds. Strain per character per round is at least 3 (their own unbroken Aspect) plus 3 more if any Aspect went unfaced, which is forced when 4 Aspects meet 3 bodies. Base Toughness is 4. A character is dead after the second round at the latest, and 7 are needed. There is no bench, no Respite and no Armor refill at the boss (R7.4).

Measured, driving proto/engine.js with its own `SIM.policy.playQuest`, 200 quests per Depth, fresh tier 1 rosters: Depth I 79/200 won, II 54/200, III 9/200, IV 0/200 with 600 of 600 characters dead, V 0/200 with 600 of 600 dead. Not one Depth IV or V quest was survivable by anybody.

This is the same argument R7.4 itself used to reject STRIKE_TARGET `all` ("2 x 2 = 4 = base Toughness, before Armor"); at Depth IV the unfaced Aspect makes `attackers` behave as `all` plus the character's own Aspect, at strike 3 instead of 2.

The P3 `--depths` gate (line 472) asserts only structure: two slots per stage, four Aspects on four stats, sealed stages, no replacement, drop rarity. It never asks whether a quest can be won, so this ships green.  
*The ruling taken:* Make the fourth Aspect answerable by three bodies and gate on winnability. Concretely: the fourth Aspect is DORMANT until one of the first three breaks (one line in bossRound), or an unfaced Aspect strikes ONE character (the most strained, party order breaking ties) rather than everyone. Then add to `--depths` a law, not a count: at every Depth II to V, over 200 quests with the policy from a roster geared at that Depth, the win rate is above 15 percent and below 85 percent, and no Depth wipes more than 40 percent of parties. Watch it fail by restoring the fourth Aspect to always-on.

**BALANCE is called tuned and is not: Depth I misses all three of the spec's death targets and all three of the plan's own gate bands, and PROTO-REPORT.md does not exist**  
*Where:* /workspaces/lucid-winds/plans/marrowdeep/HANDOFF-MARROWDEEP.md lines 10, 25, 83, 149, 697 and P3 step 5; /workspaces/lucid-winds/plans/marrowdeep/RULES.md line 6  
*The problem:* Line 10: "with BALANCE tuned against the spec's section 8.6 and 15 targets. `proto/PROTO-REPORT.md` is the evidence." Line 83: "the balance harness already hits the spec's targets". RULES.md line 6: "the evidence is in `PROTO-REPORT.md`". `ls plans/marrowdeep/proto/PROTO-REPORT.md` returns No such file or directory. Only engine.js and sim.mjs are in that folder, and sim.mjs has no grid or balance mode at all, so no tuning pass was ever run.

Measured instead, 500 Depth I quests through proto/engine.js with its own policy, fresh tier 1 rosters, no gear:
  per character death 69.0 percent (spec 8.6 target 12 to 15; plan P3 band 8 to 20)
  at least one death   80.4 percent (target about 35; band 25 to 50)
  full wipe            59.2 percent (target about 8; band 3 to 15)
  won                  40.8 percent
Every one of the three P3 assertions is red on the untouched defaults, by factors of 3.5x, 2.3x and 7.4x. P3 step 5 tells the builder to "watch it fail with `--over=BASE_TOUGHNESS=1`", which assumes the default passes; it does not, and the builder will spend the phase discovering that.

Director call 1 (line 697) also defers to a file that does not exist: "the prototype's grid confirms or moves it in PROTO-REPORT.md".  
*The ruling taken:* Either run the grid before the build starts and write PROTO-REPORT.md with the measured rows, or strike every claim that BALANCE is tuned and every reference to PROTO-REPORT.md, and move the tuning into P0 as its own phase with the `--balance` sweep as its exit gate. Give the builder the numbers above as the starting position so the phase begins from a known red rather than a claimed green. State in section 12 that a tuning pass is a phase, not a paste.

**RULES contradicts itself on Depth V rarity, and the plan builds a gate that enforces the reading RULES overturned**  
*Where:* /workspaces/lucid-winds/plans/marrowdeep/RULES.md line 494 (R9.1) against line 266 (R6.1b); gate at /workspaces/lucid-winds/plans/marrowdeep/HANDOFF-MARROWDEEP.md line 472  
*The problem:* R6.1(b) at RULES.md:266 is explicit: "Depth V uses its own 11.3 and 11.4 rows (0 / 45 / 40 / 15, budgets 4 / 7 / 10). The section 9 prose line 'Relics drop at Relic rarity only' is the error: two tables beat one line", with the reasoning that the prose reading "exhausts the twenty uniques in four quests and makes the Vault and boss '+1 tier' a no op".

R9.1 at RULES.md:494 then ends: "V: IV's shape, no replacement mid quest, drops at Relic rarity only." That is the error sentence, restated as the rule.

P3 step 5's `--depths` gate at HANDOFF:472 asserts "Depth V drops are all Relic rarity", which is the overturned reading. So the generator (built from R6.1b) and the gate (built from R9.1) cannot both be right, and whichever the builder writes first makes the other red. Director call 7c at line 719 says it was "Built on the tables", which makes the gate the wrong one.  
*The ruling taken:* Fix R9.1's last clause to read "drops at the Depth V table rows, 0 / 45 / 40 / 15 (R6.1b)" and change the `--depths` assertion to "no Depth V drop is Common, and the Relic share over 2000 drops is between 10 and 20 percent". Keep the Relic-only reading alive as a single BALANCE row named in call 7c, not as a rule sentence and a gate.

**The Sigil Ward's second-sigil affixes cannot be written: no key exists, no word list exists, and R6.2's own no-repeat rule forbids the alternative**  
*Where:* /workspaces/lucid-winds/plans/marrowdeep/RULES.md lines 272 and 289 to 292 (R6.2); /workspaces/lucid-winds/plans/marrowdeep/data/relic-words.json; proto/engine.js AFFIXES  
*The problem:* R6.2 line 272: "Draw affixes valid for the slot, without repeating a key on one item (a stat targeted key may repeat with a different stat)". Line 289: "Sigil Ward gains 'immunity to a second named Sigil' 3 and 'partial relief from a second named Sigil' 2". Line 292: "Legal maxima are then Ward 10".

Ward 10 requires 3 + 3 + 2 + 2, that is two immunity lines and two partial lines on one item. If those are the SAME keys repeated, R6.2's own first sentence forbids it and R6.2(b) permits repeats only for `toughness` and `armor`; a sigil is not a stat, so the "different stat" escape does not apply and R13.11 extends the exception only to floors. If they are DISTINCT keys, they are absent from `data/relic-words.json`, whose affix map holds exactly 25 keys and only `sigilImmune` and `sigilPartial` among them, so R10.6's own gate ("`sim.js --data` generates a thousand names and fails on any that contains 'undefined'") fires on the first Ward that draws one. The engine's AFFIXES table also carries only those two for sigilWard.

Consequence with the shipped budgets (Depth V rarity budgets 0 / 4 / 7 / 10): a Ward's reachable maximum is sigilImmune 3 + sigilPartial 2 + FILLER_MAX 2 = 7, so rule R6.2(c) fires on every Depth V Relic Ward and downgrades it to Rare. A Ward can never be Relic rarity, never carries a unique, and "+1 rarity tier" on a Vault or boss Ward drop is a no op, which is the exact failure R6.1(b) argued against for the whole Depth. The Ward Shelf is the spec's stash pillar (spec 11.1: "Sigil-Ward is the stash slot"), so its top tier being unreachable is not cosmetic.  
*The ruling taken:* Decide it in one sentence in R6.2 and mirror it in the data: make the sigil-targeted keys repeatable per SIGIL, exactly as R13.11 already does for floors per stat, worded "a sigil targeted key counts as one key per named Sigil, and immunity and partial never name the same Sigil". Then delete the two invented second-sigil affixes, so `relic-words.json` stays at 25 keys and needs no new words. Add a `--data` assertion: a thousand Depth V Sigil Wards generated, every one hits budget 10 exactly, every one is labelled Relic, no two lines name the same Sigil, and no name contains "undefined". Watch it fail by setting the repeat rule back to per key.


### MAJORS (8)

**The browser harness's readiness wait is a render-loop frame counter, and Marrowdeep has no render loop**  
*Where:* /workspaces/lucid-winds/plans/marrowdeep/HANDOFF-MARROWDEEP.md line 106 (section 2, Browser harness row) and section 4 MD_DEV; /workspaces/lucid-winds/satellites/fathom/test/harness.mjs line 58  
*The problem:* Section 2 says of harness.mjs: "Change the ready wait to `window.MD_DEV && MD_DEV.frames() > 2`". In Fathom that works because `G.frames++` sits inside the requestAnimationFrame render loop at satellites/fathom/index.html:2918, and harness.mjs:58 blocks on it for 30 s. Section 4 of this plan says Marrowdeep's VIEW is "DOM, not canvas, for everything but the dice ... tumbled by CSS (rotate and scale)". A CSS animation drives no JS counter, so a DOM card game has no natural frame tick: `MD_DEV.frames()` either never advances (every browser gate times out at 30 s) or is faked on a setInterval, which advances happily on a blank page and is a probe that cannot fail. Fathom's `nextFrame`/`waitFrames` helpers at harness.mjs:197 to 205, which tap and layout both use, have the same dependency.

The memory scar is the same shape: never prove a page is alive with rAF, because absence of a painted frame is not evidence of anything. Section 9 already has the right instinct ("waitForFunction on `MD_DEV.screen()` or `MD_DEV.card()`, never `sleep`") and section 2 contradicts it.  
*The ruling taken:* Delete `frames` from the MD_DEV surface in section 4 and from the harness row. Make readiness POSITIVE and DOM shaped: BOOT sets `MD_DEV.ready = true` only after the title screen's own nodes are in the document and one of them has a non zero rect, and `open()` waits on `window.MD_DEV && MD_DEV.ready && MD_DEV.screen() === 'title'`. Replace `nextFrame` with `waitForFunction` on `MD_DEV.screen()` and `MD_DEV.card()`. Watch it fail by returning the title screen's markup without appending it.

**The two-tab heartbeat guards the quest but not the Hall, which is where the money is**  
*Where:* /workspaces/lucid-winds/plans/marrowdeep/HANDOFF-MARROWDEEP.md section 4 SAVE (lines 278 to 288) and P2 step 9 (lines 430 to 438)  
*The problem:* The SAVE law reads: "`account.renownLifetime`, `questsCompleted`, and every wall entry MAX merge; the in progress `quest` carries `tab` and `beat` ... another tab shows 'This quest is open in another tab' with a TAKE OVER button". The ownership token covers `quest` only. `renown`, `marrow`, `roster` and `relics` fall through to whichever tab wrote last.

Worked case, both tabs in the Hall, no quest open so no takeover card appears. Renown 60. Tab A buys a Commission (40) and writes renown 20 with a new relic in `relics`. Tab B is holding a stale 60, buys Recruit (25) and writes renown 35 with a new character in `roster`. Tab B wrote last, so the account ends at 35 Renown holding both purchases: 65 Renown of goods for 25. `renownLifetime` never moved (it is income only), `questsCompleted` never moved, no wall entry changed, so every merge rule in the law is satisfied and every assertion in the P2 save gate passes. The gate at line 437 asserts only "`account.renownLifetime` never goes backwards", and correctly notes Renown itself goes backwards; the failure here is Renown going FORWARDS in value, which nothing looks at.  
*The ruling taken:* Extend the heartbeat token from `quest` to the ACCOUNT: a tab whose id is not the holder and whose `beat` is inside the window is read only in the Hall (every SPEND button greyed with one line, TAKE OVER offered), exactly as it already is for a quest. Then add to test/save.mjs: two tabs both at 60 Renown, tab A commissions, tab B recruits, assert exactly one purchase landed and the balance equals 60 minus that purchase's price. Watch it fail by removing the ownership check from `hall.*`.

**The seam gate runs one seed, so most of the game's own shapes may never be exercised and the gate can be green by luck**  
*Where:* /workspaces/lucid-winds/plans/marrowdeep/HANDOFF-MARROWDEEP.md line 386 (P1 step 10, THE SEAM GATE)  
*The problem:* "`MD_DEV.seed(20260908)` on the title; BEGIN; then three times ROLL ... for every stage asks `MD_DEV.policy.assign(state)`". One seed plays one quest. Per R5.4 a Depth I quest is Gate plus (Open or Toll) twice, Gate plus one of Chain/Relay/Vault twice, then two of Gate/Chain/Relay: a single seed can easily contain no Vault, no Toll, no Relay, no Ambush, no NERVE contagion, no hidden TN, no death and no boss retarget. Everything the seam gate proves is proved about the shapes that seed happened to deal. This is the Gerplunk scar exactly (a threshold read off one instance of a thing that comes in variants; a gate green the day the bank dealt a warm stone, red the next day with no commit between), and it will bite the day a BALANCE weight moves and the seed deals a different quest, with the gate reporting a seam failure that is really a coverage change.  
*The ruling taken:* Make the seam gate a SET of seeds with a coverage assertion. Pick the smallest set of seeds that together deal all six shapes, both hidden-TN sources (a WITS failure and Blindfold), an Ambush consumed, a NERVE contagion that lands on a third party, a mid-stage death and a boss retarget; the gate collects the shape and event names it actually saw from `state.events` and FAILS naming what it did not see. Write the assertion as the LAW ("every shape in SHAPES was resolved at least once"), never as the seed list. Watch it fail by dropping one seed from the set.

**Every layout gate and every shot boots a fresh account, and the screens that overflow are the late-game ones**  
*Where:* /workspaces/lucid-winds/plans/marrowdeep/HANDOFF-MARROWDEEP.md P1 step 11, P2 step 10, P3 step 7  
*The problem:* P1 step 11 reaches its states "by real taps from a seeded save written by `MD_DEV.fixture` and a reload", but every path described starts at BEGIN with three fresh rolls; P3 step 7's shot list is title, creation, quest, result, boss, Hall, wall. The screens that will actually break are the ones an account owns after twenty quests, and none of them is named: the Hall with FIVE Depth offer cards plus four buttons plus the Ward Shelf row at 320x568 (section 9 already predicts this as "the eighth button under the fold" but only gates the fresh Hall), the Roster at eight cards, the Character screen carrying four Traits plus eight slot tiles plus Scars plus RETIRE plus EXCISE, a Ward Shelf at its six-slot maximum, and a wall two hundred lines deep. The studio scar is precise about this: a fresh headless profile is one player, the one who has never changed a setting, and checking only that player is how Shut the Box shipped cut in half.  
*The ruling taken:* Author one LATE fixture next to the fresh one, written by `MD_DEV.fixture` and reached by RELOAD, not by setting state on a live page: 8 roster characters, one with 4 Traits, 3 Scars and 8 relics worn, 5 Depths unlocked with offers showing, 6 Ward Shelf slots filled, a 200 line wall, Renown and Marrow both five figures so the counters are at their widest. Run the whole of P2 step 10's layout suite against BOTH fixtures at all three widths, and shoot the Hall, Roster, Character and Ward Shelf from the late fixture as well. Watch it fail by adding a fifth offer card to the fresh Hall.

**The copy scan reads authored strings only and never sees a composed one, which is where a dash actually enters**  
*Where:* /workspaces/lucid-winds/plans/marrowdeep/HANDOFF-MARROWDEEP.md line 105 (Lint row); /workspaces/lucid-winds/plans/marrowdeep/RULES.md R6.5 and R8.8; /workspaces/lucid-winds/plans/marrowdeep/data/lines.json  
*The problem:* The lint row says the copy scan "runs over every string in the DATA block (all the player copy lives there, not in the body)". It does not all live there. Marrowdeep composes player sentences at runtime: R6.5 builds `[Prefix] [Base] of [Suffix]` from three word lists, R8.8 writes the wall line from name plus Origin plus Calling plus quests plus Depth plus cause, and lines.json `cards` are templates ("{name} is interred among the drowned.", "The line of {name}, {calling} to the last breath.", "What the dead carried comes back up as {renown} Renown."). A source grep over DATA cannot see the sentence a player reads. R10.6's existing generated-name gate looks only for the literal "undefined"; a dash or a bang introduced in a VIEW template literal passes every check the plan has.

The dash scar also warns that a plain hyphen-minus range is invisible to an em or en dash scan, and the plan's P0 step 4 fail-watch is only "plant a dash in one Gate line" without saying which character.  
*The ruling taken:* Add to `sim.js --data`: generate 1,000 relic names, 1,000 character names, 200 wall lines, and every card template rendered with a real record, and fail on any hyphen-minus, en dash, em dash, non breaking hyphen, minus sign or exclamation point in any of them. Watch it fail three ways: plant U+002D in a relic base word, plant U+2014 in a wall line template, and plant a bang in a card line. Change the P0 step 4 fail-watch to name the character it plants.

**Aspect hit points are called toughness in RULES and hp in the data and the engine, and the failure mode is a boss that can never be broken**  
*Where:* /workspaces/lucid-winds/plans/marrowdeep/RULES.md line 351 (R7.1); /workspaces/lucid-winds/plans/marrowdeep/data/bosses.json; /workspaces/lucid-winds/assets/MARROWDEEP_DESIGN_SPEC.md line 654  
*The problem:* R7.1: "An Aspect has a name, a locked stat, a TN, and hit points (`toughness`)." The spec's own schema at line 654 agrees: `{ name: "Grasping Chains", stat: "might", tn: 5, toughness: 3 }`. But every aspect in data/bosses.json uses `hp` ("stat": "might", "tn": 5, "hp": 3), and proto/engine.js reads `src.hp` at line 927 and stores `hp` at line 929.

This only bites the path the builder is most likely to take. Given the prototype's `--test` is red, P0 step 0 sends them to "write the engine yourself from RULES.md", where the field is named `toughness`. Reading `aspect.toughness` from bosses.json returns undefined; `undefined - damage` is NaN; `NaN <= 0` is false; the Aspect never breaks and the boss is unkillable. Nothing catches it: `sim.js --data` is specified to refuse "a boss without four Aspects on four stats" and says nothing about the hit point field, and the P3 `--depths` gate asserts structure, not that a boss can fall.  
*The ruling taken:* Pick one name and make RULES, the spec note, the data and the engine agree; `hp` is the one already in the shipped data, so change R7.1 to "hit points (`hp`)" and note the spec's `toughness` is the dead name. Add to `--data`: every aspect in every boss has an integer `hp` above 0. Add to `--test`: a boss whose three Aspects take exactly their hit points in damage is BROKEN and the quest reports won. Watch that one fail by renaming the field in one boss.

**The assertion floor is stated three different ways and all three are below the prototype's actual count, so 168 assertions can vanish silently**  
*Where:* /workspaces/lucid-winds/plans/marrowdeep/HANDOFF-MARROWDEEP.md line 98 (section 2), section 4 TEST, and line 429 (P2 step 8)  
*The problem:* Section 2 line 98: "the assertion floor (start it at 60, the proto's count, and raise it every phase)". 60 is Fathom's floor (`var ASSERTION_FLOOR = 60` in satellites/fathom/sim.js); the proto's count is 288 (`MD TEST FAILED: 6 of 288`). Section 4 says "The floor starts at the proto's assertion count", which is 288 and contradicts line 98. P2 step 8 line 429 then says "Floor raised to 120", which is 168 BELOW the count the plan starts from, so it is a lowering dressed as a raise. The floor exists to stop assertions quietly disappearing; at 60 or 120 it would not notice most of the suite being deleted.  
*The ruling taken:* Set one number in one place: `ASSERTION_FLOOR = 288` in section 4, delete "start it at 60, the proto's count" from line 98, and change P2 step 8 to "floor raised to 360" and P3 to a number above that. Watch it fail by commenting out one describe block and seeing exit code 3.

**Text size is checked by a CSS grep only, in a game whose entire content is DOM text**  
*Where:* /workspaces/lucid-winds/plans/marrowdeep/HANDOFF-MARROWDEEP.md line 105 (Lint row) against /workspaces/lucid-winds/satellites/fathom/tools/lint.mjs lines 101 to 142  
*The problem:* The lint row inherits Fathom's "no font under 0.7 rem", which at lint.mjs:101 is `[...HTML.matchAll(/font-size:\s*([0-9.]+)rem/g)]`, a grep of rem literals in the source. Fathom can afford that because its type is drawn on a canvas and rule 8b (lint.mjs:105 to 142) separately asserts "no canvas font under 11.2 px". Marrowdeep has no canvas text, so it inherits the weak half and loses the measured half. Anything declared in px or em, inherited from a shrunken parent, or scaled by a transform on a card is invisible to that regex, which is the same class of lie as the 48 px scar: a CSS number is not a rendered number. The RESULT card is the densest text in the game ("every modifier as a chip with its source") and it is the screen most likely to be shrunk to fit 320x568.  
*The ruling taken:* Keep the rem grep and add a RENDERED text floor to test/layout.mjs: at 375x667 and 320x568, on every screen reached by real taps and on both fixtures, walk every element with a non empty text node and assert `parseFloat(getComputedStyle(el).fontSize) >= 11.2` (0.7 rem at a 16 px root), reporting the offenders by id. Watch it fail by setting the RESULT card's modifier chips to 10 px.


### MINORS (6)

**--over=KEY=VAL is a numeric-literal substitution, so the plan's own Director calls 2 and 3 cannot be swept with it**  
*Where:* /workspaces/lucid-winds/plans/marrowdeep/HANDOFF-MARROWDEEP.md section 4 (BALANCE and P3 step 5) against /workspaces/lucid-winds/satellites/fathom/sim.js lines 44 to 62  
*The problem:* Section 4: "`sim.js --over=KEY=VAL` runs any sweep against an override without editing the game." The inherited implementation builds `new RegExp('(\\b' + k + '\\s*:\\s*)(-?[0-9]*\\.?[0-9]+)')` and `parseOver` does `parseFloat(kv[1])`, so it can only replace a bare number and throws "override key not found" on anything else. Marrowdeep's BALANCE, as read out of proto/engine.js, has `STRIKE_TARGET: "attackers"` (a string, Director call 2), `RESPITE: [1,1,0,0,0]` and `STRIKE: [2,2,2,3,3]` (arrays, Director call 3 and R7.4's "the prototype's grid may move BALANCE.STRIKE only"), plus SLOT_WEIGHTS, DROP_WEIGHTS, BUDGETS and TIER_WEIGHTS as tables. None of them can be swept. Only scalars like BASE_TOUGHNESS work, which is why the one fail-watch the plan names happens to be `--over=BASE_TOUGHNESS=1`. Section 4 also lists `RESPITE 1` as a scalar while the object holds an array.  
*The ruling taken:* Extend `parseOver` to accept quoted strings and `KEY[i]=VAL` index syntax, and extend `build` to substitute a string literal and an array element, keeping the throw-on-unknown-key behaviour; say in section 4 which BALANCE entries are scalars, which are arrays by Depth and which are strings. Watch it fail with `--over=STRIKE_TARGET='all'` and with `--over=RESPITE[0]=0`, and confirm the resulting run differs.

**The PUSH and TWICE chips sit on the card the thumb just pressed, and no shot in the plan puts a thumb on it**  
*Where:* /workspaces/lucid-winds/plans/marrowdeep/HANDOFF-MARROWDEEP.md section 4 INPUT, P1 step 3, P3 step 7  
*The problem:* Section 4: "Chips (PUSH, TWICE) are 48 px toggles on the assigned character's card." P1 step 3: "PUSH and TWICE chips appear on an assigned card when available." The gesture that assigns a character is a tap on that same card, so the chips appear under the pad of the thumb that just landed there, along with that card's Strain pips. The Gerplunk scar is exactly this: the spin ring passed every look pass because the shot had no thumb in it, and on glass it was 8 to 13 mm under a 12 to 16 mm pad. The plan's shot list (P3 step 7) is title, creation, quest, result mid surge, boss mid strike, Hall, wall, with no thumb composited anywhere.  
*The ruling taken:* Add to tools/shots.mjs a `--thumb` mode that composites a 90 px disc (a thumb pad at 412 wide) at the last tapped point, and shoot the Quest screen with a character freshly assigned. If the chips or the Strain pips fall under the disc, move the chips above the card or onto the RESOLVE row. Add to the layout gate: the PUSH and TWICE chip rects do not intersect a 90 px disc centred on the card's own centre.

**Two layout laws that were in the predecessor plan were dropped from section 9**  
*Where:* /workspaces/lucid-winds/plans/marrowdeep/HANDOFF-MARROWDEEP.md section 9 against /workspaces/lucid-winds/plans/fathom/HANDOFF-FATHOM.md section 9  
*The problem:* Fathom's section 9 carries "Measure `visualViewport`, never `innerHeight`"; Marrowdeep's section 9 does not, while inheriting satellites/fathom/test/layout.mjs, whose 120x120 seat box is built from `window.innerHeight` (harmless headless, wrong on a phone with a URL bar). The companion law is also missing: `innerWidth` follows the LAYOUT viewport, so mobile Chrome widens it to hold wide content and a `scrollWidth` vs `innerWidth` comparison always reads clean; that is how Reversi shipped 31 px too wide on every phone. P1 step 11 asks for "the two challenge cards and three character cards all fully inside the viewport with nothing scrolled off at 320x568", which is precisely the assertion that silently passes if it is written against innerWidth.  
*The ruling taken:* Put both back in section 9 as one line, and make the layout gate compare every overflow assertion against the DEVICE WIDTH the gate set (320, 375, 412), never innerWidth or scrollWidth, and read the seat box from `visualViewport.height` where it exists. Watch it fail by widening one challenge card to 340 px at the 320 width.

**The ear gate's high frequency ceiling was loosened from 0.10 to 0.35 with no reason recorded**  
*Where:* /workspaces/lucid-winds/plans/marrowdeep/HANDOFF-MARROWDEEP.md line 474 (P3 step 6) against /workspaces/lucid-winds/satellites/fathom/test/audio.mjs line 75  
*The problem:* The inherited gate asserts `m.highFraction < 0.10` and its header says why: that band is where an alarm lives, and the scar behind it is Gerplunk's tremolo reading as a fire alarm. P3 step 6 sets the same assertion at "share above 3 kHz under 0.35" and gives no reason. Marrowdeep's voice list is a wood knock, a rising chime, a chain snap noise burst, a coin tick and a two hit heartbeat, which is a bright palette, so a move may well be right; but 0.35 is more than a third of the energy above 3 kHz and is close to no ceiling at all. A band chosen before the mix exists is a number, not a law.  
*The ruling taken:* Build the voices, run `MD_DEV.renderAudio(60)`, write the measured `highFraction` into P3 step 6 and set the ceiling at the measured value plus a stated margin, with one line saying what was measured and when (the form Fathom's audio.mjs already uses: "Measured rms 0.0449. The band is 0.010 to 0.12"). Watch it fail by setting the chain snap's noise burst gain to 1.0.

**VIEW plans particle jitter with no information job**  
*Where:* /workspaces/lucid-winds/plans/marrowdeep/HANDOFF-MARROWDEEP.md line 199 (section 4, RNG)  
*The problem:* "VIEW's cosmetic randomness (tumble faces, particle jitter) uses its OWN stream". The stream split is right; the particles are not argued for. The studio ruling is that decoration is not neutral, it is cover for the wrong side, and the narrow exception is a particle that tells one side something true (dust from a running player is information; ambient drift is noise). Marrowdeep's entire read is which number beat which number on a card of chips and pips, so anything drifting across the RESULT card competes with the one thing the player is doing. The tumble faces earn their place (a roll is happening); the jitter is not described as telling the player anything.  
*The ruling taken:* In section 4 or section 6, list every moving element with the one true thing it tells the player (tumble: a roll is under way; the surge die sliding in: a second die was added; the pip filling: Strain landed; the Aspect card cracking: it broke), and state that anything not on that list is cut. Delete "particle jitter" or name what it says. The shot pass names any motion it cannot justify as one of its three faults.

**A HOW line states a rule that becomes false at Depth IV, and HOW is reachable from the title forever**  
*Where:* /workspaces/lucid-winds/plans/marrowdeep/data/lines.json, `how[4]`; RULES.md R11.10 and R7.1  
*The problem:* The line reads "The boss shows three Aspects, and every round each one still standing strikes." R7.1 gives every boss a fourth Aspect at Depth IV and V, and R11.10 says HOW is "shown before the first quest and from the title", so a player who reaches Depth IV can open HOW and read a false rule. The line is otherwise clean of dashes and bangs, as is every other string in the three files I scanned.  
*The ruling taken:* Replace with "The boss shows its Aspects, and every round each one still standing strikes." Same length band, no dash, no bang, true at every Depth.


---

## The refuters

Every blocker and major from the second pass was given to a skeptic told to refute it.

- **REFUTED:** Refuted: RULES R6.2 already decides every part of this, in a block that reaches the same conclusions by the same arithmetic. The finding reproduces the CORRECTED paragraph at /workspaces/lucid-winds/plans/marrowdeep/RULES.md lines 277-295 nearly verbatim, including its diagnosis ("'Lines, plural, of one key' contradicts the no repeat rule in the same sentence"), its worked case ("a Depth V Relic Ward would have carried FIVE points of Toughness"), its stakes ("a character in Ward, Feet and Chest would have worn +13 Toughness over a base of 4, which deletes spec 6.2's Scar treadmill, spec 6.3's retire or run decision and most of the Marrow supply"), and its remedies. Proposal (1) is R6.2(a) plus (c): "The filler is ONE toughness line whose value is the whole remainder, capped at BALANCE.FILLER_MAX = 2" and "If the remainder still cannot be spent after 50 draws, the item is generated at the

- **REFUTED:** REFUTED — the finding quotes a stale R6.7 and proposes, almost word for word, a correction the live RULES.md already carries.

1. The quoted text does not exist. `grep -n "three deployed characters" /workspaces/lucid-winds/plans/marrowdeep/RULES.md` returns nothing. R6.7 (line 311, committed, working tree clean) reads: "One relic at a time: its card, then the target row as small cards each showing what they wear in that slot and the point delta. **The target row is the DEPLOYED THREE during a quest and the LIVING ROSTER between quests** (audit CORRECTION: the screen is also where a Commission, the second most expensive Renown purchase, and a retired or dead character's gear are kept, and none of those happen while anyone is deployed; three cards are visible at a time, ordered by the point delta in that slot, best first, and the row scrolls sideways past three. Pillar 6 is about cards DEA

- **REFUTED:** The finding is already pinned, in the same words, one bullet above the line it cites. RULES.md line 425, **R8.0b THE PRICE INDEX (audit)**, reads: "Every Hall price below is multiplied by `BALANCE.PRICE_INDEX[deepest Depth ever completed]` and rounded to the nearest 5. It defaults to the Renown multiplier itself, 1, 1.5, 2.25, 3.4, 5.1, so a Commission runs 40, 60, 90, 135, 205. Income multiplies twice over (more stages AND the Depth multiplier: about 43 Renown a Depth I run against about 273 at Depth V) while every price the spec prints is a constant, so without the index a single Depth V run buys six Commissions and four Scar excisions, Renown stops being a decision after Depth II, and the 25 Renown that brakes the Recruit farms erodes to nothing. The index also makes farming a shallow Depth pointless, which is the direction the game already wants. Set the row to all ones to get the sp

- **REFUTED:** Already decided by the exact rule the finding cites. RULES.md R8.4 (lines 449-455) reads in full: "**Quests completed** increments once per quest, on the FINAL boss falling (a Depth II or III quest holds two bosses, and counting both would halve every unlock), **and only when the quest was played at the DEEPEST UNLOCKED Depth** (audit CORRECTION: with no Depth qualifier the cheapest road to Marrowdeep is fifty Depth I runs, about four hours of the shortest and safest content, arriving at Depth V with a roster that has never seen a Strike of 3, a fourth Aspect or a sealed stage. The thresholds and the fifty quest total are preserved exactly: 3 wins at I, 7 more at II, 15 more at III, 25 more at IV.) A shallower run still pays Renown, relics and Marrow; it just does not buy depth. Depth unlocks: II at 3, III at 10, IV at 25, V at 50."

That is the finding's PROBLEM statement and its PROPOS

- **REFUTED:** R8.7 already decides this, in the same words and with the same example. The finding quotes R8.7 only as far as "then stock Callings not already represented, until three" and stops one clause short of the sentence that rules it: "**At least one of the three cards is always a stock Calling** (audit CORRECTION, so `legacySlots` caps at 2 and the spec's `legacySlots: 2` becomes 1 at start): without it, buying the 2 Marrow Legacy slot up to 3 permanently deletes stock Callings from every future deal, so an account whose dead are a Vanguard, a Zealot and a Warden could never roll a Cutpurse again, at any tier, for ever. The cheapest purchase in the game must not narrow the character pool for the life of the account." (RULES.md lines 468-472).

That is the finding's problem statement (permanent narrowing), its severity argument (cheapest purchase, irreversible), its worked example (Vanguard/Zea

- **REFUTED:** Refuted, on four counts.

1. It is already recorded, nearly verbatim, as open Director call 7e in plans/marrowdeep/HANDOFF-MARROWDEEP.md section 10 ("The real pacing is about ten times faster than the spec's prose says... Depth II about twenty minutes, Depth III about an hour (the spec says roughly hour 10), Depth IV about three hours, Depth V about six... The retire or run decision first bites at quest four or five... the unlock counts are left exactly as written because moving them retunes everything else. The call is what happens after six hours: a sixth Depth, or a reason to replay Depth V (the Sigil offer, which now re rolls per Depth, is the natural candidate)"). Same numbers, same "do not move the unlocks", same Sigil-offer candidate. AUDIT.md's own protocol names section 10 as the place a Director call is recorded, and RULES R2.6 cites "the plan's section 10" as that register. Th

- **REFUTED:** RULES R7.4 already decides this, in the same words and with the same arithmetic, and the finding's headline numbers are measured against a default that no longer ships.

1) It restates a DECIDED line. R7.4 is titled "Strikes, CORRECTED (audit)" and carries the whole finding inside it: the same failure chain (attackers + R7.2 retarget + R13.6 => 2 per character per round, dead in round two at Toughness 4 / Armor 0 against 10 Aspect hit points broken in about six checks worth 9 to 15 damage), the same 20,000 quest measurement (81 percent per character, 78 percent wipe), the same discovery that spec 8.6 is a PRE BOSS number (13.45 / 31.50 / 1.47 and 0.404 Legacies, "8.6 almost to the decimal"), and the same four measured levers verbatim (Toughness 9 -> 17.2 / 23.6 / 12.8; Toughness 10 -> 10.5 / 14.6 / 7.7; STRIKE 1 with Toughness 8 -> 8.7 / 12.9 / 6.1; hit points 2/2/3 with Toughness 6 -> 3

- **REFUTED:** RULES R1.9 already decides this, in the finding's own terms. Its DECIDED line reads: "The composed cap, CORRECTED (audit), and it is the one that keeps pillar 3 alive: `floor + total flat` on one stat may never exceed 6, one under the top of the TN band. The two caps the spec states are in different sections and were never composed: a d8 at its half die floor of 4 plus the three permitted flat points reads a minimum of 7, so from d8 upward that character could not fail ANY check in the game, ever, under any Sigil but Shivering, for eight points spread over three items that a Depth I budget already affords. At the cap the worst possible roll still leaves a live check at TN 7. The resolver applies it at equip time and the Character screen greys the line that does nothing, the way R1.7 already greys the fourth flat point." That is the finding's problem statement (two caps never composed, d8

- **REFUTED:** The arithmetic is correct but the finding is not undecided; it is a restatement of a ruling the plan already made and already shipped into the acceptance gate.

HANDOFF-MARROWDEEP.md P3.5 (lines 464-469) is the harness law, and it already reads: "The wipe band is NOT the spec's 8 percent, because the spec's three death numbers cannot all be true at once (audit): with at least one death at 35 percent and a full wipe at 8, the expected deaths per run are at least 0.27 + 0.24 = 0.51, which forces a per character rate of at least 17 percent ... Anchor on the two that the economy needs (0.4 Legacies and 35 percent at least one death) and the third is derived: per character 13.3 percent and a full wipe at 2.5 percent or less. Tuning to the printed 8 would be tuning to a point that does not exist." The band that ships is already "wipe between 0.5 and 6", not 8. Director call 1d (line 702) state

- **REFUTED:** The finding is a verbatim restatement of a RULES line, and its central factual claim is backwards.

1. ALREADY DECIDED, with the same numbers. RULES.md:345-347 (R6.11, fourth bullet) reads: "The once per stage family is four to eight times the flat family per point. A reroll fires on about 7.5 stages a quest for 0.89 extra passes per point against a +1 flat's 0.11. R13.3 already cuts it (per fight, not per round); if the grid still shows it dominating, the price is 6, not 3." The finding reproduces the 4x-8x ratio, the 0.89, the 0.11, the 7.5 firings and the price-6 remedy. It adds no measurement the audit did not already make. R6.11 is a DECIDED line: 3 now, 6 on evidence from the grid. The finding brings no grid run, so it does not move the trigger.

2. "R13.3 makes it worse" is FALSE. R13.3 (RULES.md:639-644) says the opposite in bold: "At the boss, a ROUND is a stage for every [stage

- **REFUTED:** Refuted, because two of the finding's three legs restate RULES DECIDED lines (and misquote one of them), and the third is a prose arithmetic slip the same audit already recorded, with a conclusion that survives the correction unchanged.

Leg 2, the Depth V drop table, is decided. The finding says "R6.1 keeps both sentences". It does not. RULES.md:267-271, R6.1(b): "Depth V uses its own 11.3 and 11.4 rows (0 / 45 / 40 / 15, budgets 4 / 7 / 10). The section 9 prose line 'Relics drop at Relic rarity only' is the error: two tables beat one line", with the reason (the prose reading exhausts the twenty uniques in four quests and makes the Vault and boss "+1 tier" a no op) and the escape hatch ("If Stephen wants Relic only at Depth V it is one BALANCE row"). R9.1 at RULES.md:514-516 carries the corrected row and adds "This sentence used to restate the error and a gate was being written to enfor
