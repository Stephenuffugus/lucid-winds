/* CREASE's one stamp. Every relative import in satellites/crease, CORE's included, carries exactly `?v=`
   plus this string (tools/lint.mjs), and a change to CORE bumps it. */
export const STAMP = '20260916a';
