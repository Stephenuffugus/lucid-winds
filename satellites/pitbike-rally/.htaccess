# Modules + data + styles + the shell must revalidate — a 7-day cache strands
# returning players on stale game code. ETags make most revalidations a free 304.
<FilesMatch "\.(js|json|css|html)$">
  Header set Cache-Control "no-cache, must-revalidate"
</FilesMatch>
