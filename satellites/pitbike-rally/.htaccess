# Modules + data must revalidate — a 7-day cache strands returning players on
# stale game code. ETags still make most revalidations a free 304.
<FilesMatch "\.(js|json)$">
  Header set Cache-Control "no-cache, must-revalidate"
</FilesMatch>
