# RIPCORD — handoff

**What it is:** a spinning top battle game. Single file vanilla HTML, CSS and JS,
mobile first, no build step, no framework, no physics library, no network, no
currency. It works offline forever.

**Where it lives:** `satellites/ripcord/`. The shipping file is `index.html`,
which is generated; edit `src/` and run `node tools/bundle.js`.

**One command tells you whether it is shippable:**

```bash
node tools/check.js          # every gate, at gate sample sizes
node tools/check.js --fast   # smaller samples for iterating
```

It must print **ALL GATES PASSED**. Nothing merges otherwise.

---

## 0. Where this came from, and what is mine

Stephen sent a zip from a wedding on 2026-08-30 containing a tuned physics core
(`sim2.js`), a hand drawn winding grader (`wind.js`), a working prototype, a
balance harness, a ladder generator and a thirty page brief. **All of the design
in this game is his and most of the hard physics was already done.** The reduced
heavy symmetric top model, the rising top behaviour, the aggressor split in the
collision solver, the spin direction physics and the whole winding design came
in the zip and are unchanged except where a comment at the line says otherwise.

This session ported it into the fleet and built the rest of the work plan.

---

## 1. What is built

| Phase | What | Gate |
|---|---|---|
| A | Ported into the repo, path independent bundler | `tools/bundle.js` |
| B | Storage shim, save state, two tab merge | `test/playthrough.mjs` |
| C | Tier expansion to 110 parts, 5,111,040 chassis | `tools/partaudit.js` |
| D | Seven tuning operations, free and reversible | `test/determinism.js` |
| E | Sixteen rigs, every one a physics modifier | `test/rigtest.js` |
| F | Ladder to 25 rungs, five leagues, five bosses | `tools/ladder.js`, `test/bosstest.js` |
| G | Uri, Taya and the target range | `test/modetest.js` |
| H | Eighteen abilities, nine triggers | covered by the harness |
| I | Pass the phone, two players on one device | `test/playthrough.mjs` |
| — | The whole game shell, menu to battle | `tools/shots.mjs` |
| — | Sky Wolf embed protocol, manifest, offline worker, icons | — |

**Not built:** Phase J, the 3D viewer. The brief puts it last on purpose and
gates it on the workshop already being fun. Field mode, the endless generator
from brief section 9.5, is also not built; the ladder now covers every part
without it, so it is content rather than plumbing.

---

## 2. The files

```
index.html            the shipping file. GENERATED. Do not edit it.
src/sim2.js           headless physics, 110 parts, modes, bosses. No DOM, ever.
src/wind.js           the winding grader. Unchanged from the zip.
src/rigs.js           sixteen named synergies, expressed as physics modifiers
src/store.js          the ONLY thing allowed to touch storage
src/audio.js          every sound synthesised from the physics; no files
src/play-shell.html   the whole UI, with slots the bundler fills
src/ladder.json       25 opponents. GENERATED by tools/ladder.js.
src/version.json      the build stamp

tools/bundle.js       src -> index.html. Refuses to ship an unfilled slot.
tools/check.js        every gate, one command
tools/harness2.js     the balance matrix and pacing. Unchanged from the zip.
tools/partaudit.js    can every part be made good, and is any tier power creep
tools/balance.js      searches a part's stats until it is in band. Measure, do not guess.
tools/sweep.js        compares candidate stat blocks for one part, side by side
tools/ladder.js       generates the 25 rungs and verifies the curve
tools/catalog.js      -> docs/CATALOG.md
tools/assets.js       -> docs/ASSETS.md
tools/shots.mjs       screenshots at a real phone size, and measures the controls
tools/make_icons.py   the three app icons
tools/make_thumb.mjs  the portal card image, from a real frame of play

test/determinism.js   same seed, same round, a thousand times. Includes a mutation test.
test/rigtest.js       every rig must change at least 8 percent of rounds
test/modetest.js      Uri, Taya and the range each have their own pacing targets
test/bosstest.js      each boss must refute one strategy and be beatable by another
test/playthrough.mjs  drives the real game in a real browser, through every mode
```

---

## 3. Architecture rules, unchanged and non negotiable

- `SIM` has no DOM references, no renderer references and no globals but its
  export. The 2D build and the future 3D build consume the identical state.
- The renderer owns nothing. It reads and draws; it never writes to a top.
- **A rule lives in the simulation, not the renderer.** The modes and the bosses
  moved into `sim2.js` for exactly this reason: a renderer that keeps its own
  copy of a rule will one day disagree with the test that says the rule is fine.
- Fixed 120 Hz step, decoupled from the render loop, capped at 8 substeps.
- Determinism matters. `SIM.mulberry(seed)` is the only randomness in any test.
- No storage API is called anywhere except in `store.js`.
- No currency, no shop, no energy bar, no IAP. Parts come from winning.

---

## 4. Things this session got wrong, and what they cost

These are here because every one of them looked right at the time.

**The part auditor was wrong three times.** Its header carries the full account.
The short version: uniform sampling measured parts in chassis that disagreed with
them; a "coherent" sampler then handed every attack build the worst possible
weight layout and made the numbers worse; and a hill climb over eight match
evaluations overfit so badly it reported a part's *maximum* as lower than its own
average. Search is the wrong tool when one evaluation carries eighteen points of
standard error.

**The rig test measured two rigs against zero.** `RIGS.apply` back fills to the
cap of two, which is right for the game and wrong for a test isolating one rig.
Deadweight read at plus nineteen win points and was nearly cut for being
overpowered; almost all of it was a second rig riding along. `apply` now takes an
`exact` flag.

**The balancer optimised a statistic nothing reads.** It computed pull over four
reference chassis; the gate computes it over forty coherent random builds. Both
were correct answers to different questions, and parts came out of the balancer
"in band" and straight into the audit as ten points of power creep.

**The balancer's own metric drifted.** Its build sampler drew from a stateful
RNG, so measuring the same unchanged part twice gave two different numbers. A
coordinate descent over a moving target is a search over noise: it "improved" a
blade's pull from 35.9 to 38.0 and wrote the change out.

**`STORE.kind()` touched localStorage outside a try.** In a browser where the
property is a getter that throws, `typeof localStorage` runs the getter. It is
called during boot from the settings screen, so the whole game died before the
first frame. The playthrough now installs a throwing getter on purpose.

**`buildLadder` still read the old single drop shape** after drops became four a
rung. It threw during boot, which killed the frame loop, and the only symptom was
a black canvas. No test caught it. A screenshot did.

**One Shot was a buff wearing a drawback's name.** Triple wear on the first
strike and forty percent after averages 1.27 times normal over three
connections, and rounds here have a handful of connections. Repriced so the
crossover is at two.

---

## 5. The open balance questions, and they are Director calls

Three stats in this catalogue are priced as though the simulation consumes them
and it does not. All three were found by measuring one stat at a time with
everything else held still, which is what `tools/sweep.js` is for.

### 5.1 `radAdd`, the assist slot's reach, is a straight cost

| radAdd | best chassis |
|---|---|
| 0.0006 | 57.3% |
| 0.0010 | 47.5% |
| 0.0014 | 45.0% |

Twelve points, one stat, pointing the wrong way, and it is not one part's
problem: every Stock assist with `radAdd` at or under 0.0008 sits between 47 and
49 percent across the reference chassis, and every one at or over 0.0010 sits
between 40 and 46.

The mechanism is real. `radAdd` raises the moment of inertia, which protects
spin, and it raises the CONTACT radius, so the two tops meet sooner and more
often. Extra contacts pay only if you win them, so reach is worth having on an
aggressive build and is a pure cost on a passive one.

### 5.2 Rim grip does almost nothing, and what it does is negative

Sweeping `gearMul` across a sixtyfold range with everything else held still:

| gearMul | 0.05 | 0.38 | 1.00 | 1.80 | 3.00 |
|---|---|---|---|---|---|
| mean | 48.4% | 48.6% | 48.1% | 46.6% | 46.5% |

Two points across the whole range, and the low end wins. `jt` is clamped to
`muMax * |jn| * jtCap`, which is 1.86 times the normal impulse, and on any real
contact the unclamped value is far past that, so `mu` has no consumer. It is the
same wall that made Spin Thief measure 0.0 points until it was given a `jtCap`
lever in `rigs.js`.

Raising the ceiling does not fix it. Measured:

| jtCap | grip 0.20 | grip 1.00 | grip 2.35 |
|---|---|---|---|
| 3 (shipped) | 47.8% | 48.1% | 46.6% |
| 8 | 51.8% | 49.5% | 49.1% |
| 14 | 52.1% | 50.1% | 47.2% |

Low grip still wins at every setting. Friction is symmetric here: gripping the
other top also scrubs your own rim. Grip only pays when you are the SLOWER top,
and a stat that pays only when you are losing is not a stat anybody buys. Twelve
parts in the catalogue currently spend budget on it.

### 5.3 `taken` dominates everything

The recoil a blade absorbs is by a distance the strongest number in the game.
Holding a blade's other stats still and moving only `taken`, a wide stamina blade
goes from 33 percent mean at 0.66 to **66 percent mean and an 85 percent best**
at 0.46. Halo, the strongest Stock blade, sits at 0.60 and it is no coincidence.

That is roughly a point of win rate for every 0.007 of `taken`, which is an order
of magnitude more leverage than any other blade stat carries.

### What to do about it

The catalogue is balanced as it stands, empirically, and passes. These are not
bugs; they are a stat economy where three of the numbers on the card do not mean
what the card implies. Options, and the choice is yours:

1. **Leave it and stop pricing them.** Cheapest. It means several parts are
   shaped around stats nobody wants, and the workshop is quietly lying about what
   matters.
2. **Reprice.** Cut `radAdd` and `gear` ranges hard, widen `taken` ranges so the
   numbers on the card match their leverage, re-verify everything.
3. **Make them mean something.** Break the aggressor tie toward the larger
   radius so reach earns its contacts; make rim friction asymmetric so gripping
   costs the faster top more than the slower one. Both are physics changes and
   both need the whole harness re-run.

Nothing is blocked on this. It is written down because it will otherwise be
rediscovered from scratch, and because two of the three were found by a reviewer
reading the formulas rather than by any gate.

## 6. Other things worth knowing

**The ratchet height band is structurally weak.** Height feeds `cogH`, which
costs stability, cushion AND precession rate while paying back only strike
height, so the whole 90mm band tops out near 48 percent, stock parts included.
The balancer's floor sits just under it deliberately; a floor above it cannot be
satisfied by any stat change.

**The weight count curve is not flat and that is on purpose.** Fitting some
weights beats fitting none by about thirteen points, because weights create
imbalance and section 6.2 of your brief rewards imbalance deliberately. The gate
therefore measures counts one to four, where the spread is about nine points with
no monotone winner. The reasoning is written at the check in `partaudit.js` so it
can be disagreed with.

**Round p10 is still 1.3 seconds.** Your brief flagged it and it is unchanged. A
small tail of rounds ends almost instantly, probably spawn overlap.

**Two things a player can see must not share a word.** The catalogue shipped
with three things called Ballast (a core, a ratchet and a rig), three called
Millstone, two called Ingot, and an assist and a tuning operation both called
Shim. Every one appears in the workshop. Most of it had a single cause: all ten
stock ratchets are named by their CODE, because your brief says the name encodes
the geometry, and the ten new ones arrived with prose names instead. Restoring
the convention fixed three of the four on its own. There is now a gate. One
deviation from your brief: the tuning operation "Shim" is called **Pack**,
because Shim is also one of your assists and the operation's own description
already says "packs the thread"; say the word and I will put it back.

**A part can pass every balance gate and still be another part with a new name.**
One Forged blade shipped as a Stock blade renamed, every stat within three
percent, and it passed everything because it WAS a balanced part. There is now a
near duplicate check in the auditor: two parts in a slot that differ by less than
six percent of the slot's own range on every stat, with the same drawback and the
same ability, fail. A reviewer caught the original by reading the table.

**Nothing has been on a real phone.** Everything here was measured in headless
Chrome at 375x667 and 320x568. The portal card is marked `beta` for that reason.

---

## 7. How the game is put together

### The five slots and the counterweights
Unchanged from your brief. 22 cores, 22 blades, 22 assists, 20 ratchets, 24 bits
is 5,111,040 chassis, times 46,666 weight configurations.

### Tiers
Tier is not power. Stock is 50 parts with forgiving trade offs. Forged is 40 that
push one stat about a quarter past the Tier 1 range and pay more back on another.
Relic is 20 that take one stat to an extreme and carry a named drawback the
simulation enforces: Glass, Greedy, Cold Start, Loose Lock, Hungry, One Shot,
plus Skittish and Shear which were added during the expansion. Each is one line
of physics; none is a special case.

### The ladder
Five leagues of five rungs. Twenty ordinary opponents are generated, measured
against a reference panel and kept only if they land on the difficulty curve;
the curve is verified monotonic before the file is written. Each drops four
parts, which covers all 90 non Relic parts once the 10 starters are counted.

The five bosses are handwritten and each is a block of flags read in the match
loop. Not one reaches into `stepTop` or `collide`. Each is the only source of the
Relics in its own slot, one per win, so finishing a Relic set means going back.

### The modes
Pangkah is the striking match. Uri is endurance with no contact, which is
expressed by not calling the collision solver rather than by a flag inside it.
Taya pins the loser and gives the winner one free strike, costing lock wear that
carries into the next round. The target range is solo, aimed by where your circle
bulges. Pass the phone is two builds and two winds on one device, and it is
unlocked from the first session because that is where the drama lives.

---

## 8. Deploying

```bash
node tools/check.js                       # must print ALL GATES PASSED
node tools/bundle.js                      # regenerate index.html
git push origin add-sproing-jumper:main   # this is the deploy
```

Bump `src/version.json`, the `?v=` on the service worker registration in
`src/play-shell.html`, `SHELL_VERSION` in `sw.js` and the portal card's `?v=`
together. This host edge pins a bare `sw.js` URL for seven days.

After deploying, grep the live HTML for the new build stamp. A 200 is not
evidence.

---

## 9. What to do next, in order

1. **Play it on a phone.** Nothing else on this list matters as much. The portal
   card stays `beta` until you have.
2. **Decide the `radAdd` question** in section 5.
3. **Field mode**, brief section 9.5: generate opponents on demand at the
   player's measured strength using the `ladder.js` sampler. Infinite content,
   zero authoring.
4. **Look at the p10 round tail**, brief section 15.
5. **Phase J, the 3D viewer.** `docs/ASSETS.md` is generated from the live
   catalogue and carries the common mount spec. Do not model anything until the
   mount is fixed.
