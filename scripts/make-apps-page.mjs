#!/usr/bin/env node
/* portal/apps.html — the Practical Apps page (Stephen's design, 2026-08-17).
   Regenerate with:  node scripts/make-apps-page.mjs   (from repo root)

   Sections, names and taglines come from the LIVE SWS hub page at
   https://sws-apps-9646d.web.app/ — the single source of truth, generated in
   the SWS-apps repo by design/hub.mjs. The art is this repo's own copies in
   portal-assets/sws-thumbs/ (Stephen's thumbnails — see the SWS-apps repo,
   apps/<slug>/marketing/thumb-256.png, for the originals). If the hub gains
   an app, copy its thumb here, then re-run this.

   NEVER hand-edit the card list in portal/apps.html; regenerate. Hand edits
   are how his thumbnails got silently lost twice before. */
import { readFileSync, writeFileSync, existsSync } from 'node:fs';
import { dirname, join } from 'node:path';
import { fileURLToPath } from 'node:url';

const REPO = join(dirname(fileURLToPath(import.meta.url)), '..');
const ORIGIN = 'https://sws-apps-9646d.web.app';
const hub = await (await fetch(ORIGIN + '/')).text();

const sections = [];
const secRe = /<section class="group" data-group>\s*<h2>([\s\S]*?)<\/h2>([\s\S]*?)<\/section>/g;
const cardRe = /<a class="card" href="\.\/([a-z-]+)\/"[\s\S]*?<b>([^<]+)<\/b><span>([^<]+)<\/span>(<span class="tag">Shared online<\/span>)?/g;
let s, total = 0;
while ((s = secRe.exec(hub))) {
  const [, title, body] = s;
  const cards = [];
  let m;
  cardRe.lastIndex = 0;
  while ((m = cardRe.exec(body))) {
    const [, slug, name, line, shared] = m;
    const png = join(REPO, 'portal-assets', 'sws-thumbs', slug + '.png');
    const svg = join(REPO, 'portal-assets', 'sws-thumbs', slug + '.svg');
    if (!existsSync(png) && !existsSync(svg)) {
      console.error(`MISSING ART: portal-assets/sws-thumbs/${slug}.png — copy it from the SWS-apps repo first`);
      process.exit(1);
    }
    const art = existsSync(png) ? `/portal-assets/sws-thumbs/${slug}.png` : `/portal-assets/sws-thumbs/${slug}.svg`;
    cards.push({ slug, name, line, shared: !!shared, art });
    total++;
  }
  sections.push({ title, cards });
}
if (total < 24) { console.error(`only ${total} apps parsed from the hub — did its markup change?`); process.exit(1); }

const card = (c) => `      <a class="app" href="${ORIGIN}/${c.slug}/" target="_blank" rel="noopener">
        <img src="${c.art}" alt="" loading="lazy" decoding="async">
        <b>${c.name}</b>
        <small>${c.line}</small>${c.shared ? '\n        <span class="tag">Shared online</span>' : ''}
      </a>`;

const body = sections.map(sec => `
  <section>
    <h2>${sec.title}</h2>
    <div class="grid">
${sec.cards.map(card).join('\n')}
    </div>
  </section>`).join('\n');

const html = `<!doctype html>
<html lang="en">
<head>
<meta charset="utf-8">
<meta name="viewport" content="width=device-width, initial-scale=1, viewport-fit=cover">
<title>Practical Apps · Sky Wolf Studios</title>
<meta name="description" content="${total} free practical apps from Sky Wolf Studios — planners, sign-up sheets, checklists, PDF tools. No ads, no subscriptions, no tracking.">
<meta name="theme-color" content="#080c09">
<meta property="og:title" content="Practical Apps · Sky Wolf Studios">
<meta property="og:description" content="${total} free practical apps — planners, sign-up sheets, checklists, PDF tools. No ads, ever.">
<meta property="og:image" content="https://lucidwinds.com/portal-assets/sws-thumbs/specials-planner.png">
<link rel="icon" href="/favicon.ico">
<link rel="preconnect" href="https://fonts.googleapis.com">
<link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
<link href="https://fonts.googleapis.com/css2?family=Fredoka:wght@500;600;700&family=Nunito:wght@400;600;700;800&display=swap" rel="stylesheet">
<style>
/* Generated by scripts/make-apps-page.mjs — regenerate, don't hand-edit.
   Night-forest arcade palette, same tokens as portal/index.html. */
:root{
  --bg:#080c09; --panel:#121a13; --panel2:#1a251b;
  --ink:#f1e9d8; --cream:#f1e9d8; --muted:#98a28e;
  --line:#2a3722; --gold:#e4bd5f; --leaf:#82ddcd;
  --disp:'Fredoka',ui-rounded,'Segoe UI',system-ui,sans-serif;
}
*{box-sizing:border-box}
body{margin:0;background:var(--bg);color:var(--ink);
  font:16px/1.5 'Nunito',system-ui,sans-serif;
  padding:0 14px calc(48px + env(safe-area-inset-bottom))}
.wrap{max-width:1040px;margin:0 auto}
header{padding:26px 0 6px}
.back{display:inline-flex;align-items:center;gap:7px;color:var(--leaf);text-decoration:none;
  font-weight:700;font-size:.95rem;min-height:44px}
.back:hover{text-decoration:underline}
h1{font-family:var(--disp);font-weight:600;color:var(--gold);
  font-size:clamp(2rem,6vw,2.9rem);line-height:1.02;margin:6px 0 10px}
.promise{display:inline-flex;flex-wrap:wrap;align-items:center;gap:8px;
  background:var(--panel);border:1px solid var(--line);border-radius:999px;
  padding:10px 18px;color:var(--muted);font-size:.93rem}
.promise b{color:var(--cream)}
section{margin-top:30px}
h2{font-family:var(--disp);font-weight:600;color:var(--cream);font-size:1.35rem;margin:0 0 12px}
.grid{display:grid;gap:12px;grid-template-columns:repeat(auto-fill,minmax(150px,1fr))}
.app{display:flex;flex-direction:column;gap:8px;text-decoration:none;color:inherit;
  background:var(--panel);border:1px solid var(--line);border-radius:18px;padding:12px}
.app:hover{border-color:var(--gold);background:var(--panel2)}
.app img{width:100%;aspect-ratio:1;border-radius:12px;object-fit:cover;display:block;background:var(--panel2)}
.app b{font-size:1.02rem;line-height:1.25;color:var(--cream)}
.app small{color:var(--muted);font-size:.85rem;line-height:1.4}
.app .tag{align-self:flex-start;margin-top:2px;padding:2px 9px;border:1px solid var(--line);
  border-radius:999px;color:var(--leaf);font-size:.7rem;font-weight:700;
  letter-spacing:.04em;text-transform:uppercase}
footer{margin-top:44px;padding-top:22px;border-top:1px solid var(--line);
  color:var(--muted);font-size:.9rem;text-align:center}
footer a{color:var(--leaf)}
@media (max-width:480px){.grid{grid-template-columns:repeat(2,1fr)}}
</style>
</head>
<body>
<div class="wrap">
  <header>
    <a class="back" href="/portal/">◂ Back to the Arcade</a>
    <h1>Practical Apps</h1>
    <p class="promise"><b>No ads. No subscriptions. No tracking.</b> ${total} free tools — most keep everything on your device; the shared ones say so.</p>
  </header>
${body}
  <footer>
    <p>Every app is free, forever. Made by Sky Wolf Studios — the same studio as the <a href="/portal/">Arcade</a>.</p>
  </footer>
</div>
</body>
</html>
`;

writeFileSync(join(REPO, 'portal', 'apps.html'), html);
console.log(`wrote portal/apps.html — ${total} apps in ${sections.length} sections`);
